REVIEWED — blueprint 0.2.30, mapping set 0.3.40, reviewed by Vahid Mohammadi.
Profile: 19 value(s) from the file given. Every line below is what the run would type, attach or leave, for that profile.

The plan has 1 blocker(s); no preview is built for a form that cannot be filled:
  - no_mapping: startDate — Required field "What date do you want to start:*" has no mapping. A specialist decides what belongs here — it is not something to work out while a form is open.
