REVIEWED — blueprint 0.2.29, mapping set 0.3.39, reviewed by Vahid Mohammadi.
Profile: 18 value(s) from the file given. Every line below is what the run would type, attach or leave, for that profile.

The plan has 5 blocker(s); no preview is built for a form that cannot be filled:
  - no_mapping: studyTerm — Required field "How do you want to study?*" has no mapping. A specialist decides what belongs here — it is not something to work out while a form is open.
  - no_mapping: qualification — Required field "Qualification applied for:*" has no mapping. A specialist decides what belongs here — it is not something to work out while a form is open.
  - no_mapping: startDate — Required field "What date do you want to start:*" has no mapping. A specialist decides what belongs here — it is not something to work out while a form is open.
  - no_mapping: fundingSourceKnown — Required field "fundingSourceKnown" has no mapping. A specialist decides what belongs here — it is not something to work out while a form is open.
  - no_mapping: fundingStage — Required field "What stage are you at with your funding?" has no mapping. A specialist decides what belongs here — it is not something to work out while a form is open.
