# State of the system — the standing account

**Version:** 0.64.0 · **Date:** 2026-09-07 · **Written for:** someone who knows the product and has
not read the code.

> **This document is the standing account, not a snapshot.** `where-we-are.md` is a per-phase
> journal that accretes; the CHANGELOG is per-version. This is the one place that is rewritten to
> stay true, and it is updated at the end of every phase. Where it disagrees with an older document,
> this one is right.

---

## 0 · The one-paragraph version

AAS takes a student who has explicitly decided to apply to a specific university course and carries
that application from conversation, through preparation, to a filled form on the real portal —
stopping before submission. Twenty-six packages and five applications, all five deployable processes —
the sixth, a research build, was removed in P53 (ADR-0086). **2,829 tests, 145 files, zero skipped**, against real PostgreSQL and Redis, in two lanes —
the twenty-one files that launch a browser run serially, everything else in parallel.
One hundred and forty architecture decision records, all accepted (ADR-0006 §3 amended in P38). **AWS spend is no longer $0:** one bucket, one
customer-managed key and one revoked role exist, created by Vahid on 2026-09-09 to verify the S3
checksum binding (ADR-0092 §4); the amount is the billing console's to state. Nothing is deployed. The journey works end to end against a *replayed*
portal. **Run A was made against the real one on 2026-09-22:** attempt 10 signed in to Sheffield's
postgraduate form through the consent path and filled, saved and read back six pages of Part 1 on
your own account, under the fifth signature — **one signature, yours, admitting your own account
and nothing else (ADR-0118, P144)** — then handed over and stopped at `ready_to_submit`. The
portal's own line: *"Completed Part 1."* **Nothing was submitted, and nothing here submits.** What
that run proved, what it cost and what it did NOT prove — Part 2, course choice, supporting
documents, a second portal, a created account, a real student — is the standing record,
[`what-run-a-proved.md`](./what-run-a-proved.md). The product still needs a second reviewer before
any real student (blocker 2) and Bedrock credentials, which are with you. **What that
one signature leaves uncaught, and why a second reviewer is a precondition of serving a real
student, is written for the developer who comes next in
[`what-was-skipped-to-get-it-standing.md`](./what-was-skipped-to-get-it-standing.md).**

---

## 1 · Every phase, in order

### Foundation — before the phase numbering (0.1.0 → 0.18.0, 2026-08-27 → 08-30)

| | What it did | Why |
|---|---|---|
| Phase 0 | Inventory of the existing AskiMate system, integration contract, repository structure, AWS bootstrap plan, and the first twenty-nine ADRs | Decide before building; nothing since has had to be undone at this layer |
| Phases 1–5 | The domain core (states, transitions, events, branded values), document extraction, field mapping, the fillable session, validation/preview/authorisation, and the orchestrator with a replay harness | The product's actual logic, built pure and I/O-free so it could be tested without infrastructure |
| `0.1.0` | The first versioned state — one version across every manifest, and a changelog that does not invent history | Eighteen manifests said `0.0.0`; there was no way to say what "this build" meant |
| `0.2.0` | `PostgresCaseStore`, passing the same contract suite as the in-memory one; versioned migrations with checksums | A case now survives the process that created it — in-memory blocks the second run, not the first |
| `0.2.1` | A safety claim that was wrong, and the enforcement that made it true | The first of many: a stated guarantee measured and found not to hold |
| `0.3.0`–`0.6.0` | Durable execution in four steps: the run model, the `WorkflowRunStore`, orchestrator checkpointing, and "a consequential action happens at most once — or we admit we cannot tell" | A crash mid-application must not create a second account or a second submission |
| `0.7.0`–`0.11.0` | The inline secure turn, phases A–D: the password request takes its place in the conversation, the composer stays live, the React secure control, and one client rather than two diverging ones | A student typing a password must not leave the conversation, and the guard must not fail open |
| `0.12.0`–`0.12.1` | Contract-first OpenAPI for both services, checked against the code; then migrations for the independent product | The wire contract is a reviewed artefact, not a description written afterwards |
| `0.13.0` | `packages/conversation` becomes the single domain authority | Two implementations of the same conversation decision had drifted |
| `0.14.0`–`0.16.0` | The Conversation Service exists; the browser talks to it; a real browser types a real credential into a real cross-origin Secure Plane | The two-origin design stops being a diagram |
| `0.17.0` | Seven coverage gaps closed — which found two real defects | Coverage claimed is not coverage held |
| `0.18.0` | The runner no longer holds a password: the Fill Agent consumes the credential inside the Secure Plane (ADR-0042) | The component that loads pages we do not control is the most likely thing to be compromised |

### The numbered phases (P1 → P34, 2026-08-31 → 09-06)

| Phase | What it did | Why |
|---|---|---|
| **P1** | A student conversation creates and owns a durable run | Until this, a run had no identity a conversation could refer to |
| **P2** | A controlled fixture portal that actually requires an account | The first portal in the repo that behaves like a real one |
| **P3** | Two blockers resolved by decision rather than by working around them | Working around a blocker is how a system acquires a second truth |
| **P4** | The Conversation Service becomes the first production caller of the Secure Interaction Service | The secure step opens from the run, not from a test |
| **P5** | The Automation Runner claims leased work — it pulls, nothing calls in (ADR-0045) | The browser process gets exactly one inbound port, and it is CDP |
| **P6** | A real account created on a portal, with a password nobody in the system can read | The end of the credential design, proved by asking the portal to log the student in |
| **P7** | The first real end-to-end journey: a student asks, and ends up with an account | Four planes and two databases in one run |
| **P8** | The runner fills the application form; the plan crosses as value + provenance (ADR-0046) | A branded `ConfirmedValue` cannot survive a wire, so it is reassembled through the one mint |
| **P9** | Durable multi-page execution; page progress lives in the intent ledger (ADR-0047) | A lease must name the page it holds, or a retry refills the wrong one |
| **P10** | A run that stops says so and can be picked up; the intervention record (ADR-0048) | A stopped run that nobody can see is a stranded student |
| **P11** | The run driver drives the case state machine; a student's authorisation is captured through it (ADR-0049) | Two models of a case, joined by a moving join rather than a shared id |
| **P12** | The account lifecycle completes through the student's own decision; a case can conclude (ADR-0050) | Handover is the student's act, and a case that cannot end is a leak |
| **P13** | The student answers in the conversation; a correction can reach the portal (ADR-0051) | The interview stops being a demo script |
| **P14** | A background worker owns autonomous progression (ADR-0052) | The system must act when nobody is watching |
| **P15** | A student can stop: cancellation is reachable and does not strand the account (ADR-0053) | The first reachable terminal state |
| **P16** | The Automation Runner's supervisor — competing claims, lapsing leases, real concurrency | Decided by PostgreSQL, so a fake proves none of it |
| **P17** | The intent is durable *before* the action, not after (ADR-0054) | ADR-0045 §4 claimed a crash was detectable when it was not |
| **P18** | A process refuses to start when it is not safe (ADR-0055); the five-deployable map | Startup is where a misconfiguration becomes a security property |
| **P19** | Identity delegated to a real OIDC provider; verification established at login (ADR-0056) | ADR-0038 described a guard nothing implemented |
| **P20** | The catalogue loads a reviewed artefact and can prove that is what it loaded (ADR-0057) | An approval must bind to content, not to what the content says about itself |
| **P21** | A case opens from an offer the student accepted, not an identifier they sent (ADR-0058) | An id is not something a student can consent to |
| **P22** | The student can read what they are authorising (ADR-0059) | Measured: only the test suite could complete an authorisation |
| **P23** | The journey is startable and readable over published routes (ADR-0060) | A client must never become a second source of workflow truth |
| **P24** | The run says what it is waiting for, and the hash that decision must carry (ADR-0061) | One of the four student decisions could not be formed by any client |
| **P25** | The student client, in the service that serves its origin | ADR-0039 answered this for services and left it open for the client |
| **P26** | The interview's *question* reaches the student, in the log (ADR-0062) | ADR-0051 gave the answer a home and left the question homeless |
| **P27** | The published contract is checked against the real Express router (ADR-0063) | Six discrepancies had accumulated because the guard read enums and never `paths` |
| **P28** | The interview's decision to stop reaches the system (ADR-0064) | A student who refused three readings stranded the run for ever |
| **P29** | A run only a person can carry on stops, and says so (ADR-0065) | The driver discarded the orchestrator's hand-over wherever it was raised |
| **P30** | Three declarations name a document; one decides — the reviewed mapping (ADR-0066) | Measured in both directions; the other two are inert and now structurally barred from the planning path |
| **P31** | AAS obtains documents; what blocks it is policy, not design (ADR-0067) | P30 had recorded this as an open product question. It was decided in ADR-0010/0016/0022 and already implemented |
| **P32** | The storage boundary refuses what ADR-0022 says it refuses (ADR-0068) | The gate was a helper an implementation was trusted to call; it is now the only thing `store` accepts |
| **P33** | The document transport boundary, costed — no ADR, because no decision was made | Turning a question into a decision with two costed answers is the correct outcome; writing an ADR would record a decision nobody made |
| **P34** | An authorisation is spendable only in the application it names (ADR-0069) | `DisclosureSubject.caseId` had recorded the application since Phase 1 and nothing ever compared it |
| **P35** | `BlueprintPage.requiredDocuments[].documentRef` → `fieldRef` (ADR-0070) | Two layers shared one name, and the repository contained both readings of it. Free today; costs every catalogue approval once one exists |
| **P36** | A stopped run reaches a person, and the notice carries nothing about the student (ADR-0071) | The whole recovery design was built and waited on somebody running a CLI. The student was told; the specialist was not |
| **P37** | ADRs 0005–0021 read against the code (ADR-0072) | Five phases running had each caught an older ADR asserting a guarantee the code did not provide. Thirteen of seventeen hold; the rest produced two fixes, two corrections and two open findings |
| **P38** | One application per submission identity, and the second one the student asks for (ADR-0006 §3, amended) | P37's two open findings, closed together. `claimSubmissionKey` is armed at case-open; a re-application opens a NEW case referencing the prior one, because the old same-case increment produced a terminal case that could never move |
| **P39** | A declared capability with no production caller fails the build (ADR-0073) | P37's question, asked by `pnpm run verify`. It found `openReapplication` uncalled on its first run — the constructor ADR-0006 §3 had named four hours earlier |
| **P40** | A run a person is holding is returned to the student, never restarted (ADR-0074) | A student whose application stopped for a specialist came back to a 500. They now land where they left it, their questions still arrive, and an advancing decision is refused with a reason rather than a 404 |
| **P41** | A refusal reaches the person it is for (ADR-0075) | Two codes existed so a client could tell them apart, and the page had words for neither; underneath, both services published 413 and 415 and neither had ever sent one — a body over the limit came back as `500 internal_error`, blaming us for something only the student could shorten |
| **P42** | The student can instruct the second attempt the system refuses them into (ADR-0076) | ADR-0006 §3's two-step exchange was built, published and tested in P38, and nothing but a test had ever called either half. The refusal now opens the second attempt when the server says the prior application has concluded, shows the advice first, and sends the student's own words |
| **P43** | Two determinations, made structural rather than written down (ADR-0077) | B1's claims question answered — no document period rests on defending legal claims, only the audit record — and enforced by `validateSchedule`; and a special-category field cannot be extracted, because a profile field that is not classified against Article 9(1) does not compile |
| **P44** | Documents are held and reused, and the twelve periods are set (ADR-0078) | B5 decided A — hold and reuse — which corrected five B1 rows written before it, and all twelve periods are determined. The vault stays shut on B2, and the report now says a retention policy is not permission to store |
| **P45** | A document running out is the student's choice, once, in writing (ADR-0079) | The expiry thresholds ADR-0078 left absent, approved and configured. The recorded wording is the wording shown, because the record takes the branded warning and not a string |
| **P46** | The visa path is a compliance boundary, not a scheduling gap (ADR-0080) | ADR-0021's decision stands, its reasoning was weaker than the truth, and the word OISC appeared nowhere in the repository. Registering the boundary found that `blocksApplication` — the line ADR-0021 calls the single one that keeps the visa journey out — has no production caller |
| **P47** | The browser tests run in a lane of their own (ADR-0081) | Two full runs in five failed on a browser test that passed alone. The seventeen files that launch a browser now run one at a time; the contention was fixed, not a single assertion or timeout. The list of them is checked in both directions against what the files actually do, following imports, because grepping found twelve of seventeen |
| **P48** | The record of what cannot be reached is checked too (ADR-0082) | `checkMinorGate` — the minors gate — was in the enforced register and in no row of this document, and `packages/notify` sat under "Declared but unreachable" with a cell beginning "Reachable." Neither is a code defect and neither would have failed a build, which is why the prose is where a false record now accumulates |
| **P49** | An ADR and the lists of it must agree (ADR-0083) | Three hand-written records of one fact, none compared. ADRs 0001–0004 were accepted on 2026-08-26 and a partial index edit fifteen minutes later missed four rows, so this document grew a blocker asking Vahid to decide what he had already decided. The first finding of this shape where the record claimed LESS than the system did, not more |
| **P50** | The census is generated, and its arithmetic is checked (ADR-0084) | §7's per-area table had six of twenty rows wrong and `scripts` — 264 tests — with no row at all, but the finding that mattered needed no run: the table did not add up to its own stated total, by 136. The tilde in "everything else ~346" is what made that unfalsifiable |
| **P51** | A published demonstration is guarded on what it shows (ADR-0085) | Five of twelve published commands had no guard — P37 fixed the walkthrough and left the rest. Exit code is not the property: `extraction-demo` accepting its INVENTING reader would exit 0 and mean ADR-0016's grounding guarantee had stopped holding |
| **P53** | The research build is removed, and what it proved is kept (ADR-0086) | `apps/chat-integration` retired by Vahid's decision. Four properties in it were about the two DEPLOYABLES, not the research build, and moved to `scripts/plane-separation.test.ts` rather than going with it. Taking it away found that the PRODUCTION client's `postMessage` had never been covered by the wildcard-origin rule |
| **P54** | The four lawful-basis determinations (ADR-0087) | B2 answered by Vahid — the last policy blocker on documents, open since P31. Contract for storing and disclosing, consent only for a minor's route, authorisation required for sending. A national ID additionally needs Article 9(2)(a); a passport does not. Ten of seventy (type, purpose) pairs now pass both gates, against none before |
| **P55** | The Schedule 1 document must exist before the processing (ADR-0088) | `national_id` is refused at the storage gate until the DPA 2018 Sch. 1 appropriate policy document exists — structurally, naming what is missing and who holds it, so re-enabling is a deliberate act with a name on it. `other / audit_evidence` is recorded as **decided**-refused rather than open, giving the lawful-basis side the third state the retention side has had since ADR-0023 |
| **P56** | A national ID leaves the supported document types (ADR-0089) | Vahid, reading P55's result: a type refused at the gate is *"unreachable surface with a policy justification attached"*. `national_id` leaves `DocumentType`, and the Article 9 clause, the separate-consent gate and the whole Schedule 1 module go with it — `assertStorable` is back to two gates. The determination was correct and is recorded in full, so re-adding starts from the reasoning; the Sch. 1 document comes first. Found the schedule parser CASTING `documentType` rather than checking it |
| **P57** | The document transport: the gates run before a byte is accepted (ADR-0090) | **B4 answered** — the last of ADR-0067's five blockers, and the only one that was engineering. An upload is a two-step exchange: the declaration runs the storage gates, and only then does a route exist that will read a body. `assertStorable` has a production caller for the first time since P39, so the declared-but-unreachable table goes **7 → 6** — the first entry ever to leave it. The store is in-memory and refuses to start in production; the durable encrypted one is next |
| **P58** | robots.txt is read, obeyed and kept; requests are paced (ADR-0091) | The two preconditions Vahid set before any run against a live site. Obeying is half of it — every run writes `robots.json` with the file verbatim, because a crawler that quietly complies leaves no evidence that it complied. An unreadable robots.txt allows **nothing**. A one-second floor nothing can lower. Adding a second rule to a one-rule guard surfaced **four** defects in the old code, including one that failed CLOSED and looked exactly like the rule working |
| **P59** | The document never enters a process we run (ADR-0092) | Vahid's decision, in his words, for the durable store: the gates run, then a pre-signed upload is minted, and the bytes go browser → S3. Reached through the boundary check REFUSING the in-process design — a control working, recorded as such. `packages/keys` and a sixth deployable considered and not taken, with his reasons. Rests on one unverified fact about S3, so this phase builds the verification (`pnpm run verify-s3-checksum`, a judgement that cannot say VERIFIED without its control experiment) and the provisioning request. **Nothing provisioned, port untouched.** Run 2026-09-09 against Vahid's bucket: REFUTED under the SDK's hoisted checksum, then VERIFIED on both halves with the checksum a signed header (ADR-0092 §4). Port still untouched |
| **P60** | An upload URL cannot be minted unbound (ADR-0093) | Condition 1 met on 2026-09-09 (both halves VERIFIED, ADR-0092 §4) and Vahid: *"Reshape the port … make that structural in the minting code, not a note in the ADR."* `BoundUploadUrl` is a branded type with one producer, and the producer reads the URL it minted back and refuses one whose signature does not cover the checksum header — the SDK's default, which hoists it into the query string where S3 never reads it, is refused rather than recorded. Proven against the real SDK offline. `DocumentVault` has no method that takes or returns bytes; `PUT …/content` and `acceptBytes` are gone, `POST …/confirm` asks the bucket what it holds; the S3 vault exists in the Conversation Service. **Not built:** durable metadata and production wiring (production start still refused), CORS, the retrieval's caller |
| **P61** | Document metadata is durable, and the transport starts in production (ADR-0094) | Migration 0017: `document_intakes` and `documents`, with **no column that could hold a byte** (asserted on the SQL and on `information_schema` at startup). `PostgresDocumentIntakePort.take` is one `DELETE … RETURNING` (three racing takes, one wins) and **re-runs the gates** against the schedule in force. The S3 vault's metadata sits behind a record store; the one durability check tells three in-memory cases apart. The entry point builds the transport from `AAS_DOCUMENTS_BUCKET`, `AAS_DOCUMENTS_KMS_KEY_ARN` (an ARN, checked) and `AAS_RETENTION_SCHEDULE_DIR` — all or none — loading the governing schedule through the parser moved into the domain package. Startup test: the real process starts with `documents=s3`. Nothing sent to AWS. **Not built:** the client's upload control, the retention sweep, the runner's fetch |
| **P62** | The student's page makes the PUT, and the CORS rule is exercised by the PUT it makes (ADR-0095) | The page has a document panel: type from the server's list, file, and the page hashes the file (the ONE hash it computes, stated in its header as the exception ADR-0092 forces), declares, PUTs the bytes to the bucket on the URL the declaration answered with, confirms, and re-reads `GET …/documents`. The purpose is derived by the server from the governing schedule — the page never sends one. The browser test stands a real HTTPS bucket on a second origin that admits **exactly the CORS rule parsed out of `docs/provisioning-request-document-vault.md`**, so the page's PUT is proved against the rule as written, with a real preflight. The three transport codes moved to `REFUSALS`. **Found, not resolved:** the gates' `detail` is on the wire and the contract says no `detail` exists (blocker 18). **Not built:** the retention sweep, the runner's fetch, a document the interview asks for |
| **P63** | Expired document intakes are swept by the worker (ADR-0096) | The worker's fourth job, `sweep_document_intakes`: migration 0018 widens the closed `job_kind` vocabulary; `sweepExpiredIntakes` is one bounded DELETE, oldest first, idempotent; sixty seconds under the same lease discipline as the other three, `AAS_WORKER_SWEEP_MS` to change it. The worker still names no vault — the table it deletes from cannot hold a byte, and the worker's test writes its abandoned rows by hand because the app is forbidden the documents package. The race with `take` is shown not to be one. **Not built:** the retention sweep (`purgeContents` still has no caller), the runner's fetch |
| **P64** | The preview names what the student holds (ADR-0097) | Slice a of the attachment path (§2). `RunDriver` hands the orchestrator the student's held documents from the vault's METADATA store, keyed by document type — one per type, the current one, never a superseded or purged record — so a run whose student holds the passport the mapping attaches reaches `authorise` with it named in the preview, and the authorisation binds to a hash that covers `(fieldRef, documentRef, contentHash)` (ADR-0069). `PreviewDocument.filename` → `describedAs`. The driver names no vault method that yields bytes, asserted. **Not built:** slices b–e; nothing is sent |
| **P65** | One yes over a preview that names each attachment, and the gates refuse in a closed set (ADR-0098) | Vahid's two decisions of 2026-09-10, verbatim in the ADR. Slice b: `renderPreview` writes, per attachment, which document, going where (institution and portal host) and for what (the form's label, this application); `SubmissionPreview.portalHost` is derived from the blueprint's first observed URL and is INSIDE the content hash, so a re-pointed application voids the yes. Blocker 18 closed: the storage gates' five refusals are three published codes (`document_not_retainable`, `document_basis_undetermined`, `document_type_refused`) with words on the page; every `detail` left the wire in the Conversation Service, two of them older than the transport; `no-free-text-on-the-wire.test.ts` refuses the next one. **Not built:** slices c–e; nothing is sent |
| **P66** | Uploads cross as references, and the plane hands a document over only after the gates (ADR-0099) | Slice c. An upload crosses to the runner as four things — which box, which document the mapping named, where the box is — and no bytes, id or hash. `POST /internal/v1/work/{runId}/documents/{documentRef}` under the lease: the run is at execute and names the upload; the captured authorisation still hashes to the preview rendered NOW; a `DisclosureRequestRecord` from what the student saw runs `authoriseDisclosure` (determination 3), then `mayTransmit` WITH THE CASE, then a sixty-second retrieval URL. The runner's `documentSourceFor` fetches, hashes, and mints the brand through `authoriseDisclosure` again — never a cast — before `executePlan` runs `mayTransmit` at the moment of attaching. `authoriseDisclosure` leaves the register. **Found:** the runner's entry point performs `create_account` only; `fillApplication` enters the register (blocker 19). **Not built:** slices d and e |
| **P67** | The page decides whether it can show the secure step before it asks for the capability (ADR-0100) | Closes the two properties ADR-0086 left open. `decideRendering` runs BEFORE `bootstrapSecureStep` over three observed capabilities — this build, `window.isSecureContext`, and a `no-cors` probe of the secure origin the page reads from the new non-minting `GET /v1/secure-origin`; a refusal is a code and a fixed sentence on screen, mounts no frame, and cancels nothing. The journey now builds and serves the real page and the real secure control, and the password is typed into the REAL cross-origin frame; the outbox delivers the receipt through `internalAppend`. **Found:** since P25 the page framed `/v1/secret-requests/{id}/control` while the Secure Plane serves `/control/{id}` — a 404 on the production path for forty-two phases, unseen because the page is not a router and the journey typed by `fetch`. The path is now `secureControlPath` from the contract, held to `secure.v1.yaml` by the drift guard |
| **P68** | Blocker 19 framed as a decision sheet | [`decision-sheet-blocker-19-how-a-runner-is-signed-in.md`](./decision-sheet-blocker-19-how-a-runner-is-signed-in.md). What is already true in the tree (step order, single-use credential, the session dying with the work item, the test-only bridges); five options — one signed-in sitting with `authorise` moved first, a second ask through the secure box (`portal_sign_in`), co-browsing (the only honest form of "the student authenticates in a handed-off session"), a persisted cookie (rejected), password reset (recovery, not a path); side by side; a recommendation; the open-portal fraction stated as unmeasured with two ways to measure it; four questions for Vahid. Nothing built, nothing provisioned |
| **P69** | Blocker 19 decided; the yes comes first (ADR-0101) | Vahid's six answers recorded verbatim: A1 yes; A2 yes, in memory, five minutes matching ADR-0034; B yes, `portal_sign_in` as the resume path only, with the phishing-normalisation argument in the ADR; open portals refused explicitly; C recorded as the plan for a second-factor portal; D no. And one requirement not on the sheet: detect a CAPTCHA or second factor and stop saying which, before slices d and e. **Built:** A1 — `nextStep` returns the account's refusals before the authorisation and its asks after it; the orchestrator, driver, supervisor and journey tests walk the new order; the journey starts at the yes, and the password is asked for only after it. The handover stays ahead of the authorisation, because a stopped run still owes the account back. **Not built:** A2, B, the detection, C |
| **P70** | A runner that meets a CAPTCHA or a second factor stops and says which (ADR-0101 §6) | Vahid's requirement before slices d and e, built. `captcha_met` and `second_factor_met` on the wire; the runner reads the page at four points — the registration form before anything is typed or the handle spent, the page the portal answers with, the application form, and the page a fill was bounced to — with a detector narrower than discovery's signals on purpose (a postcode box is not a second factor). The plane stops the run through the one stop mechanism, naming the challenge, the action, the page, the reviewed observation it contradicts, and for a creation met by a code that the account may already exist; one fixed message per code; `escalated`. **Found:** since P5 `reportWork` recorded every failure as `failed_cleanly` and discarded the code — `needs_the_student` did nothing. The fixture portal presents both challenges; both are met by the real runner |
| **P71** | One sitting, in memory, five minutes (ADR-0101 §2) | Slice d — A2 built. `SessionHold` keeps the browser context a run's account was created in, keyed by run, in memory, and closes it after `SECURE_HOLD_CEILING_SECONDS` idle — one constant in `packages/contracts`, from which the vault's ceiling (ADR-0034) is now defined, so the two lifetimes cannot drift apart. The runner's entry point performs `execute`: the held page, confined to the portal's host, `fillApplication`, the disclosure source of ADR-0099. A claim names the runs the runner holds (`sessions`, required on the wire); the driver offers a run to its holder first and a fill to nobody else. Released when the last page is saved, on a challenge, when the student is needed, and at shutdown. `fillApplication` leaves the register — **five** remain. **Not built:** the resume path (§3): the journey's restart still signs in by a cheat marked as such, through the `adopt` seam P72 fills |
| **P72** | `portal_sign_in` is the resume path, and the run says why (ADR-0101 §3) | B built, as the resume path only. The plane records who last reported a run's session live and until when (`run_sessions`, migration 0019) from the runners' reports and the one ceiling; the contract's `SESSION_ENDING_FAILURES` erase it at both ends. Past it, and only where an account exists, the run asks for the password a second time — purpose `portal_sign_in`, typed once, the reason rendered in the frame — then the `sign_in` step and work kind: the blueprint's login form (`authentication.login`, new and optional), the handle, no plan. `signInToPortal` types, submits and asks the page; the context is the held session and the fill follows. No intent for a sign-in: the Secure Plane's lifecycle is the record. Where the path cannot apply the run says so before a box opens. `portal_password_reset` left the contract; the purposes agree and the drift test asserts it. The journey's restart is driven through the real path; the cheat is gone. **Not built:** slice e |
| **P73** | One intent per document attached, and the record of what left (ADR-0069's third layer) | Slice e, the last of the attachment path. The page's intent key sees its attachments by `documentId@contentHash`, so a replaced document re-offers the page and a page without uploads keeps its key. The Run Driver opens one `attach_document` intent per upload at the claim — `page/field=documentId@hash` — and settles exactly the ones the runner's report names, for this run's case only; `document_transmissions` (migration 0020) is the audit row of what left, written from the same report. A page is not done until every document it carries is recorded as attached; one it saved without naming stops the next claim as the uncertain case, named as the attachment. **Found:** a page whose only content was an upload was never offered — `#nextPage` counted fields to fill and not files to attach. `attach_document` leaves the register — **four** remain |
| **P74** | The attachment path meets a portal that takes a file | The fixture portal grew a documents page — a multipart upload it hashes and shows on review — and the journey's student holds a passport: a metadata row in the plane's own store, bytes behind a vault stand-in the runner fetches by the plane's sixty-second URL. The restart test now goes on to page three through the production performer: the plane opens the `attach_document` intent, the runner fetches, hashes, gates and attaches, the portal holds the same SHA-256, the intent settles and `document_transmissions` has the row. **Found:** the preview named the blueprint's observed host while a run made to a deployment (`portalOrigin`, ADR-0057) sent the bytes to another, so the runner's transmission gate refused — `wrong_destination`, correctly — an authorisation the plane's own gate had passed. The preview now names the deployment's host, so what the student authorises is where the document goes (ADR-0098 amended); a run re-pointed after the yes stops at the yes again. Also: a plan with uploads and no fields to type is a plan (`parseTransportedPlan`) |
| **P75** | The destination inside the yes, made a named property at every level | Vahid: *"Keep it that way, and keep the property that re-pointing a run after a yes stops at the yes again."* Three tests state it: the orchestrator over a state re-pointed after a recorded authorisation; the Run Driver over the database and the catalogue — a run past the yes, the entry re-pointed at a deployment, `advance` stops at `authorise`, the preview names the new host, no runner is handed the work, a yes to *that* host lets it go on, and removing the deployment stops it again; the journey reads the fixture portal's host in the preview the student is shown. **Found:** the rendered text named the host only under each attachment, so a student with nothing to attach said yes to a destination they could not read. Every preview now carries a `Portal:` line (ADR-0059 amended); no hash changed |
| **P76** | The live-run record catches up with what was built | `docs/what-a-controlled-live-run-needs.md` — the document the README calls *"the remaining blockers"* — was dated 2026-08-26 and still said retention was a hard stop, the lawful basis unregistered, persistence in-memory, a `WorkKind` needed for attachments, the interview a terminal harness. Every one of the eighteen areas re-read against the code and the ADRs: two blockers off the list (ADR-0078, ADR-0087), three areas moved, the rest restated as *built and proved against the fixture, unproven on a real portal*, which is the truth. Blocker 9 here struck (done in P73). Vahid's items stay his — the target, the bucket, discovery, the models — with nothing restated that he has not typed |
| **P77** | Deliberate regressions over the attachment path and the destination inside the yes | Eleven mutations, applied to disk and restored byte-for-byte: the transmission gate's case and host checks, the destination in the preview's hash and its deployment reading, the driver's one reading of the deployment, the runner's refusals before a byte is fetched, the wire's HTTPS rule, the settlement's case filter, the hand-over's hash comparison, the executor's gate call. **Ten caught**; making the preview ignore the deployment fails the whole journey, which is P74's defect on demand. **One not caught:** the hand-over's own hash comparison is a second reading of a fact the orchestrator's assessment already refused on, one step earlier; it can differ only in a race, and it is kept and labelled as exactly that. `docs/p77-regression-audit.md` |
| **P78** | The Sheffield target file, the sourced facts, and the network answer | Vahid confirmed the target and gave six things its public pages state; each is recorded with its source URL and retrieval date, as stated by the institution and observed by nobody here. `targets/sheffield-pgt-2026-09.json` exists, parses, and has not been run; the course and the intake year are marked as his to supply rather than invented. The sibling form's *"an email will be sent containing your login details"* is recorded as `portal_issued` to be confirmed by observation, with what the code does with that answer today: creation without a secure step, and no routine sign-in built for a relayed credential (ADR-0101 built B as the resume path for `student_chosen`). The network question answered from the docs and the proxy: the environment's **Network access** level governs; Trusted's list carries `*.amazonaws.com` and not Sheffield; the narrowest widening is Custom with `sheffield.ac.uk`; Full buys nothing the runner's own host confinement does not already refuse |
| **P79** | Attached inspection: the tool reads a browser a person signed in to | The loop A1 made — a reviewed blueprint of the form is a precondition of the account, and a blueprint of a form behind a login needs an account to see it — recorded in ADR-0101 as a known consequence in Vahid's words, and closed by a mode rather than a change to A1. `PlaywrightAttachedInspection` attaches over CDP to a Chromium a person launched and signed in to, opens one tab in their context, and reads: discovery's guard (GET, HEAD, OPTIONS to the target's hosts) on the whole context for as long as it holds it, so the person's own tabs are read-only too and given back on close; navigation allow-listed by prefix; a redirect to the login page recorded as a finding; captures scrubbed of input values. `pnpm run inspect:attached` writes what discovery writes, and `inspect-discovery` reads it. Proved against the fixture portal's login in six tests: the gated page read through the person's session with nothing but GETs; a POST from the person's own tab refused while attached and landing after; a browser signed in to nothing bounced and recorded. **Found:** the service constructs the deterministic model client in every path (blocker 3 amended, checklist areas 7 and 8). robots.txt not applied in this mode, stated in the run record |
| **P80** | The first real attached read, and what it found | Vahid's first run against Sheffield's PGT form: the attach, his session, the guard, the pacing and the exit all worked; the read failed with `page.evaluate: ReferenceError: __name is not defined`. tsx injects esbuild's helper into the serialised in-page script; the three launching sessions shim it on the contexts they create and the attached session did not, and vitest's transform hid it from every in-process test. Vahid: *"Fix the transform, not the script … make the test fail first without the fix."* A test that spawns the real command under tsx against the fixture login failed on that error first, then passed with the shim. Found on the way: the test's first version spawned synchronously and starved the fixture portal it was serving. `run.json` now says which rule refused each request and why |
| **P81** | The first real form, read | Vahid's attached inspection of Sheffield's PGT application: eleven pages, zero failed, nine of them Part 1 form pages, 309 controls, seventeen document slots, Part 2 unread. Recorded in `docs/captures/sheffield-pgt-2026-09-10/`. The finding he named — a `POST getGradingSystemsForCountry.do` fired on the education page — is one link of a chain the structure shows whole: country → institution (typeahead) → grading system (server lookup) → grade, plus a subject search; every other dependency on the form is static show/hide, tabulated. The site's search form on every page is excluded by design. Mandatory fields are marked by `*` in labels and enforced on save, and on five of nine pages the tool read no labels at all. The password question and the three-choices question are answered as far as the captures allow and named as open. Three schema gaps for step 4: options that arrive after another field, a typeahead as a fill mechanism, a repeatable entry. **Found on our side:** the one-time-code heuristic flagged six postcode boxes and wrote a false `mfa` handoff (P82) |
| **P83** | The presigned URL is dated at the mint's `now` | CI #176 went red on the docs-only push `a02d989`: `s3-document-vault.test` refused a URL valid until `16:48:04.000` against an intake at `16:48:03.964`. Root cause on our side, not CI's: the SDK dated the signature at its own clock, truncated to the second, which can land one second after the `now` the bound was computed from, and the exact check in `assertBoundUploadUrl` rightly refused it. `PresignRequest` now carries `signingDate`, set by the mint to its `now` and passed to the SDK's `getSignedUrl`; the in-memory store stamps the same date. A test reproduces CI's failure before the fix — an SDK-like presigner dating at the next second — and passes after. The check is unchanged; only the date the signature carries is |
| **P82** | The observation script stops reading a postcode as a one-time code | The two false signals from the first real form, fixed where they were made. `input[name*=code]` had matched six postcode boxes on Sheffield's contact page and written an `mfa` handoff where there is no second factor; the observe script now requires the name or id to BE a code field, the set the runner's challenge detector has used since P70, or `autocomplete="one-time-code"`. Page-text signals match whole words and read the page with script and style bodies cut out, so "registered charity" and a script that mentions registering no longer make an account-creation page. The signals fixture carries the three false positives; the two new tests failed on Sheffield's exact evidence before the fix. The first version cloned the body to cut scripts out and a cloned `<img>` fetched its source — caught by the CLI test's refusal count; the text is read by walking the tree, and a test asserts the observer fetches nothing. Also regenerates the census CI #177 found stale after P83 |
| **P84** | Two of the four answered, in Vahid's words | Course and intake supplied: MSc Management and International Business, September 2027 (`2027-09` in the target file, so the key carries it; the course page URL's `/2026/` is the page he read it from, not the intake). The password question closed on his direct statement — *"I typed the password I chose. Sheffield did not email me one"* — `student_chosen`, AUTH 1 yes, AUTH 2 no; recorded as his statement with the date, not inferred from a link or the sibling form. Still his: the registration and login pages in a signed-out profile, now for the locators only and specified page by page in the capture README; the inspect-dependencies output; Part 2; the three schema gaps |
| **P86** | ADR-0102 — use the refusal the form offers | Blocker 20 built as Vahid decided it. `BlueprintField.dataCategory`, set by the reviewer and never by discovery; a mapping set is refused while any field is unclassified. `ValueSource.form_refusal` — the value the form offers, a mandatory rationale, the form's own words or nothing — the only source `checkUsable` accepts on a special-category field, refused when misused, not offered or composed. With no refusal mapped the plan blocks `special_category_unhandled`, required or not, and a specialist is asked. The preview lists refusals under *We did not answer these for you*, never among the answers, inside the hash. Transport, wire and both conversions carry the kind. Found while costing the sheet: `profile_field` and the interview's `ask` were typed by the whole registry; both are now `OrdinaryFieldKey`, measured to refuse a special-category key at the line that names it. Eleven tests, all failing before the mechanism. The curated Sheffield draft classifies its fourteen equal-opportunities fields |
| **P87** | The correction recorded, a refusal covers its question's other controls, and the first real mapping set | Vahid's dated correction in ADR-0102: the ethnic-origin list offers *Prefer not to say*; *Information withheld* was the label's wording he reported as the option. `form_refusal.covers`, found while writing the first real set — Sheffield's disability question is twelve boxes and the rule would have blocked the plan on the eleven others; a cover must be special-category, unmapped and covered once, is planned as nothing and named in the preview. The hash now moves with each refusal (P86 had declared it and not hashed it). The equal-opportunities mapping set is written as a draft for Iman's review; the curated draft classifies all 216 fields as proposals; the review pack counts 23 judgement rows and 193 mechanical; a test holds the drafts to the real checks |
| **P88** | Three defects of the discovery tool the first real form exposed | Fixed fail-first, each against the shape Sheffield showed: radio inputs sharing a name become one field whose options carry the submitted values, recorded by the observer (the draft had one field per input and no values); an advance-control candidate is never blank, a sentence containing "start" is not a button, and the page's control prefers an id (the draft had a blank locator on five pages); `inspect-discovery` counts refusals by rule and calls only a write on the target state-changing (the summary had called fifteen off-host tags writes). ADR-0102 §7 now says what the P86 hash test did wrong — it agreed with the code — and the assertion is retired |
| **P89** | The personal and contact pages mapped as far as the registry reaches | The Sheffield mapping set is one draft for the whole blueprint and now carries personal and contact: names, the date of birth as three selects (three new date-part patterns, `D`/`MMMM`/`YYYY`, fail-first), the e-mail twice, the address parts, and the country as a partial option map of eight captured names that refuses any other rather than approximate. A confirmed fixture profile fills them under the real checks. **Raised, not decided:** employment's four required fields have no profile field — the registry collects no employment history — so the plan blocks on them until Vahid decides what the profile collects |
| **P90** | The plan honours `visibleWhen` | Recorded by discovery since the blueprint schema existed, read by nothing until now: a field's condition, and its section's, is evaluated at plan time against the plan's own values, to a fixed point. A field the form hides for these answers is neither filled nor missing — dropped from instructions, blockers and uploads, listed under `hidden`, and not a violation in the validator. Sheffield's two postcode boxes were both planned with one hidden on the page; the international one is now absent for a UK address, and an unmapped required field on a hidden branch no longer blocks. Fail-first on a synthetic address page and on the Sheffield draft |
| **P91** | The entry page read; the AUTH questions answered from it; the registration and login locators authored | Vahid's signed-out read of the entry page and the reset page, three pages, zero failed, recorded unedited. From the capture: AUTH 1 yes, 3 none offered, 6 no CAPTCHA on those pages (tags refused), 8 yes by design; 2 and 5 settled for what the pages show and open for what follows a submit; 4 and 7 not settled by any page read, and not inferred. The curated draft carries the login URL and locators and a registration page first in the walk; the mapping set sends the e-mail from the profile and the passwords to the Secure Plane. The eight facts are recorded with `unobserved` on 4 and 5, and the chooser refuses on exactly those — the design holding: what settles them is Vahid's own account of registering and signing in. Two tool defects the draft showed, fixed fail-first: `password` inputs came back unknown; buttons came back as fields |
| **P192** | **The interview's second phase: the fields whose value has several parts, and the guardian path built inside it (ADR-0140).** `FieldSpec.parse` was `(raw: string) => T | null` — one utterance, one whole value — so the six composites were not data to add but **machinery**: an address has six parts, a passport is a statement or three facts. **Built:** `CompositeFieldSpec` with ordered `FieldPart`s (each with its own reason, shape and parser), `askWhen` so ADR-0117's `none` arm stops the questioning, `OMITTED` as a distinct value for an optional part the student says there is none of, attempts counted per QUESTION (`identity.passport#expiry`) rather than per field, and **one confirmation against the whole value** — because the whole value is what enters the profile. A correction to the whole of a composite is refused and the field is asked again: *"no, flat 4"* could be a new first line or a new second line, and choosing is us answering. **Fields:** `identity.passport`, `contact.address`, and the five guardian fields. Counted rather than estimated (`PROFILE_FIELD_KEYS` against `FIELD_SPECS`): **26 of the registry's 35 fields now have a question — 21 of the 30 ordinary ones and all five guardian — up from 19.** The nine ordinary fields still without one are the five list-valued keys plus `education.highest_qualification`, `education.english_language_test`, `immigration.uk_status` and `immigration.uk_study`. **His rule, applied to every parser before it was written** — *"a value the student did not state is never supplied by us, however obvious the default looks from where we sit"*: `heldOrNone` refuses *"I might be able to find it"*; `passportNumber` requires five characters and a digit, stated as a judgement, because without it `soon` read as a passport number (found by a test I had written wrong, and the parser was tightened rather than the test loosened); **`countryCodeIso2` refuses "Iran"** — there is no reviewed country table, and a half-table fails invisibly (blocker 61). **The guardian fields, at his instruction, inside this phase**: minority is DETERMINED and never asked — a test forbids *are you under 18*, *are you a minor*, *how old are you* in any spec's text, while telling a minor *"because you are under 18"* is allowed because it states back a determination already made; the questions say a person checks this part, because a mandatory review is something the student is owed the truth about rather than only a routing rule; and the guardian is a THIRD PARTY who has consented to nothing and cannot be reached through this chat, while `MINOR_ROUTE` rests on their consent — raised as blocker 62, not decided. **Measured, then guarded:** through the real driver, an entry whose required field was repointed to `contact.address` asked the first line, took the answer, dropped it with the request and stayed `running` — the log carries one event per field and none for a part. The driver now **does not put the question at all** and stops for a person, with a message that says it was not asked rather than that we asked as many times as we should (blocker 60). **Held:** `education.highest_qualification` has no parts — it is a `Qualification`, the shape of one entry of the list class he reserved pending his read of Part 2 — and still escalates with *"will not improvise"*, held by a test. `Address.postalCode` stays required and a student in a country without postcodes cannot complete an address (blocker 63). | 2026-09-23 |
| **P191** | **The interview's first phase: the twelve scalar fields it could not ask for.** *(One count corrected 2026-09-23 in P192: the figures below are wrong. Counted from `PROFILE_FIELD_KEYS` against `FIELD_SPECS`, the registry holds 35 keys — 30 ordinary and 5 guardian — and before P191 seven had a question, so **23 of the 30 ordinary fields had none**, not twenty of twenty-seven. The phase's work is as described; only the arithmetic was wrong.)* Twenty of the registry's twenty-seven had no question, so a person edited a file — his words, *"Nothing above matters to a real student until that is closed."* Built: `identity.country_of_birth`, `identity.sex`, `study.intended_start`, the three finance fields, `residence.country`, the three ADR-0115 claims, `residence.in_uk_now` and `residence.uk_entry_date`. **Three parsers, each refusing rather than guessing:** `yesNo` reads yes or no and returns null for *maybe*, *sometimes*, *I think so* — ADR-0115's claims are signed at the bottom of an application; `yearMonth` reads `2019-09` and *September 2019* and refuses `09/08` for `isoDate`'s reason, and never invents a day (*"Sheffield asks a day because it asks a day, not because anyone knows it"*); `money` **refuses a bare number** — 20000 is not an amount until the student says of what, and defaulting it to sterling because the university is British is ADR-0136's failure exactly. **Red first:** six tests, all six failing on a missing spec. **One existing test moved rather than being weakened** — the escalation test named `finance.sponsor_name`, which now has a question, so it names `contact.address`, which is a composite and its own phase. **Not in this phase, deliberately:** the five guardian fields, reachable only on the minor path, which is a mandatory-review category and deserves a phase that looks at it rather than a spec added in passing. | 2026-09-22 |
| **P190** | **Three records at his instruction, before the interview: the agent-portal possibility, the password sheet, and the ADR-0108 answer.** Blocker 58 and blocker 59 carry the first and third; [`decision-sheet-holding-the-students-password.md`](./decision-sheet-holding-the-students-password.md) carries the second, with the addition he asked for — **what we do when the portal's own terms forbid sharing a password**, which he rightly took out of the DPIA and made a question about whether the student can honestly agree. Four positions priced, including *do not hold it and accept the per-attempt cost*, which is today's answer with the cost now measured at six passwords across ten attempts. **The disclosure already happens** at the moment the student types into the box; holding extends its duration rather than creating a new category of act, and the sheet says so rather than being comfortable about it. | 2026-09-22 |
| **P189** | **The fifth signature, and the refusal test taken down.** Vahid signed `sha256:e2a10113c9e0f3536c1081cb3f51a7ea682ed6fceec9694d391a708a85f2b080` in commit `eb4e83e`, one signature for ADR-0138's condition and ADR-0139's wording together, his own account only, **from his own hash computation** — *"Signed eb4e83e, sha256:e2a10113…, from my own hash computation."* The directory loads again and admits exactly that account, so the assertion that it refuses was true for two hours and false the moment he signed: taken down, as in P186. **Also corrected, because they described the present and were wrong:** ADR-0138's *Consequences* named `sha256:cdb43561…` as the hash awaiting a signature — it was never signed and never will be — and this table's P187 row said the same. Both now name what was signed. The journal and the changelog are left as they stood: they record what was true at the time. | 2026-09-22 |
| **P188** | **The sentence that says what the university is being told named four field names, two of them the wrong document (ADR-0139).** Vahid, reading P187's preview before signing it: *"Those are field names, not documents a student knows. And they mislead: `officialCertTranslStatus` is the FINAL ACADEMIC CERTIFICATE — its own options read 'I will upload my final certificate now/later' — not a translation."* He drew the line against the seven field names he let stand on the 16th himself: *"it is the sentence that says what we are telling the university, it is the part the student authorises, and it names the wrong document. ADR-0059 is broken on exactly the line that matters."* **Built:** `RequiredDocument.title` — `{ text, readFrom }` — said in *You attach yourself* and in the coming-later sentence; with no captured title the preview SAYS the form does not name the document rather than falling back to `fieldRef`. **Which capture, asked and answered:** NEITHER of the two he offered. His screen reads are a person's report with nothing for a build to check; the option labels name an act (*"I will upload my final certificate later"*), not a document. What the repository holds is the companion radio's `row_text` label, captured **2026-09-10**, and carried character for character by two of the three later reads (the third carries the field refs instead — it could not reach the row text positionally, P126, so it is silent rather than contradicting) — heading fused to help text and truncated mid-sentence. Splitting it by a pattern (all six continue *"This is …"*) would be ADR-0136's failure in a new costume, so the split is a person's judgement and the BUILD holds it to the capture: `text` must be a whole-word prefix of `readFrom`, and `readFrom` must BE the companion's captured label on that page. **Red first, twice:** without the preview change the title never appears and the label is printed; without the parser change it round-trips to `undefined` and the hash does not move. Blueprint 0.2.28, mapping set 0.3.34. **One signature covers P187 and P188** — his words: *"There is no point signing cdb43561 and re-signing in an hour."* **Signed** `sha256:e2a10113…` on 2026-09-22, commit `eb4e83e`, his own account only, from his own hash computation; the test that asserted the refusal came down in P189. | 2026-09-22 |
| **P187** | **Blocker 57 decided B by Vahid, and built: a document slot carries the condition in the student's own terms (ADR-0138).** Attempt 9's `certificateStatus` refusal was real — Sheffield shows the *Proof of Registration* and *Most Recent Transcript* block only while a qualification is still running, and ticks both radios *Not required* itself once it has ended. His words: *"the condition in the student's own terms, on the slot, signed. When the qualification's end is 'completed', the two pre-completion slots — `certificateStatus` and `transcriptStatus` with their files — are not planned, not previewed and not set."* **Built:** `RequiredDocument.askedWhen` — `{ part, is, because }` — read off the repeating page's own entry through the same rule a mapping's `part` format uses, answered PER ENTRY before anything is planned for it, dropping the slot AND its companion together and recording both in `hidden` with `whenEntrySays`. An entry that does not answer the path stops the plan (`render_refused`) rather than guessing; `askedWhen` on a page that does not repeat is refused at parse. **Red first, twice:** without the plan change the finished qualification still planned the slot for both entries and an unreadable path was ignored in silence; without the parser change `askedWhen` round-tripped to `undefined` and the hash did not move — the P97 shape. **The discontinued branch, read rather than guessed:** `education.js` has no notion of a discontinued qualification at all; it goes by the date, and a qualification the student left ended in the past, so the page hides the block exactly as for `completed`. So the condition is `is: ["expected"]` — one rule covering all three ends. **The known divergence, named and accepted by him:** the page compares the AWARD date (else the end date) with today, so a completed qualification awarded in the future would be shown the block while the plan does not set it — the save then leaves two radios unanswered, this portal drops the entry silently, and the listing count catches it as *uncertain*, not succeeded. His words: *"That is a loud failure, and I accept it."* **The entry changed and the fourth signature is VOID:** blueprint 0.2.27, mapping set 0.3.33, and `what-will-be-typed.md` loses the two *UploadLater* lines, the two *You attach yourself* lines and two names from the coming-later sentence — four documents previewed where the page shows four. The approval was removed, not re-pointed. **`sha256:cdb43561…` was never signed:** reading this preview Vahid found the ADR-0139 fault and said *"There is no point signing cdb43561 and re-signing in an hour"*, so the two changes moved the hash once, together, and he signed `sha256:e2a10113…` on 2026-09-22 (`eb4e83e`). | 2026-09-22 |
| **P186** | **Attempt 9's silent box, read out of the portal's own code — and the refused line now names the check that refused (ADR-0137).** The month maps took (`Sept`, `June`, `July`) and the institution box held again; `certificateStatus` refused, silently. **Vahid's reading holds, and `education.js` says more than he expected.** `endDateChanged` shows `preCompletionDocuments` on ONE branch only — `endDate > startOfMonth`, the end or award date in the FUTURE — and hides it (`display:none`) on both others; that branch `return`s early, and the two that do not then tick **`certificateNotRequired` and `transcriptNotRequired`** themselves when those radios are unset. So for a qualification that has finished the page hides that pair AND assigns them, which is exactly what his own `summary.do` reads for both of his completed qualifications: *Not required or uploaded elsewhere*. The blueprint records **six** slots on page 7 and the pair is the first two — his count of four visible of six, confirmed. **What is NOT established:** that the two radio groups are DOM children of `#preCompletionDocuments`. No capture holds the page's HTML; the chain is the two names, the branch, the self-ticking, the count and the summary line. The line built below settles it from the runner's own mouth on the next attempt. **Built:** `ControlNotActionableError` with a closed set of the runner's OWN checks — `not_visible`, `not_enabled`, `not_editable`, `not_present`, `did_not_settle` — and `ours` beside `drift` on every outcome, so a failure line may print the runner's words and still never the portal's about the student's answer. An act on a control is bounded at five seconds rather than Playwright's thirty. Red first, through a real browser on a radio inside a `display:none` container and on a disabled box. **Not built, because the schema cannot say it:** `FieldCondition` is equals/not_equals/is_checked/is_not_empty/in over another FIELD's value, and Sheffield's rule is a DATE COMPARISON AGAINST TODAY over two pairs of selects. Blocker 57, with three options and none chosen. **And main was red on arrival:** his re-signature (db5fe3d) added the approval without updating the P185 test that asserted the entry was unsigned — corrected here. | 2026-09-22 |
| **P185** | **Attempt 8 filled the institution box — and one month name refused. Every option map in the entry is now audited against the captured list, and the build refuses one that is not (ADR-0136).** P184's fix held live: **18 of 19 boxes on `education.do` took their values**, the institution box among them, for the first time in eight attempts. `startDateMonth` did not: the set sent `Sep` and Sheffield's list says `Sept`. Three of its twelve names are four letters — June, July, Sept — and the synthetic profile's education dates are exactly those three months, so **all three would have failed in turn**. **The audit, all 45 option maps against the blueprint's recorded options:** 41 agreed, 3 did not (`startDateMonth`, `endDateMonth`, `awardDateMonth`, each on Jun/Jul/Sep), 1 has no list to check (`subjectSearch`, a free-text box). The nationality, visa, employment, date-of-birth, country, radio, qualification, grading and grade maps were all read from captures and are all correct — their notes say *as captured* and *in the select's own spellings*. **Where the claim came from:** ADR-0112's Built (P134) section, *“the months by the selects' three-letter names”* — written as a fact four days after the 2026-09-10 capture that records `June, July, Sept`, and contradicted by the blueprint on the same page of the same signed entry. Nothing on the record supports it. The same ADR's last consequence names the task it skipped. **The `saveBtn` shape again.** Withdrawn in ADR-0112 itself. **Built:** `checkUsable` refuses `option_map_not_offered` — the question ADR-0102 already asks of a refusal, asked of the maps that carry the student's own answers, walking the format tree because a format nests. Red first. **What moved in what-will-be-typed.md:** three typed values and two identifiers, five lines of 124 — `Start:: Sep→Sept`, `End:: Jun→June`, `Date of Award:: Jul→July`, the set 0.3.31→0.3.32, and its own reference hash. **The entry is UNSIGNED:** the content hash moved, so the approval is void and was REMOVED rather than re-pointed — an agent cannot sign for him. The directory refuses the entry, loudly, until he signs `sha256:56388e65…8f21ef`. | 2026-09-22 |
| **P184** | **The cause of the institution box, measured at last: the widget the runner has just finished with takes the focus back, and the next box's keystrokes land in it (ADR-0135).** The fixture Vahid asked for, built exactly: the committed `tom-select.complete.min.js` (2.3.1) and `education.js` served byte for byte, the widget constructed with the settings he read off the live instance, Sheffield's own `loadInstitutionSearch` behind a local search endpoint, and the runner driven at it **through `attach()`**. It reproduced the live failure on the first run — no request by any method, the grading `POST …?institutionCode=` with an empty code, no script error, country set, the same line word for word. **Then measured, not reasoned about:** reading the widget's state through the runner's sequence showed `active: institutionCountry-ts-control` and the country box holding `y of Sheffield` — *the keystrokes went into the box the run had already finished with*. Patching `HTMLElement.prototype.focus` on the page named the caller: `de.open` → `de.focus` → `control_input.focus()` on the COUNTRY widget, about ninety milliseconds after the click on its entry had resolved. The vendor source says why it does not stop — `open()` ends in `focus()`, `focus()` queues `setTimeout(onFocus, 0)`, and `onFocus` with `openOnFocus: true` calls `open()` again. **Ours, not the portal's:** a person cannot type into the next field milliseconds after choosing in the previous one, which is why it worked by hand every time and why four explanations that blamed the portal all survived. **Built:** the focus is drained at the end of the act that caused it and must stay settled across four consecutive reads — an earlier version that broke on the first matching pair still failed four runs in five; the box is read back and retyped, bounded at three; and a box that still did not take the text SAYS so, naming the element that holds the focus as structure and never as content. **And the lookup log now records a request when it is ASKED**, so an earlier act's answer cannot be reported as a later box's doing, and a request with no answer yet reads as exactly that instead of as no request at all (blocker 55). Red first: five runs in five before, five in five after. Blockers 49 and 55 closed; 54 answered by his own read. **What it does not claim:** that Sheffield will fill. The fixture is the portal's code, not the portal. | 2026-09-22 |
| **P183** | **Sheffield ships Tom Select 2.3.1, so ADR-0133's reason was wrong — and the code says what did and did not happen at that box.** Vahid fetched the page's own scripts after attempt 7 (`docs/captures/sheffield-pgt-2026-09-21-education-scripts/`, commit 33aa901). **The version:** `tom-select.complete.min.js` reads *Tom Select v2.3.1*, and the minified source binds `se(i,"input",…)` on the control input. A one-act fill dispatches `input`, so attempts 4 to 6 would have reached `onInput` too. ADR-0133's Context is **amended** and its 1.x explanation withdrawn for this portal; the decision stands and the typing is **not** reverted — it is what a person does, it satisfies both versions, and it costs the portal the same one lookup. **The load path, read from `education.js`:** `loadInstitutionSearch(query, callback)` reads `#institutionCountry`.value and calls `GET ./ajax/institution/search.app?name=…&studyAbroad=…&country=…`; `studyAbroad` comes from a global `erasmusStudyAbroad` the file never declares. It runs only when `canLoad()` — `!!settings.load && !loadedSearches.hasOwnProperty(query)` — and **the construction that would set `settings.load` is inline in `education.do`, which no capture holds** (blocker 54). **`institutionChanged()` refutes half of Vahid's guess:** it reads two values, toggles the India warning and the unlisted block, and calls `loadGradingSystems()`. It does **not** destroy, rebuild, clear or disable the Tom Select, and does not replace its load function or settings — nothing in `education.js` does. **The POST is explained:** `loadGradingSystems()` is the only producer of that URL, and its first branch is taken whenever `#institution`.value is not one of five named strings — **the empty string passes all five**, giving `institutionCode=` exactly as observed. Tom Select's `updateOriginalInput()` fires a real `change` on the original element on every `addItem`, so the country choice's own `change` → `institutionChanged()` → that POST; the watcher records **responses**, so an earlier act's answer can land inside a later box's window (blocker 55). **What the runner types, confirmed:** `entries.text`, the recorded label *University of Sheffield* (23 characters); the *9 characters* in the line is the value `SHEFFIELD` it must match on `data-value`, not what it typed. **Checked and ruled out:** the fill does not retry in-page — `fillApplication` opens the form once — so `loadedSearches` caching is not what silenced the search. Read-only phase; blockers 54 and 55 raised. | 2026-09-21 |
| **P182** | **The guard the record has claimed since ADR-0014 now runs where a real fill runs (ADR-0134).** P181 found it installed only inside `open()`, which nothing a deployable calls. Vahid: *“install it on the attached context. Not as a new rule — as the rule the system already claims.”* **Built:** `guardContext`, called by `attach` and by `open` alike — host allow-list, robots.txt on every request and not only navigations, forbidden endpoints, and the record of everything state-changing. Once per context and with the logs on the context, because a run fills page after page in one held context and Playwright runs only the most recently added handler: stacking one per page item would have left the earlier sessions' logs silently empty, which is the failure this phase exists to remove. **And the sentence he banned cannot be printed:** `WriteLog` is armed by the guard as it goes on, and an unarmed log answers *NOTHING WATCHED this run's network* instead of *the portal saved nothing*. The test that asserted the old sentence failed first, which is how it was found to be the only thing that had ever read that log. **The classification, his choice of the three: navigation or not** — `isNavigationRequest()`, no reviewed list and no body read. A portal saves a page by submitting that page's form, which navigates; a widget asking what institutions exist does not. **A label on what is recorded, never a refusal**, and the caveat stands in the ADR: none of it tells a draft save from a submission, and that guarantee is where it has always actually been — the type and the click guard. **The seeing, both halves (blocker 52):** the watcher records every method on the portal's own host, so the line may say *no request* and mean it; and a `pageerror` listener says the page's own script FAILED, never what it said, because an uncaught error on a form page can quote the value that caused it. Red first at every level, including a fixture in Sheffield's own shape — a search by POST carrying its parameters in the query string, and a handler that throws before it fires. **Three tests go through `attach()`, the door production uses**, and all three fail without the fix: the guard refuses an off-allow-list host the PAGE reaches for, what the page sent is recorded and labelled, and one context is guarded once however many page items attach to it. **What the reachability check would need to catch this shape** is written in its own header, three ways, none built — it passed throughout, exactly as its header said it would. Blockers 51 and 52 closed; **53 raised: `forbiddenEndpoints` is installed and always empty**, because the blueprint's `SubmissionModel` records a page and a control and no URL. | 2026-09-21 |
| **P181** | **The guard he suspected is not the cause — because on the path a deployed run takes it is not installed at all.** Read from the code, in answer to his three questions of 2026-09-21. **One — what a fill does with a request the PAGE makes that is not a GET, on the portal's own host: it lets it through, and records nothing.** Two layers. `decidePreparationRequest` has no method rule; it refuses on exactly two grounds — a host off the allow-list, and a URL under a `forbiddenEndpoints` prefix the blueprint records — and `performer.ts` passes no such list. Above that, the only `context.route` in the fill session is inside `open()`, and nothing a deployable runs calls `open()`; it cannot, because ADR-0046 makes the fill attach to the held, signed-in context, and `attach()` installs no handler. `SessionHold.open` installs none either. So during a real fill there is **no host allow-list on the page's own requests, no robots check on its subresources, and an empty `WriteLog`** — `summarise()` would answer *“the portal saved nothing”* after three saved pages. The 10 September refusal he cites is real and from a different guard on a different path: `decideDiscoveryRequest`'s method rule, which is inspection, read-only. **Two — whether such a request was made during attempt 6: unanswerable, and nothing can answer it.** `#writes` and `#blocked` are fed by the handler that is not installed, the P179 watcher drops every non-GET at its first line, and the context carries no trace, no video and no HAR. **And the line he was shown says more than it knows, which is mine.** *“The page made NO request of its own to the portal”* is a claim about methods the watcher cannot see — while the one lookup on this page whose shape is on record is `POST …/getGradingSystemsForCountry.do?institutionCode=&noCache=…`, a read whose parameters travel in the query string. Corrected to name GET and to say the rest is unwatched; red first, and the failing run printed back the exact sentence he was sent. **Three — whether a script error is visible to the runner: no.** There is no `pageerror`, `console`, `requestfailed` or `crash` listener anywhere in the repository; the fill session's only page listener is `page.on("response")`. If `institutionChanged()` threw, nothing would know and nothing would say. **Why the build did not catch the dead guard:** `check-reachability` asks whether a capability has a production call site, and this one has — inside a method whose own callers are a test and a script. That is the limit the check states about itself in its own header. Nothing else built: blockers 51 and 52 raised, blocker 49 reopened, and the rule for telling a lookup-by-POST from a write is **proposed, not chosen**. | 2026-09-21 |
| **P180** | **The runner types key by key, because Tom Select 1.x listens for `keyup` and a fill dispatches `input` (ADR-0133).** Vahid measured it by hand on the live form: typing `sheff` opened the list with all eleven entries; the runner's fill asked the portal nothing, which P179's line is what showed. He withdrew a console test of his own as void — his Network panel hid Fetch/XHR — leaving the runner's line as the evidence. **Read from the vendor source:** 1.x binds `keyup` and has no `input` listener (`tom-select.ts:317`); 2.x binds `input` instead (2.0.0 on). A one-act fill fires nothing on 1.x. **Which version this portal ships is not established and is said so:** no capture holds that page's scripts. **The difference he cared about more, answered:** the country box is a LOCAL list, and `onFocus` → `refreshOptions` renders its entries with no search — so the click found one; the institution box is REMOTE and empty until `load` runs. Country passed for a real reason, and **nothing verified it**, which was his point: a fill's read-back is a shape nobody compares, and the input a widget fronts is cleared by the widget at the choice. **Built:** typing key by key at 50 ms — under Tom Select's 300 ms trailing debounce, so one box costs the portal one lookup, the same as a person; and the blueprint's `frontedBy` carried through the plan, the stored plan and the wire, so a box that finds nothing says whether the field the earlier control sets **holds a value** or **holds NOTHING** — never which, since on this chain it is derived from the student's own history. Red first at both levels: through a real browser on a page in 1.x's shape (a one-act fill cannot fill it at all and asks nothing), and on the executor. Blocker 49 closed. | 2026-09-21 |
| **P179** | **Attempt 5 answered the box in one line — the list was empty — and the line now says what the page asked for.** The P178 line read `It offers: .` So: ADR-0106's rendering limit was already ruled out by the executor, and the mapping-versus-list cause was never reached, because there was no list to disagree with. **Of Vahid's four candidates, the runner could already tell apart exactly one** — a request the guard or the robots gate refused, which `#blocked` records; the route guard sees requests and never answers, and nothing read the hidden `<select>` the search takes its country from. **Built:** the page's own lookups during a box's fill, in shape — path in full, each query parameter by name and whether it arrived empty, the answer as a status and an entry count. The query's VALUES never appear: on this box one is the reviewer's recorded text and on the next it would be the student's answer. Nothing of a body is kept but its count. `undefined` and `[]` are kept apart — nobody watching says nothing, watching and finding none says so — because a default of `[]` would have made every unwatching caller assert that the portal was never asked. Red first, through a real browser on a fixture in the real form's shape: a search that takes a second parameter and answers nothing without it. **And his correction, recorded where it was wrong:** the `0159` copy is of `id="institution-ts-dropdown"`, entries `institution-opt-1` to `-11` — this box, on 2026-09-11 (he corrected his own passing reference to the 14th). ADR-0109's decision stands; its account of why the doubt went away is withdrawn, and the risk it weighed is **observed**, not conceivable. Blocker 49 rewritten, blocker 50 raised. | 2026-09-21 |
| **P178** | **Attempt 4: three pages of a real university's form filled, saved and read back — and the line that stopped it could not say why.** `raise-missing` released the run, the resolution put it back, one password, and it went further than anything before: sign-in through the consent path (recorded choice, 4 of 4 checks held), then `personal.do`, `contact.do` and `nationality.do` each pressed, read back, **every filled value seen**. `education.do?new=true` then failed twice on one box — `1 of 19 boxes did not take its value (drift): institution-ts-control` — and stopped for a person under ADR-0122 (`page_structure_changed`, `escalated`, both codes on the record). **His two candidate causes, read from the code rather than guessed:** ADR-0106's rendering limit is **ruled out** — an ordinary field's read-back is recorded and never compared, so a choice rendered outside the box produces a *filled* outcome, not a failure; the mapping-versus-list one is **consistent but not established**. *(Corrected in P179: the date question is closed — the `0159` copy is of 2026-09-11 — and the explanation ADR-0109 carried is withdrawn, because the copy is of THIS box. See blocker 50.)* A third he did not name — the entries never arriving inside the five-second wait — reads the same. **So the line could not tell them apart, and now it does:** a drifted box's own error follows the count, and only a drifted one, because those two errors are the runner's own (the locators it tried; the portal's option list, with the wanted value as a character count). A `refused` keeps its silence, guarded by a test of its own. Red first. **And the ADR-0110 mixture, accepted in advance, has now happened:** `summary.do` section A reads *Niloofar Hosseini, born 02 April 1999* beside his own *Mrs* and *Female* — `titleCode` and `sex` are unmapped, so the runner never touched them. Recorded field by field in the Run A README. Blocker 49 raised. | 2026-09-21 |
| **P177** | **The stuck run answered from the code, and the defect that made it invisible closed (blocker 48).** Vahid, after signing d64e75d: *"a paused run exists that no specialist can see and no command can release."* Read rather than guessed, and every step of it in the code: the intervention key is `runId:action:target` with no attempt and no time; `raise` answers with the row it collided with; and migration 0003's unique constraint carried **no `resolved_at` condition**, so "one open intervention per stuck action" was written as one **for ever**. The 18 September intervention he resolved that morning is that row — attempt 3 stuck on the same page, the insert was a no-op, `#pause` read the September announcement and said nothing, and the run went `uncertain`, where nothing offers it work. **Answered plainly: no route released it.** The resolution route is a 409 on a resolved row (by design, ADR-0048); the derive path returns a held run without re-stopping; a re-application needs the prior case concluded. Built: **migration 0007** makes the uniqueness partial, `raise` infers that index, `findForAction` answers the OPEN one as the port always claimed. **Red first at both levels** — the store contract, and the driver on the real database, where the second episode raised nothing and the queue was empty. And the missing caller for the runs it already happened to: `scripts/local-stack.sh raise-missing <conversationId>`, the same shape as ADR-0126's repair — it raises through `#pause` itself so the words are the ordinary ones, refuses a run not held by a person, refuses one that already has an intervention, refuses one with nothing unfinished, and resolves nothing, because an adjudication stays a person's act. Also: the signature test flips back on his third signature, and the education evidence line corrected — his measurement, not my inference. | 2026-09-21 |
| **P176** | **Attempt 3 signed in through the consent path first time; the fill then met a locator no read ever showed.** Vahid's run: nothing over the button as the page opened, `div#ccc-overlay` just before the press — the race, caught by option 0 where the first attempts could not see it — the path pressed in order, **4 of 4 checks held**, signed in. Everything from P160 to P172 worked in one run. Then `personal.do`: `LocatorNotFoundError`, because the entry said `id=saveBtn` and that page's Save has a **name and no id** (his live read). **The audit he asked for, from the captures and not from that one page:** the 2026-09-10 discovery recorded `id=saveBtn` on three pages (nationality, language, documents) and a blank or stray label on the other six; curation generalised the id to all nine. Since `fieldRef = name ?? id` and all nine carry a `saveBtn` field record, the six with no id can only have taken that ref from the **name**. So six pages are now `name=saveBtn` and three stay `id=saveBtn` — each authored from its own read, which is why the table is mixed. Red first, pinned in a test with the evidence per page. **Also his:** the fill's line now says *the save button was not on the page at all* rather than leaving it to an error class, since an obstacle is the portal's doing and a missing control is ours. **And my defect:** P172 claimed the student's panel carried the "no button that says no" sentence; that edit never landed (a script aborted partway and I reported the result without re-checking), so the panel showed the presses and no reason for a week. Fixed, with the published contract's missing field, and guarded by a source check aimed at that failure. Blocker 43 seen on a real queue; the runbook gains the stale `postmaster.pid`. Hash `sha256:21060fca…` → `sha256:3238406a…`; typed lines identical again. | 2026-09-21 |
| **P175** | **The second signature landed (60b303e); the entry loads again, and the sequence for attempt 3 corrected before it starts.** Vahid computed `sha256:21060fca…` himself before signing rather than taking it from an agent's report, the superseded `baca64a9` approval went out in the same commit as the new one came in, and `catalogue check` reads *1 reviewed entry, admits one account only*. The signature test flips back to asserting the load and the admission, and now also asserts that **one** approval is on file, so a stale approval beside a live one would fail. **Corrected for him before he starts:** he expected resolution → sign_in → notice → consent question → choice → box → password. The box comes FIRST. The consent question is raised only from a sign-in that reported `consent_banner_met` (measured at `run-driver.ts:2584` and `run.ts:977`), and a sign-in cannot be attempted without a password, so attempt 3 needs **two** — one spent meeting the notice, one for the sign-in that carries his choice. Conditional throughout: if the notice does not intercept the press, there is no question and one password does it. | 2026-09-21 |
| **P174** | **The signed consent field is written into the Sheffield entry; the hash moved and the approval is void.** Vahid signed the draft on 2026-09-20 (*"Signed. Write it into the Sheffield entry as printed."*) and it is in, byte for byte as he read it. Two hashes, and only one moved: the entry's content hash `sha256:baca64a9…` → `sha256:21060fca…`, so his 2026-09-16 approval no longer covers it and `catalogue check` refuses the directory in its own words; the preview's reference `sha256:11354004…` and **every line of `what-will-be-typed.md`** are unchanged, because the consent field governs what happens before the form rather than what goes into a box. Caught by the entry's own drift guard: the entry must be the two curated drafts plus the signature and nothing else, so the same block went into `blueprint.draft.curated.json` — the drafts stay the record of the reads. The signature test now asserts what is true — the load REFUSES and names the new hash, which is ADR-0057 biting on a real edit to a real signed entry — with the admission it will assert again written into it, because a test claiming the directory still loads would be claiming an approval that does not exist. **No agent wrote an approval.** Waiting on his second signature; then the new approval goes in and the superseded one comes out in the same commit. | 2026-09-20 |
| **P173** | **Both halves of the Sheffield read-back measured, and the profile that made them named.** Vahid pressed *Accept all cookies* on a fresh profile and read what it wrote: `{"functional":"accepted","analytics":"accepted","marketing":"accepted"}`, `interactedWith` true, 90-day expiry. So the accept clauses assert the **exact string** rather than presence, and the refusal's absence is sharpened by it — in his words, *"the same portal writes a string when it means yes and writes nothing at all when it means no."* Recorded at his instruction, and marked as the same kind of claim as the close control: the exact value is **one press, one account, one day**, not a property of Civic CookieControl, and if it changes a student who accepts is stopped rather than signed in, which is the read-back working. New capture record `docs/captures/sheffield-consent-2026-09-19/` holds all four reads, both presses, the unreadable configuration, what loads with the record empty, and the profile — **`aas-consent-read`, which now holds every category accepted and must not be used for any further capture**, because a profile's consent state is part of what a capture means (the lesson of P168's withdrawn claim). The draft is regenerated and still parses. Nothing signed; the entry is still empty. | 2026-09-20 |
| **P172** | **A path of more than one press says what it means (ADR-0131 amended).** Vahid passed the generated question on both his checks and stopped on one line: the generic *"I would press 'Settings', then 'Close Cookie Control' — in that order, and nothing else"*. His words: *"That reads as if the student is being told our technique, and it invites the question 'why does closing something mean no?'… the fact that there is no 'no' button should be in the question rather than inferable from the path."* Built, red first: `ConsentChoice.howItIsMade`, **required** by the parser on any path of more than one control and refused blank; the question leads with it and still quotes every control (*"This site has no button that says no, so I open its settings and close them again without switching anything on, which is how the site records that you did not agree to the extra parts. That is two presses: …"*); the run reading and the student's page carry it, and the client refuses a reading whose multi-step choice lacks it, so no page can show the path without the reason. A count in the middle of a sentence is spelled, not a numeral. | 2026-09-20 |
| **P171** | **The Sheffield consent draft, put to Vahid to read before it goes near the entry — and an invented locator caught in it.** He asked for the draft printed the way a student reads a preview: the path, every control's words as quoted, the cookie and clauses, and the question in full. Generated through `describeConsentChoice` rather than typed, and it parses as a reviewed entry. **The correction:** P170's draft named `#ccc-settings`; there is no such id — his banner read lists that button with **no id**, classes `.ccc-notify-button.ccc-link.ccc-tabbable.ccc-notify-link`, text *Settings* — and the id was the agent's invention, the thing ADR-0109's rule against guessing a locator exists to stop. Located now by the class the read does show, scoped to the notice. Recorded with the draft: the refusal's clauses are his own measurement (after Settings and Close, `optionalCookies` empty, `interactedWith` true); the accept clauses are weaker and marked so — nobody has pressed *Accept all cookies* and read what it writes, so the keys come from his earlier read of his own standing cookie and presence is asserted rather than a token nobody has seen. If they are wrong, a student choosing accept is stopped and handed to a person, which is the read-back working. **Unsigned; the entry is still empty.** | 2026-09-20 |
| **P170** | **ADR-0131 shape 3 built: the refusal is a state we verify, not a button we trust.** Vahid's panel read and one press settled it — opening Settings records the notice as met with nothing accepted, closing leaves that standing, and the notice does not return for 90 days: *"Shape 3 is live."* Built, red first, on the fixture: a choice is a fixed `path` of controls that each carry their own words (P166's rule one level in), pressed in order, and a step not on the page stops the sign-in rather than looking for another way through; `verify` names the cookie and the clauses that must hold in it — key present or absent, optionally an exact value, never truthiness, because a record that says `"revoked"` says it in a truthy string; the runner reads the record back and ends with the new failure `consent_not_recorded` when it disagrees or cannot be read, before any second press of the sign-in button. Not counted against ADR-0120's two (the login form was never answered); the student is told plainly, including that their password went into the form before the press and is spent; a person is asked, because what failed is the entry's own account of the portal. The question now carries both halves: every control on every path quoted, and `beforeAnyChoice` — required, so a reviewer who has not measured what already ran cannot author a notice. The lying fixture (`settings-records-everything`), whose two presses read as a refusal and record an acceptance, is caught and never signed in; with the read-back disabled that test signs in. Recorded with it, at his instruction: the page carries no readable consent configuration and the vendor library is refused here, so what *Close* records rests on one press, one account, one day — which is why the read-back is the mechanism. The Sheffield entry is still unwritten; its draft is in the ADR for his signature. | 2026-09-20 |
| **P169** | **A correction: the six trackers were on an accepted profile (ADR-0131).** P168 read the 2026-09-18 capture as showing Hotjar, Yandex webvisor, TikTok, LinkedIn, Meta and DoubleClick loading before any consent, and said so in three records. Withdrawn: it was the agent's inference from the capture's README word *fresh*, not a measurement, and Vahid's own reads settle it the other way — that profile had accepted every category, which is also why it met no notice at all. Measured 2026-09-19 with the consent record empty: **Google Tag Manager only**. Consent gates the rest, and an empty record is not decorative. The capture's README now records that its "fresh profile" does not hold for the consent cookie. What survives is the honesty condition carried into the build: the tag manager runs before any choice, so a refusal is true about what follows and false about what already ran. | 2026-09-20 |
| **P168** | **The Settings panel refuses shape 1, and the refusal, if there is one, is a state (ADR-0131 amended).** Vahid read the panel on his own account, Settings pressed and nothing else: no save button (only *Accept all cookies* and *Close Cookie Control*), and three category toggles with no `id`, no `name` and no label text, so an entry cannot name one and position is not a name. **Shape 1 does not exist and is not written.** Corrected in the record rather than left to stand: `#ccc-close` has words, so P166's rule does not reach it; the defect is that its words name an action and not the consequence it records. Opening the panel alone wrote `optionalCookies: {}` with `interactedWith: true` and a 90-day expiry — the notice met, nothing accepted. What would settle whether that is a durable refusal: two reads can kill it (the toggles' `checked` state; the page's own `initialState` per category), only one press of *Close* can confirm it, with four outcomes named. **Corrected in P169:** the agent read the 2026-09-18 capture as showing six trackers loading before any consent; that profile had ACCEPTED everything (Vahid's read of his own cookie), which is also why it met no notice. With the record empty the page loads **Google Tag Manager only** — consent gates the rest, and an empty record is not decorative. What survives is the honesty condition: GTM runs before any choice. Shape 3 proposed by the agent, **not decided**: a fixed path of controls that have words, plus a read-back of the consent record, so the refusal is verified rather than trusted — its cost, that the option is described in our words and not the notice's, put to Vahid. | 2026-09-19 |
| **P167** | **The limit of option 2, recorded as option 2's (ADR-0131 amended).** Measured: a choice is one press; the runner presses it and presses sign-in at once; a *Settings* choice would open the panel, press sign-in into it, fail, spend the password and count against ADR-0120's two; the parser would not stop it; a one-choice notice cannot be authored. So option 2 as built cannot express *accept all, or a settings panel nobody has read*. Vahid: *"Record it as a limit of option 2, not as a defect of Sheffield: the mechanism assumes a notice whose choices are each one press, and some notices are not like that."* His condition for a path through a panel, verbatim in the ADR: every button named in the entry and quoted to the student, the path fixed, not discovered at run time. Shape 1 (the named path) first, on his read of the Settings panel; shape 2 (*accept all, or do not apply through us on this portal*) kept available, not ruled out. Nothing built; nothing in the entry. | 2026-09-19 |
| **P166** | **A control with no words is never a choice on a notice (ADR-0131 amended).** Vahid read the Sheffield notice on 2026-09-19 from a fresh profile, structure and the banner's own words only: *Accept all cookies*, *Settings*, and a close control with no text. He asked what the close control does before the entry names it; it could not be found out from here — the vendor's library is refused by the network policy and the portal is off limits by his rule — so it is recorded as not known, with the four-step read for his own account and what each outcome would mean. His decision, in his words: *"A button whose meaning is set by configuration the student cannot see is a button nobody can be honestly asked about. Record that as the rule rather than as this portal's case."* Recorded as the rule: `readConsentBanner` refuses a choice whose label is blank, empty or whitespace, red first (the empty case was already refused; the whitespace case was not). Not decided: the Sheffield consent block — the notice offers *accept all* or a second surface nobody has read, and whether option 2 can express that is his to decide. | 2026-09-19 |
| **P165** | **ADR-0131 — a consent notice is answered only with the student's own choice (option 2, blocker 47).** Vahid: *"a cookie choice is a choice made on the student's account, in their name, against an institution that may one day be asked what they consented to."* Built, red first, on the fixture: the blueprint records the notice's words and each choice's label, meaning and button (`authentication.consent`, signed content — the canonical hash moves); the runner tells the notice from any other obstacle by one of its buttons being on the page, presses the button for the student's recorded choice and the sign-in once more, or presses nothing and reports `consent_banner_met`, not counted as a failed sign-in; the plane's next step is `consent_choice`, BEFORE any password box; the student is told the notice was met, then asked in its own words with what each choice means; `consent_choice` is a decision accepted whenever the portal records a notice, refused for a key it does not offer, recorded per student and portal (migration 0023, `student_portal_consents`), carried on the next sign-in as a key and a locator only, visible and changeable on the run reading and in the student's page with one button per other choice. Option 3 refused in his words and kept; 5 and 6 refused as a fabricated consent record and an assertion that a person clicked where a person could not. Not decided: the Sheffield notice, never read — the entry's consent field waits on a read of it when present, and his signature. Eight driver tests on Postgres, four settle tests on Chromium, the contracts, the drift and the catalogue tests green. | 2026-09-18 |
| **P164** | **ADR-0130 — the point is read at every press, not only a failed one (option 0, blocker 47).** Vahid: *"Read the point at a successful press as well as a failed one, and once as the login page opens, so attempt 3 names it either way."* Built, red first: `sayOverButton` prints what stands over the sign-in button as the login page opens and just before the press, in ADR-0129's words the other way round — *nothing over the sign-in button (button#signIn (static, 57×21 at 546,80))* or *over the sign-in button: div#cover (fixed, 1280×720 at 0,0)* — then *the button was pressed*; the failed-press line is unchanged and pinned so. Read before the press, not after, because a press that lands may navigate the page away. The fixture proves the race made deterministic: a layer added after the page opened reads as nothing at the open, over the button just before the press, at the button's point at the failure, and its text ("We use cookies") reaches none of the three lines. Nothing in the runner's behaviour changed: no wait, no dismissal, no second press. Ten settle tests green on Chromium. | 2026-09-18 |
| **P163** | **The fill says what it did; the sign-in count is named for what it is (ADR-0124 amended).** The third conversation's attempt 1 NAMED the obstacle — `div#ccc-overlay`, fixed, the full 1280×720 viewport, CookieControl's backdrop, at the press, with *another element intercepts pointer events* pending — the fifth hypothesis and the first measured. Attempt 2 signed in, unread at its press. The first page fill (Personal details) then reported `uncertain (runner_fault)`, a code produced in two places, and the fill had no line to say which: P157's defect in a second place. Vahid: *"it should be fixed before the next attempt rather than after."* Built, red first: `FillApplicationDeps.log` and `atPoint`; a line at every stop the fill can make, through the closed vocabulary — the Save press names its pending check and the point's layers (`point-of-control.ts`, shared with the sign-in); the intake names a throw. Answer 4 corrected by measurement: ADR-0120's count is cleared by a sign-in that holds (*"a new episode of two"*), so the third sign-in would have printed `attempt 1`, not `attempt 2`; `signInAttempt` → `signInFailuresSoFar` (zero admitted), the line reads *failures in this episode: N of 2 allowed*, the student hears no ordinal and *"if it fails again I will stop and someone will look at it"* (his words); the rule untouched, pinned by a fail-succeed-fail test. Records: the y=690 candidate withdrawn (the button was at y=566); what this week's attempt numbers meant, with dates; the account will hold a mixture after Run A (his words, no values); the listing read-back is before-plus-one; the overlay decided (blocker 47). Nothing reached the portal (his own check of the page); the intervention waits at his word. | 2026-09-18 |
| **P162** | **ADR-0129 — the runner reads the point at the moment the press fails.** The `--as-runner` read (Vahid, dd12175): *nothing over the button* as the page opens — the fourth wrong hypothesis on this step, tested not fixed. So the obstacle is there between the page settling and the click, and only the runner can see that moment. Built: `point-read.ts`, one in-page `elementsFromPoint` read with two allowances — the reader keeps text, the runner keeps STRUCTURE ONLY (tag, id, classes, position, box) because its line goes to a log; `sign-in.ts` reads it in the catch and prints *pending: <which of Playwright's checks>* from a closed set of Playwright's own phrases, never the element Playwright quotes. Not the picture: buildable with every input masked, not built until attempt 3 says *nothing at the point* — his word. The reader's own defect fixed: `isNavigationRequest()` is true for a subframe's load; two pixel iframes read as *the page tried to send the tab elsewhere* and cost a round of attention; only a main frame counts now. Recorded in the capture README: the read is the quieter of the two (twenty POSTs refused there succeed in the runner; CookieControl, Hotjar, a session recorder loaded), and the button at y=690 of 720 with a cookie library on the page as the leading candidate, with its caveat. Blocker 45 raised and closed. | 2026-09-18 |
| **P161** | **ADR-0128 — read the page the runner sees before anything learns to push past it.** Attempt 2, with the split: *"the sign-in button could not be pressed … the password box is still on the page"*. The press, not the answer; both P157 hypotheses wrong; the split found the site in one attempt. What is over the button is unknown because the runner's page is UNOBSERVED: every capture refused GTM, and the runner allows it, presents `AskiMate-Runner/1.0` and a 1280×720 viewport. Vahid: *"Do not fix the press … I want to see it named from a capture before anything in the runner learns to push past it."* Built, in the attached tool: `--as-runner` lifts the host rule for READS only, records every off-host read, rewrites the User-Agent header to the runner's at the guard, sets the runner's viewport — the method rule untouched, writes refused on every host; `--covering <locator>` reads `elementsFromPoint` at the control's centre, top-most first, with tag, id, classes, position, z-index, box, role and text; `RUNNER_PRESENTS` pinned to what `sign-in.ts` opens. Fixture: a tag on a second host refused by default and loaded as the runner with the runner's agent seen by the tag host; a transparent cover named `div#cover position fixed`, stack `cover > signIn`; proved through the real command under tsx. Nothing in the runner changed. | 2026-09-18 |
| **P160** | **ADR-0127 — the submit is two waits with two names.** Run A's repeat, read from the log ADR-0124 built: `TimeoutError: the step timed out` at the submit — not the race's error, so the race is exonerated. And the second hypothesis was wrong too, in the way that matters: `waitForLoadState("load")` was called BEFORE the click on a page `goto` had already loaded, so by Playwright's own contract it resolved at once and guarded nothing; the clock that expired was the click's own. Vahid: *"the third time this week a piece of code did something other than what it read as doing … measure, do not read — and it should be stated as the reason for the split."* Built: `settleSignIn` — the press (`noWaitAfter`, 15 s, unchanged) and the answer (`framenavigated` on the main frame, armed before the press, 30 s — a measurement, not a fix), each with its own phrase, the overlay case adding *the password box is still on the page*; the landing confirmed by URL because a locator would be invented; NO screenshot, as a decision. Each wait driven to its own failure on the fixture (slow answer, covered button) in a real browser, red first. The overlay side names no banner: the runner's page is unobserved — tags refused in every capture, allowed in the runner — and a tags-allowed read is the next diagnostic. | 2026-09-18 |
| **P159** | **ADR-0126 — the repair: a stop recorded before P158 needs a caller of its own.** Vahid pulled 412d001, restarted, refreshed the session, ran the same two calls and got the same 403. His read was right and it is now established rather than believed: P158 changed what a NEW cancel does, and nothing re-examines a case already half-cancelled. **Reproduced on the fixture** by stopping a case exactly the way `#cancel` stopped one before 412d001 — the first act alone — and then measuring the three doors: the Worker's `dueRuns` filters on `AUTOMATABLE_STATUSES` so an `escalated` run is never advanced; `claimWork` offers a stopped case nothing; a second cancellation is refused by the transition table. No caller left. Built: `RunDriver.finishStoppedCase(conversationId)`, reached as `scripts/local-stack.sh finish-stopped <id>` through the service's own binary and env file — no second composition root (ADR-0041). It REFUSES any case not at `WINDING_DOWN`, naming where the case is (asserted against a real live case); it performs no transition of its own, so the obligations guard applies unchanged and a case still owing an account is reported rather than concluded; it is idempotent. Proved through the REAL command in the local-stack test, per P80. What it does not do: read his database for him — the runbook carries the query. Blocker 44 raised and closed in the same phase. Red first; typecheck, lint, census green. | 2026-09-18 |
| **P158** | **ADR-0125 — the stop finishes its own job.** Found by Vahid walking the failure path in Run A. A cancellation is two acts by design (ADR-0053): `WINDING_DOWN` at once, unguarded, and `CANCELLED` only once nothing is owed. The second act lived in `#windDown`, which runs only on an ADVANCE — and the Worker advances `running` and `suspended` only, because `uncertain` and `escalated` wait for a PERSON by design (ADR-0065). So a run a specialist was holding could be stopped and could never conclude, while the message it had already sent said the work had stopped. **The first diagnosis was wrong** (it blamed a missing write to `workflow_runs.status`, reasoned from reading the code) and four resume sequences built on it failed on his machine; the corrected one came from **changing one variable on the fixture and reproducing**, which also exonerated the ADR-0118 gate. His words: *"Option one. The stop finishes its own job… the escalation rule was deliberate and it should stay whole… it must do so only when nothing is outstanding — you said that and I want it enforced, not assumed… A student should never read 'stopped' and later find it was not."* Built: `#concludeCancellation`, called by BOTH `#cancel` and `#windDown`, so there is one model of the obligation check. Taking *enforced* seriously found a second defect: the guard in `checkTransition` that protects an outstanding account was **never reachable through `decide`**, which built its context from the case alone and never supplied the obligations — the rule lived in the caller, since P15. The obligations now travel on the transition intent, and their ABSENCE at `CANCELLED` is a refusal of its own kind (`obligations_unknown`, separate from `obligations_outstanding`, because they are two facts). The message takes a union, never a length check: concluded says *nothing is outstanding*, not concluded says *stopped, but it is not finished* and names the account — and the promise it makes is kept, because concluding at the handover now tells the student so. ADR-0065 unchanged; blocker 43 raised, not fixed. Red first (five failing); typecheck, lint, census green. | 2026-09-18 |
| **P157** | **ADR-0124 — the runner says what it did.** Run A reached Sheffield and the sign-in failed twice with `runner_fault`; ADR-0120's cap stopped it correctly, and the runner's whole account on disk was `turn: worked` twice. Two causes: no logging beyond one line that printed the turn's KIND, where *worked* means a turn ran rather than it succeeded; and five bare `catch {` in the sign-in path that never bound the error. His decisions: *"'worked' has to go… Print the outcome"*; *"I would rather it were strict than useful… If a message cannot be scrubbed with confidence, print the class alone and say the message was withheld"*; *"a line at the start of each sign-in attempt… If the runner dies mid-attempt, I want to know it started."* Built: `runner-log.ts` with a CLOSED pattern set — a recognised message yields OUR phrase, never any slice of the thrown text, and an unrecognised one is withheld with the class named; the turn line carries the outcome and code, typed against the real turn result; the sign-in writes a start line naming the attempt, the URL and the run; `ClaimedWork.signInAttempt` carries the PLANE's count, named in the free-text constraint so a later number still fails the build. Blocker 36 raised and closed in the same phase: migration 0022, `attempt_failures`, both codes in the intervention. The submit/load race is deliberately NOT changed. Red first; typecheck, lint, census green. | 2026-09-18 |
| **P156** | **ADR-0123 — Run A found it before a student did.** Vahid ran step 3 on his own account and the run stood at `fix content (running)` with no question, no message, no intervention and no log line. Reproduced on the signed entry with the synthetic profile: the plan clean, the validator eighteen violations, all starred boxes in the language section he had saved empty on the live portal on 16 September. Two of our own records read different things about one page: the plan read the section's `optional` fact (P147, ADR-0119), the validator read the marks alone, and the validator had never run on the signed entry because the P150 test checked plan blockers only. His decisions, in his words: *"The star is not the evidence. The save is… a star is not a requirement either"*; the section fact governs; *"a step that cannot ask for what it needs must stop for a person and say so."* Built: the validator reads `optionalSectionWords`, the same function the plan reads, marks untouched, **no draft changed, hash unchanged, no re-sign owed**; the Run A profile test runs the validator on the signed entry (eighteen red, then none); `contentHandoverOf` in the orchestrator and the driver's ADR-0065 stop taking it — a `fix_content` the interview cannot act on raises `specialist:content_rejected` with box, rule and bound on the record and never a value, escalates, and tells the student once. Blockers 34 and 35 closed. Red first; typecheck, lint, census green. | 2026-09-17 |
| **P155** | **ADR-0122 — the page fill stops at two.** Blocker 32 closed at his word, before Run A: *"build the cap first… Two attempts then stop, same shape as 26 and 120, and the student told which attempt failed and what the page did."* A failed `execute` report completes the page's intent with its code; the ledger row now remembers what each attempt failed with, in order, across a reopen (`attempt_failures`, case-store migration 0006, both stores, contract-proved); `#afterFailedPage` tells the student which page, which attempt and what the page did on the first failure and leaves the page re-openable once, and on the second raises an intervention naming the page, both codes and *"once is chance, twice is the portal"*, tells the student, and sets `escalated`. A lost session is not an attempt. A save the runner could not confirm is `uncertain` and stops at once as before; `portal_refused` is said as what it is — a box that would not take its value and a page not saved — and the person is told a rejected value and a wrong box cannot be told apart from the record. Blocker 31 decided A for Run A (re-seed with his account's e-mail from a copy outside the repository; his act) and B recorded as the thing to build. Red first; typecheck, lint, census green. | 2026-09-17 |
| **P154** | **Signed.** Vahid signed the Run A entry on 2026-09-16 (commit 3575eb1): blueprint 0.2.26 and set 0.3.31 reviewed by him, `sha256:baca64a9…`, one signature, own account only — studentId `af398e01-c154-469d-a086-3e9c8c60a020`. Verified here: the hash recomputes to his, the directory loads one entry admitting that account and nothing else, and an edited label moves the hash (a test holds all three, and the pins that said *unsigned* flipped). Step 5 done on his machine and it found four things, now in the runbook with their fixes: Homebrew, PostgreSQL and the `postgres` role, Redis, and Redis's default save-to-disk, which the Secure Service refused — the envelope cache's *ciphertext must not reach disk* check firing on a real machine for the first time, the right way (ADR-0034 addendum, §3.2 of the deployment doc); `CONFIG SET save ""` does not survive a restart and the runbook says so. The stack was up on the fixtures; the Run A section of the runbook says how it is restarted on the signed catalogue. Step 6's sequence handed to him in the report, page by page, with what each point should look like and what to do when it does not | The signed entry and its approval (his commit), the tests flipped, the read regenerated (`REVIEWED`), the runbook's two new sections, ADR-0034 addendum; version 0.150.0 | Run A itself. Item 6 done; one item open of ten (Part 2, item 10, not on Run A's path) |
| **P153** | Before the signature, at his word (*"Fix the three label defects first, as draft 0.2.26… If the labels in the blueprint are wrong, then the thing I read and the thing I signed are not the same thing"*), four label fixes and one sentence. Draft 0.2.26: the nationality page's radios carry the third read's option labels (Yes / No and the document-status sentences) where that read's values equal the draft's — fourteen groups restored, three off the path kept as their name because the read's *No* ran on into help text; `dobMonth`/`dobYear`, `startYear`, `endYear` carry the row's question from the row's first control (his fourth, the marker shape in the labels); `institutionCountry` and `institutionCode` carry the new blueprint fact `frontedBy` — set by the Tom Select boxes — which the parser reads, `checkUsable` guards (`fronted_field_invalid`: mapped, or not another field on the page) and the plan lists nowhere. The preview's sentence replaced everywhere: no run submits (ADR-0014), so *"This is exactly what will be entered into the portal on your behalf and saved there. Nothing is submitted by this…"* — one true sentence, since there was no product path to differ from. His acceptance of the four unobserved things recorded in his words in the Run A README with the four named; ADR-0059 addendum on the preview and the page walk disagreeing since P72 until a person asked to read the output. The seven labels no capture holds still carry the field's name, said so. Red first: the fronted rule (three tests), the sentence (three pins), the draft pins | Draft 0.2.26 (set 0.3.31 re-bound), the entry re-assembled from it, the read regenerated; `frontedBy`; version 0.149.0 | His read of the regenerated file; the three signing acts |
| **P152** | Step 3: the entry assembled and the read handed over, at his word (*"Carry on with step 3"*), with his two conditions met first. `docs/run-a/catalogue/entries/sheffield-pgt-2027-09.json` is the two drafts byte for byte after parse (a test holds it), the portal-authentication facts, empty required documents, the repository's refs and the secure-channel delivery; NOT signed — both artefacts still draft, no reviewer, and `catalogue check` refuses the directory; the three signing acts are his and written in the Run A README in order. `pnpm run catalogue preview <entry> <profile>` prints what a run would type into which box, page by page, in the student's own preview words, from the same builder and renderer as the yes; `docs/run-a/what-will-be-typed.md` is that output for the synthetic profile and a test holds they are the same. Found by the read and fixed, red first: the preview listed the registration page's e-mail box as *exactly what will be submitted* on a run that signs in to an account the student holds — the driver's page walk never visits a page carrying a credential field, and now neither does the preview. Answered before assembling: nothing in the entry is expected to change; four things in it have never run on the live portal (the grading-system dependency, the two Tom Select boxes, the subject search, the login locators) and each would mean signing again; no smaller settled entry exists. Three display-only label defects in the read named, not changed | The entry file, `catalogue preview`, the preview's registration-page rule, `docs/run-a/README.md`, the read; version 0.148.0 | His read; the three signing acts; Run A |
| **P151** | Three of his answers built before step 3. **ADR-0120:** the sign-in failure shape was blocker 26 again — *"apply ADR-0114's shape to sign-in. Two attempts, then stop for a person, with the student told which attempt failed and what the portal said"*; the attempts counted in `run_sign_in_failures` (migration 0021) since the session was last live, the spent handle named and offered to nobody whatever the outbox has delivered, the first failure told as *the first of two* with the box opening again, the second stopping for a person with the attempt and the code on the record, `escalated`, no box on a held run. What cannot be distinguished is said, not guessed: `portal_refused` on a sign-in is the login form still showing, so the student and the person are both told a wrong password and a portal fault cannot be told apart. **ADR-0121:** the provenance vocabulary carries the true word, `seeded` — *"a value that reads as something it is not, which someone eventually takes at face value"* — in the domain, the contract mirror, the OpenAPI enum and the seed. **The count:** two items open of ten, items 3, 4 and 5 struck for Run A and kept open for the product, at his word. The e-mail stays synthetic, with the Run A README saying Sheffield may write to it and nothing will arrive. Red first: five tests failed against the old code, the third reproducing the dead handle handed to the next runner | ADR-0120, ADR-0121, migration 0021, `RunSessionStore.signInFailed`/`signInFailure`, `RunState.session.signInFailed`, `#afterFailedSignIn` and its words; version 0.147.0 | Step 3 (the entry assembled); his signature |
| **P150** | Step 2 of the sequence to Run A: the synthetic profile, shown before it seeds (his flag: *"I want to see its values before it seeds"*). `docs/run-a/synthetic-profile.json` holds eighteen values in the store's stored shape — a synthetic Iranian national resident in Tehran, no passport, one University of Sheffield BSc (a UK study at university level on a student visa, no current visa), one current job in Iran — exactly the registry fields the Sheffield set reads, the entry date deliberately not held. `pnpm run profile:seed <file>` prints the values and writes nothing; `--write --subject <label>` creates the `students` row (e-mail verified), writes the entries through the real store with a provenance that says it was seeded and that no interview took place, prints the `students.id` UUID, and refuses a non-empty profile. Found on the way and mapped: the profile's UK study at university level makes the nationality page ask which qualification from its own list — blocker 25's shape on a second page — mapped for that one qualification only, as P149 mapped the institution box (set 0.3.31). Found and corrected: the runbook said a dev-session subject is "the string you chose"; it must be a `students.id` UUID, since `profile_entries.student_id` references `students`. The country box's preview line read as a DOM id; it now carries the row's question (draft 0.2.25). The sign-in failure shape traced from the code for his second flag and reported | Set 0.3.31, draft 0.2.25, `scripts/profile-seed.ts`, `scripts/run-a-profile.test.ts` (six tests, two against a real migrated database), `docs/run-a/`; version 0.146.0 | Step 3 (the entry assembled, his signature); the provenance word for a seeded value, his call; blocker 25 for a real student |
| **P149** | Step 1 of the sequence to Run A, at his word (*"A Sheffield degree for the synthetic profile… Start step 1"*): the education chain mapped for the synthetic profile's one University of Sheffield qualification. Set 0.3.30 maps the six boxes the page still needed — the country BOX and the institution BOX (the Tom Select controls the runner fills; the hidden selects behind them are set by the boxes and mapped by nothing), the search word and the subject, the grading system and the grade — each through a PARTIAL option map onto the captured lists that names what the synthetic profile holds and nothing more, so that any other institution, level, subject or grade spelling is a loud `render_refused` at plan time, never an approximation at the fill. Draft 0.2.24: the country box records its 255 entries (the captured select's, P101's confirmed reading) so a mapping may name one under ADR-0109; `gradingSystemId` follows the institution box — the control that is filled, which sets the hidden select the lookup reads — inferred from the dependencies read and P102's shape, not observed as a fill, and flagged for Run A. The search word is `business`, the one the 87 results came from, never the subject typed whole. Only grading system 7 is named because only its grades were read; a Master's is refused at plan time rather than waiting at the fill for a list nobody read. Red first: six pins failed against 0.2.23/0.3.29, then passed | Set 0.3.30, draft 0.2.24; a draft test planning the synthetic qualification onto all six boxes with the typeahead text, and four refusals by name; version 0.145.0 | Steps 2 and 3 (the synthetic profile shown before it seeds; the entry assembled); blocker 25 for a real student; his signature |
| **P148** | Degree is required, and Sheffield says so out loud: Vahid's full throwaway entry with the qualification select unchosen was refused by name. His route taken: set 0.3.29 maps the degree for the synthetic profile's two levels onto the captured award titles (a reviewer's judgment, recorded); any other level a loud blocker; `unlistedDegree` hidden and unmapped; no page-wider mechanism; the set hands nothing to the student on any page. The first-screen entry rewritten as he asked: *"this portal does both and you cannot tell which from the field"*, with both behaviours observed on the same page. Found and fixed: a required unmapped box on a repeating page whose visibility a mapped field decides is judged per entry (red first). Distance item 5's remaining piece is the institution box (blocker 25) | Set 0.3.29; the per-entry gap rule; version 0.144.0 | Blocker 25 and the vocabulary; his signature |
| **P147** | The language page settled from the screen and two more of his decisions built. Vahid saved the page empty: *"No error, no complaint, straight through"*, and the summary said the section need not be completed. So: the seventeen are un-handed (*"Left empty is the honest state"*; set 0.3.28), the section-level fact built as proposed and erasing nothing — `BlueprintSection.optional.formSays`, parsed and hashed; a marker inside such a section is not required to save (`isRequiredToSave`); the preview says *The form says: "…"* in Sheffield's words (draft 0.2.23) — and the pattern he named, Sheffield's silent drop, written on the capture README's first screen where whoever reads next meets it before trusting a save. *"Mandatory" means three things* said where the markers are tabled (P146). The attached read cut as not owed. Degree still handed, pending his throwaway save; the route if required is confirmed: map it for the profile's levels. Three tests red first | The section fact; set 0.3.28; draft 0.2.23; version 0.143.0 | His throwaway save on the education page; blocker 25 and the vocabulary; his signature |
| **P145** | The cut of 2026-09-16, accepted by Vahid in full, and ADR-0119 built the same day. The open work sorted into needed-for-Run-A, needed-before-a-real-student, and habit; his reasoning kept for the developer: *"the goal changed, the list did not, and a week of work was on it out of habit rather than need."* The own-act mechanism: any box the reviewer hands to the student is their own act on a page the runner still fills; the preview reads page by page and says under each page which boxes the student fills themselves and that the application is not complete until they do; the record keeps three states apart — filled (the hash), handed (`OwnActRecorded`, always with its page), never mapped (`UnmappedRecorded`, one per page, `ApplicationCase.unmapped`); `toStoredPlan` no longer refuses a non-file handoff. Set 0.3.27 hands nineteen required boxes for Run A: no required field on Part 1 is without a mapping or a hand. Blocker 29 built: `list_exceeds_form` stops the fill by name when a list outruns the form's blocks. The review pack frozen with a line at its top. Eight tests red first. The language page's read written for him (capture README, Item 4) | The mechanism; blocker 29; set 0.3.27; the frozen pack; version 0.142.0 | His read of the language page; blocker 25 and the vocabulary; blocker 30 kept |
| **P144** | ADR-0118 built the day it was decided. Vahid: *"Drop it to one: I approve, and I am the only signature."* The three copies of the two-person rule changed: the registry accepts the author's signature only when it names the one account it admits (`ownAccountOnly.studentId`; unbounded is `self_approval_unbounded`), the approvals parser goes through the same rule, `checkUsable`'s `reviewed_by_author` is gone. The gate he asked for, as a thing in the code: the loader carries the approval's admission onto the served entry and every listed target; the service shows a one-account target to the one student it names, offers it to nobody else, refuses every other student's start and re-application by name (`not_for_this_applicant`, 403, worded), and every later lookup for a bound case answers as if the entry were gone — a catalogue swapped under a running case stops it. Four tests pinned the old rule; each was run red against the old code first. `catalogue check` prints the admission. [`what-was-skipped-to-get-it-standing.md`](./what-was-skipped-to-get-it-standing.md) written for the developer, linked from the README's first screen: what the one signature leaves uncaught, and the candidates for the same cut, none cut | The gate; the records; version 0.141.0 | His answers on the candidates list; blocker 25 and the vocabulary; blockers 29 and 30 |
| **P143** | Records, at his word, and the blocker 25 sheet. ADR-0115 carries why the script was read — *"A student resident in Iran would have answered 'have you always lived in Iran' and we would have stored it under always_in_uk"* — the field the page answers itself (`livingInUK` after *no* to living outside: a value the student never gives and this system never types; in the set's note, 0.3.26, and the pack), Sheffield's own dated bug (the study block's `"yes"` clause that never fires; drift if they fix it), and blocker 30 for what the condition language cannot say, with his sentence. ADR-0113 carries the interview's scope note on the seven claims. [`decision-sheet-blocker-25-…md`](./decision-sheet-blocker-25-a-portals-own-list-against-the-students-words.md): the institution box and the four more places on the form with the same shape, three options, B recommended, and the nationality vocabulary answered with it | Nothing built. Distance to Run A: five items open of ten | His answers on blocker 25 and the vocabulary; blocker 30 |
| **P142** | `nationality.js` read from Vahid's commit. Two registry fields renamed once, from the script, as he asked: `residence.always_in_residence_country` and `residence.outside_residence_country_last_three_years` — the page fills both questions with the permanent-residence country's name, *the UK* only when that is the United Kingdom. The draft (0.2.22) carries the page as twelve sections with the script's show/hide as `visibleWhen`: a mapped field the page hides is neither typed nor missing, so a resident abroad is shown none of the always-lived questions, the entry date or the seven claims, and the P139 note on the entry date is resolved by the page. Set 0.3.25: `fundingNationality`, `countryOfBirth` (partial, ISO-keyed) and the UK-study block after *yes* from `immigration.uk_study`; a `date` rule may be followed by an option map, for a month select spelt the portal's way | Proved on the synthetic profile: a resident abroad with no UK study and no passport fills the page with nothing blocking; with UK study, everything but the per-level qualification select. `no_mapping` blockers with an empty profile down to twenty. Two limits of the condition language recorded, not approximated: one controlling field per condition; no OR across fields. Open: the five per-level selects (blocker 25's shape) and the registry's nationality vocabulary, both flagged for him and in the pack. Distance to Run A: five items open of ten | His call on the per-level selects and the nationality vocabulary; Iman's sitting |
| **P141** | ADR-0117 built (group 4: *"A… Fold them."*). `identity.passport` is one value — `held` with number, expiry and an optional issuing country, or `none`, the student's statement — in place of the three keys; the extraction plan assembles it as one composite (a passport missing its expiry is not read at all). The `part` rule's `absent` clause gains `{ typed }`: the portal's own instruction for a stated absence, quoted from the page into the reviewed mapping and stored nowhere else — his important half: *"'no passport' is Sheffield's instruction, not a fact about the student… the tempting shortcut is to store the string once and reuse it everywhere."* Set 0.3.24 maps `passportNumber`. P140 (docs only, `6bf1d2f`) read the row's exact words and the page's sections off his committed markup, and found the show/hide lives in `nationality.js`, which the capture does not hold | The plan's `no_mapping` blockers down to twenty-seven. His two further decisions recorded in ADR-0115: the three-year window is the interview's (most recent first; the mapping types the first four), with the silently dropped fifth period raised as **blocker 29**; the two script-filled spans are renamed by nothing until the script says what they hold. Iman's first sitting starts now; the pack names what may still move. Distance to Run A: five items open of ten | `nationality.js` (he is getting it): the page's show/hide, the two spans' contents, then the UK-study mappings and any renaming; blocker 29 |
| **P139** | ADR-0115 built, on his confirmation with one change — *"residence.uk_entry_date should be month and year, not a full date, and the day the page asks for is the mapping's problem rather than the profile's"* — and his two reasons recorded in the ADR (the three starred questions asked, not computed: *"the history is what they remembered; the answer is what they claim"*; the seven claims never derived: *"a wrong yes opens a document slot"*). Three registry groups: `residence.*` (seven fields, the history list-valued, the entry date a `YearMonth`), `immigration.uk_status` (seven booleans), `immigration.uk_study` (none, or studied with its details); the `derived` proposal origin naming its source field. Set 0.3.23: thirty-six nationality mappings from the three groups; every day select unmapped by decision; the UK-study block shown after *yes* unmapped until its show/hide is read off his committed markup | Proved on a synthetic profile: no day typed anywhere; a current period's end empty; the history's periods into the four blocks. The plan's `no_mapping` blockers down from forty to twenty-eight. Noted, not decided: a mapped field with no value blocks whether the page requires it or not, so the entry date for a student never in the UK depends on the page hiding it — read in the same script. Distance to Run A: five items open of ten | His committed `nationality.do` markup: the passport row's words (group 4) and the page's show/hide script; then the UK-study mappings; then Iman's sitting for items 3 and 4 |
| **P138** | ADR-0116 built (blocker 28: *"a cancel is the student's stop"*). A seventh run status, `stopped_by_student` — not automatable, not held by a person, not terminal; `isHeldByTheStudent` the fourth set of the partition. The driver reads the Secure Plane's `secret_cancelled` off the log on the next tick and, before any box would open, stops the run: one message in plain words, the status, the phase untouched; nothing offered to a worker or a runner. The student restarts by asking to apply again: `start` finds the run they stopped, the decision opens a fresh box and its checkpoint sets `running` in the same write; told after, only when it carried on. `secretStepFor` answers a cancelled request with the box again. The demonstration client shows the stop in its own words and the choice again for a stopped run | Red first: the status machine, the step after a cancel, the stop and the restart on Postgres. Blocker 28 closed. Distance to Run A: five items open of ten | His confirmation of ADR-0115's shapes; his read of the passport row |
| **P137** | ADR-0114 built (blocker 26: *"C, and the number is two"*). The intent ledger counts attempts made and names the secure request each attempt spent (migration 0005; `completeIntent` detail); the state carries `accountCreationFailed`; `secretStepFor` sends the run back to the box while the log's latest request is the one a failed attempt spent — by identity, not by the lifecycle word the outbox delivers later; the driver's second-box guard reads the same. `reportWork`: an unusable password is not an attempt (told when one was handed, box again); the first failure told in the closed set's words with the box opening again; the second raises `timeout_exhausted` naming *the second of two attempts* and the code, tells the student the account could not be created, `escalated`. The challenge stop and this one are one method. Found and closed: an escalated run's advance could open a password box (P40's no-guard rule) | The P121 loop reproduced red first as the third driver test's first assertion. Blocker 26 closed. Blocker 28 raised: a cancelled box loops the same way, silently — his call. Distance to Run A: five items open of ten | His confirmation of ADR-0115's shapes; his read of the passport row; blocker 28 |
| **P136** | Records and sheets, nothing built. ADR-0113 in Vahid's words: the interview collects a list entry by entry, never from a pasted CV the model summarises — *"the model is the source of what goes into the profile, and confirmation after the fact is weaker than it looks"* — recorded so the Run B build starts from it. His two notes on item 7 carried into ADR-0091: the gate refuses the whole unit of work rather than skipping a page, and the account paths' reliance on the gate alone is what he would revisit first. Two decision sheets for him: blocker 26 (the retry loop after a spent secret; A cap, B ask again, C both; the sheet argues C) and distance item 3's second half (what the nationality page asks that the registry does not hold, in four groups — residence over time, UK status claims, study in the UK before, the passport when there is none — with options and a recommendation per group) | The nationality page's seventy-two fields sorted by registry reach: four reached, the rest in the four groups. Distance to Run A: five items open of ten | His answers on the two sheets |
| **P135** | Distance item 7's second half: ADR-0091 applied to the fill path. Before every unit of work the Automation Runner performs, a gate reads the portal's `robots.txt` as `askimate-aas-runner`, keeps it ten minutes, decides every URL the work would open, and refuses the work with the contract's new `robots_disallows` when any is disallowed or the file could not be read (failing closed). The fill session refuses a disallowed navigation again and a disallowed subresource at its request guard, and paces its navigations at the site's Crawl-delay or the one-second floor, whichever is longer. Proved red-first on the fixture portal's own `robots.txt` (`/private/` refused at the gate and at the session; two navigations one second apart) and in the gate's unit tests (kept, re-read, fail closed, the floor); the journey runs the real performer through the gate | At Vahid's question, the interview's coverage written down under Run B: seven of twenty-seven registry fields have a question, the rest stop with *"No question is defined"*; Run A's profile is confirmed through the store and never meets it; Run B meets it first. Not applied, said so in ADR-0091: the account paths open one page each and rely on the gate's decision; the runner keeps no `robots.json`. Item 7 done. Distance to Run A: five items open of ten | Blocker 25; Iman's review; his word on the interview build |
| **P134** | ADR-0112 built on Vahid's confirmation (*"Confirmed, all four, as proposed"*), his three reasons and one condition in the ADR in his words: the award date as the one date most likely to be compared against a document, because the certificate carries it — what an admissions office does with the claim, not a form-filling argument; the completion year removed because *"beside an end date it would be a second truth about one fact, and two fields that can disagree is what this system refuses"*; the three-way end read off the portal, not imagined; and the extraction reading an award date as month and year or nothing, never a year with a month we chose. `Qualification`: `start`, an `end` that always carries a date with the student's claim (completed, expected, discontinued), an optional `award`, `completionYear` gone; seven test files and the gated fixture moved; the transcript plan reads entry, completion and award, the award refused as a bare year — both cases tested. Set 0.3.22 maps the six education date selects per qualification, the award boxes empty when there is none | Proved: two qualifications typed once each, the second's expected end as the date it is and its award empty; the four date selects leave the plan's `no_mapping` blockers — forty remain, all on the three unmapped pages; a profile with no list is unavailable on the four. Blocker 27 closed; distance item 5's registry gap closed, blocker 25 and the reviewer's option maps remaining. Distance to Run A: six items open of ten | Blocker 25; Iman's review |
| **P133** | Blocker 27 decided by Vahid (ADR-0112), in his words: the dates go into `Qualification`, the same reasoning as employment — *"a qualification without dates is not a qualification anyone can assess"* — a start and an end as month and year, the shape to be proposed and confirmed as last time. Proposed in the ADR: `start`, an `end` that is always a date with the student's claim about it (completed, expected, discontinued — the third because the form names the case), an optional `award`, and `completionYear` removed as a second truth. His question answered rather than decided silently: hold the award date on its own, optional, never derived from the end — a different fact, the one an assessor checks against the certificate, invention if derived, and absence claiming nothing because the portal's boxes are simply left empty | His two instructions carried: the trailing-space and duplicate subject values are his to remember when a mapping is authored, recorded; the *Qualification* row's star is unsettled until someone looks at the screen, and no later phase may resolve it by the more convenient reading — written where the flag stands. Nothing built. Distance to Run A: six items open of ten | His confirmation of the shape |
| **P132** | Item 5's read, as it stood (`--as-is`, 645da1f): United Kingdom, University of Sheffield, grading system 7 (*UK Bachelors Degree (BA, BSc)*), *business* searched. The grade list arrived — system 7's six grades and two non-grades, values as labels; Sheffield's four grading systems by numeric id with the *Not in list* escape; the subject results by search-then-select, eighty-seven for one word, two values with a trailing space the label hides and one listed twice; the hidden institution select holding `SHEFFIELD`, the value P118's typeahead entry carries. The chain `institutionCountry → institutionCode → gradingSystemId → grade` folded into the curated draft 0.2.21 from observation; set 0.3.21 re-bound; pinned in the draft test | Found by the read: the registry's `Qualification` has no start or end date and the page's four date selects are mandatory in every read — blocker 27, his decision, the shape of item 2; no mapping authored. `degree` and `unlistedDegree` came back unlabelled and unmarked where the third read had them marked; the file has no markup, so the draft keeps the marks and the pack flags the difference. Per-institution and per-system lists are recorded as such. Distance to Run A: six items open of ten | His decision on blocker 27; blocker 25 |
| **P131** | The summary page's markup committed by Vahid (ebac18d, checked by him for his surname and e-mail: zero matches). Employment's listing has exactly education's shape — `div.homepageInfomation > h5` *Previous Employment N*, then the entry's table and its *Edit* / *Delete* links — so the locator authored on his word stands and the draft (0.2.20) is not corrected; `preparation.test.ts` serves the captured page to the runner's own `count`: one employment entry, two education entries, none for a section without one. Carried at his word, not absorbed: ADR-0106 gains a stated limit — a repeating page's entries are counted, never read back; possible only when a portal offers an edit view of a saved entry, and not before — and the markup shows Sheffield does (`employment.do?update=N`), so it is possible in principle and neither built nor decided. The review pack's fifty markers now say, in his words, that one star marks every control in its row, so the marked count is an upper bound to be read as rows, not separately-required fields | Also from the markup: the add link is the bare `employment.do`; employment's delete has no confirmation where education's does. Distance to Run A: six items open of ten | His item 5 read |
| **P130** | Vahid's read of `summary.do` with one throwaway job saved and deleted after (9a0bcea): the heading reads exactly *Previous Employment 1*, the same shape as education's; the curated draft 0.2.20 names the listing (`h5:text-matches("^Previous Employment [0-9]+$")` in `div.homepageInfomation`, the wrapper on his word since the capture's HTML is not in the commit), pinned in the draft test; set 0.3.20 re-bound. `employment.do?new=true` opens empty after a save, so it is the way in and no add-another control is needed. Five marked on the form: `startMonth`, `startYear`, `position`, `employerDetails`, `duties` — the fifth is the row's star attached to the year that shares the date row, moot for the fill. The summary lists a job's dates, title and employer and **not** its duties | Checked against ADR-0106 as decided: a per-item page is counted on its listing and never read back by value (§2), so nothing confirms a saved job's values today and the count is the whole verification; his observation rules out a listing read-back of duties ever, and a value read-back would need the entry's own page, which is not in the record. Distance to Run A: six items open of ten | His item 5 read |
| **P129** | ADR-0111 built on Vahid's confirmation (*"Shape confirmed. Build it as proposed."*), the referee narrowed by his word to a name and a role — no email, no phone, *"Hold what we need when we need it"* — and his two reasons in the ADR: an end that is a date or "current", never a blank, and the referee as B1 row 4's reference-letter problem in a new place. Registry: `employment.history`, list-valued, ordinary, the entry with employer, address, position, a start month and year, an explicit end, basis, duties, referee. Two format rules: `part … absent: "leave_empty"` (a current job's end boxes left empty by the student's statement) and `join` (name and address into Sheffield's one box). Curated draft 0.2.19: `page8` *Employment history*, repeating over the group; set 0.3.19 maps the four required fields, the start year and the end date per job | Proved: two jobs typed per item, the current one's end empty; a confirmed empty list fills the page zero times and the preview says *Employment history: none — the page is left as it is* inside the yes; a profile never asked is `value_unavailable` on the four, so confirmed-empty and unasked are different. The plan's `no_mapping` blockers are now the forty-four of the three unmapped pages. Not built, said so: the interview asks for no list-valued field yet; `maxlength` is not checked at the plan; the `summary.do` listing waits on his throwaway save. Item 2 done. Distance to Run A: six items open of ten | His item 5 read and the `summary.do` capture, same sitting |
| **P128** | Item 2 decided by Vahid (ADR-0111), in his words: employment goes into the registry as a new group, one entry per job, designed for the general case; the page marked repeating as education is; a page filled zero times said plainly in the preview. The fact that shaped it, from `summary.do` in the portal's words: *"No employment information entered. If you do not have any relevant employment then you do not need to complete this section."* — the section is optional, the stars apply per job, no student is stuck, and P127's "not in the record" is corrected. The shape proposed in the ADR — `employment.history`, each entry employer, address, position, start month and year, an explicit end-or-current, basis, duties, an optional referee named as a third party's data — awaiting his confirmation; nothing built | Confirmed-empty is not unavailable: a student with no work history confirms an empty list, the plan fills the page zero times, and the preview's existing zero-count line (P96) says so inside the hash. Two things the build needs from him beyond the shape: the `summary.do` listing for a saved job (his account holds none) and the add-another control. Distance to Run A: seven items open of ten | His confirmation of the shape; his item 5 read |
| **P127** | Item 5's read could not be taken as the tool stood: the attached read opens each page in its own tab and the education page's grade list, loaded only after an institution is chosen, is gone before the read. `--as-is` on `inspect:attached`: the session adopts the person's own open tab at the URL — none or several refused, never navigated, never closed — and reads it as it stands; the `__name` shim put in by hand because `addInitScript` reaches a tab only on its next navigation; run record `readInPlace: true`. Proved red-first on the fixture's passport list (loaded after the nationality) in-process and through the real command under tsx. Runbook paragraph; the education page's steps in order in the 2026-09-10 README | Item 2 read from the registry for his decision: `startMonth`, `position`, `employerDetails`, `duties` are an employment history, and the registry holds no field of a student's working life under any name — new facts, his call; the page is one entry per job and the draft does not yet mark it repeating; whether an entry is required at all is not in the record. Distance to Run A: seven items open of ten | His item 5 read with `--as-is`; his item 2 decision |
| **P126** | Vahid's word to stop, in his words — the ceiling is 106 of 149 because 43 fields have no question of their own; the 13 between are markup a positional rule cannot reach, not work left undone; 79 right is better than 93 with 14 wrong — and the fold: curated draft 0.2.18 carries the third read's 79 row-text labels and 50 observed markers on the five pages, the fourteen wrong labels excluded (names stand), the thirteen as their names; set 0.3.18 re-bound, unchanged. Review pack revised: the thirteen listed on their own with page and field and their rows marked; the fifty markers tabled; two flagged as the author's (language's eighteen of twenty as the test block's rule; `unlistedDegree` marked by the select's star) | `scripts/sheffield-draft.test.ts` pins 79, 50, the fourteen, the thirteen, the two carried marks, and the plan: forty-four observed-mandatory fields on nationality, language and education are `no_mapping` beside employment's four — items 3, 4 and 5 made visible; a handed slot's six companions are not blockers (ADR-0107). Item 1 closed with thirteen carried into item 6. Distance to Run A: seven items open of ten | Iman's sitting: the thirteen from the screenshots, the fifty confirmed; items 2 and 5 his reads |
| **P125** | Vahid's third read of the five pages (2026-09-14, bd4fa2b): 93 of 149 labelled, 50 marked, read from the file against the re-read. The fifty-six unlabelled sorted: forty-three with no question of their own (the search boxes, five tied labels, the repeating country and other-institution blocks under one header, the six file inputs whose companions carry the slot's text) and thirteen that matter — nationality's eleven top questions and two marked language radios — which three reads have not reached, nationality's markers unmoved at twenty. The re-read's wrong labels are gone as they were and fourteen new ones stand: five on education carrying the previous control's help sentence before their own question, nine carrying a table's column header. `subject`'s "Results:" corrected: it was never wrong | The observer refuses both shapes rather than guessing — own words of more than one sentence or ending as a statement give no label with the marker kept; a question row above is taken only over one control or one group — red first on a fixture in the file's own texts. The ceiling stated: the thirteen sit where no rule over rows can look; a fourth read would not reach 149 or 27; the recommendation is to stop and read them from the screenshots at review, the pack's own fallback. Distance to Run A: eight items open of ten | His word to stop; then the fold into the curated draft, the fourteen excluded |
| **P124** | Vahid's re-read of the five pages with the row-text observer (2026-09-14, 9a3a1db): 76 of 149 labelled, 42 marked, and item 1 not closed — the seventy-three unlabelled named from the file (nationality's top selects, its dates, all four previous-country blocks; two marked-but-unlabelled language radios; education's slots; marketing's other-institution boxes), four labels wrong where one row asks several things, every companion's "later" option carrying a comment node's markup as its words. His six console observations checked against the file: four held, one half (the no-passport instruction is not in the file), one corrected (twelve starred yes/no groups on nationality, not `refugeeStatus` alone — his snippet's name filter). Forty-two against his twenty-seven: nationality marks twenty, language eighteen, education four, consistent | The observer now reads a control's own words (between the previous control and itself; not for or after a radio), then the row's question, then the question row above, marker from either; a comment node is never a radio's words. Each proved on the fixture, red first. Iman's 23 judgement rows and 193 mechanical stand; the `required` section grows by the observed markers once the read is complete, and no time figure was ever recorded. Distance to Run A: eight items open of ten | His third read; the draft folds it in only when complete |
| **P123** | Vahid's attached read of the five unlabelled pages (2026-09-14) recorded as not closing item 1: the label is the field's name on 139 of 149 fields, no asterisk, nothing required, because the observer resolves labels only through markup ties and these pages tie none. The observer now reads the row's question (`labelSource: "row_text"`) and the visible `*` (`required`, source `observed_marker`), a radio's own words from the text after it, a row that names no question left as the field's name, an empty tied label as no label; the attached summary prints the two counts | Said plainly at his request rather than worked around. Proved on a fixture in the shape he described; his re-read with the updated tool closes item 1. Distance to Run A: eight items open of ten | His re-read of the five pages |
| **P122** | ADR-0110 built: a run may start on an account the student already holds — decided by Vahid, 2026-09-14 (*"the run enters my existing account. Not a fresh synthetic applicant."*), the profile synthetic and the account his, two decisions. `existing_account` decision before the yes; `PortalAccountDeclared` on the case; the driver derives the account from it; ADR-0101 §3's sign-in path runs from the start with its own wording; the handover drops both address proofs for an account the student made (his waiver, in his words). Proved through the five real processes on the fixture, the account registered beforehand, no creation intent ever opened. Item 7: his robots.txt read evaluated by the runner's matcher — every observed path allowed, the `/user/*` disallows path rules; the matcher's false "Allowed by" reason found and fixed red-first | The in-process journey shares one key provider and adds headers by hand; the process journey does neither, and the existing-account variant runs the same way. The fill run still reads no robots.txt of its own (P123). Distance to Run A: eight items open of ten | Items 1, 2 and 5 are his reads; P123 is mine |
| **P121** | The journey through the five real processes the local-stack script starts (`scripts/local-stack-journey.test.ts`): HTTP, the real frame from the Secure Service process, the Worker's own clock past the yes, the Runner process creating the account and filling the fixture portal with the Fill Agent typing over CDP, the handover, ready to submit, nothing submitted. Five defects found by it and fixed red-first: the Worker's env without the portal origins (the yes voided as `content_changed` every tick); no page or control served; the Fill Agent's certificate under `x-aas-service` against a Secure Service reading `x-service-cert`, masked by three test wrappers; `__name` under `tsx` at the sensitive-context door; two random local master keys in the two secure-plane processes | Every one of the five was invisible to `scripts/journey.test.ts`, which builds the planes in one process, shares one key provider between its vaults and added the header by hand. `SERVICE_CERTIFICATE_HEADER` at every hop; `AAS_SECURE_LOCAL_MASTER_KEY` read by both processes, refused in production; the shim at `openSensitiveContext`; the script builds and serves both bundles and gives the Worker the origins. Blocker 26 raised. Distance to Run A: nine items open of ten | The same processes, pointed at a reviewed entry from a machine that reaches the portal; blocker 26 |
| **P120** | The local-stack runbook (distance item 9): `scripts/local-stack.sh` stands the five deployables up on one machine, migrated, and checks each; proved by a test against the fixture; the Sheffield variant documented. Found on the way, fixed red-first: the runner's entry point launched its browser with no remote-debugging port, so the CDP endpoint it declared to the Fill Agent was served by nothing | From the real deployable, every credential fill would have failed at the password. The browser now listens where `AAS_BROWSER_CDP_URL` says; a portless URL is refused; `main.test.ts` starts the real entry point. Distance to Run A: nine items open of ten | The script runs from a machine that reaches the portal; the rest of the distance list stands |
| **P119** | The distance to an end-to-end run against a reviewed Sheffield entry, read from the repository and written down (`docs/distance-to-a-reviewed-sheffield-run.md`): ten items for the fill run, three more for the product; blocker 25 raised — the institution box cannot be mapped by rule from a free-text profile field | Vahid, 2026-09-13: *"I have lost track of how close that is, and I would rather know than assume."* The record's own numbers: five pages whose mandatory fields were never read (placeholder labels); the employment page blocking the plan (`no_mapping` on four); nationality and language unmapped; the education page's grade list unread with an institution set; review; `robots.txt` never read; the account and how a run enters it; no local-stack runbook (mine); Part 2 unread. Every report from here carries the declared-but-unreachable number and the distance count | The reads are his; the runbook is mine; blocker 25 is his to decide |
| **P118** | ADR-0109: a typeahead mapping names the value the form submits — decided by Vahid, 2026-09-13, after his reading of the `<select>` settled P117's contradiction as his own mislabelled copy: *"Name the value."* Both conditions built: the reviewer records the text as the field's `options`, and the runner chooses the ONE entry that reads that text AND carries the value; the preview shows the text | The usable-set check refuses a mapped typeahead without entries, a value not among them, free text, and the escape by value — which holds where the escape's value is its own label, as on Sheffield. The runner refuses the escape before typing, for confirmed values and constants alike; P102's open case closed. Two entries that read the same are told apart by value, proved on the fixture. Fifteen tests red before the build. Sheffield draft 0.2.17 records the institution box's eleven entries and its escape | A mapping to the institution box waits on the reviewer; the country box's entries are the captured `<select>`'s; his day-later read on the `<select>` tests drift |
| **P117** | The institution dropdown read live (Vahid, 2026-09-13): eleven entries with values; the two identical *Sheffield International College* entries carry different values, so they are distinguishable by value and not by text; but every value differs from his 2026-09-11 copy with order and text identical | *"Hold the value-versus-text decision. It is not answerable on this evidence."* Held. The earlier copy never entered the record (P102: *"still to arrive"*), so nothing recorded is contradicted. Nothing names either box; the *Not in list* escape stays behind the decision. Offered: a same-sitting read of three layers — dropdown value, posted `<select>` value, search response — to separate a layer difference from drift, before his day-later read | His day-later read, and the three-layer read if he takes it |
| **P116** | The contact page read back (Vahid, 2026-09-13): ten fields, nothing normalised, the postcode stored exactly as typed; the UK postcode is two boxes, and the mapping that typed it as one was wrong | New `uk_postcode` format (`outward` / `inward`): split at the postcode's own seam, never 3+3; refuses a non-postcode; case left as written. Set 0.3.16 maps both boxes; a non-UK address hides both. Five shapes and seven non-postcodes, red first. His two passing observations checked against the capture: `confirmEmail` and `corrTelephone` are the captured names | The `perm*` boxes stay unmapped; the repeating page's listing is counted by a test on his shape, not yet by a run |
| **P115** | The field-by-field comparison on `personal.do` (Vahid, 2026-09-13): six fields, shape compared, nothing normalised, no values reported | *"Phase one works as built on this portal for the personal page. No vocabulary gap from this read."* A select is compared against the option value the runner chose, so codes and month names match by construction; the two free-text fields came back as typed | The contact page still wanted: the postcode — likeliest to be normalised, and mapped as one value into what the capture read as two boxes — and the e-mail asked twice |
| **P114** | `personal.do` reopened after a save comes back filled (Vahid, 2026-09-13): the reopen-and-read of ADR-0106 has a page to read on Sheffield; his limit kept — he did not compare field by field | The read-back is exact on the redacted shape recorded at the fill, so a normalised value reads as not kept and the page as `uncertain` every run; his filled-form reading cannot tell an exact echo from a normalisation. Asked for: a field-by-field comparison reporting only the fields that differ and how, no values. Nothing changed in code or draft | If a field is normalised, a vocabulary gap — *expected to be stored as* — to raise |
| **P113** | The Previous Education listing read from `summary.do` (Vahid, 2026-09-13): an `h5` reading exactly *Previous Education N*, no class, no id, in wrappers every section shares. His question — text-shaped or structural? — answered: text-shaped, exact to the number, is the right one here | Draft 0.2.16 names the listing with `:text-matches("^Previous Education [0-9]+$")` under the misspelled wrapper class kept as written; a runner test on a fixture built to his reading proves the count is the entries (2), not the sections (4) or a substring (3). P82 removed an unreviewed heuristic, not a reviewer-named locator; the `label` strategy was always text | Employment's listing is one more read when that page is mapped; the mechanism is already general |
| **P112** | The nationality uploads settled by Vahid's read of `showHideDocumentUploads()` (2026-09-13): all five hidden, each shown only on a claim of UK status; *"a path our students do not take"*. The draft had carried them as five ordinary slots; corrected in 0.2.15 before Iman's review | `visibleWhen` on four file inputs from the function's own reads; the fifth is a disjunction the vocabulary cannot say — recorded, not bent. Test: the four hidden in a plan for an international student, nothing mapped to any of the five, no upload or blocker names one. Sheffield's handlers name absent ids and the container names differ from the input names — his finding, not concluded from. His question answered: a read without saving would be enough and is not needed | The condition vocabulary has no *or*; raised, not built. The companions are moot for the path |
| **P111** | The nationality handlers read live (Vahid, 2026-09-13): five `onchange`s each tick `<slot>UploadRadio` by id, and no radio on the page carried such an id. Reported by him as a contradiction, not resolved; recorded the same way | The intent is recorded verbatim; the five companions are NOT in the draft, because the id-to-group step is name similarity until a radio is seen with the id, and because a handler naming an absent id throws — so whether the page ticks anything on attach is unknown. The runner would need only name and value, both read. Four console reads that settle it are in the README | His reading; then either five companions and the two-acts sentence stands, or a reviewer's pairing flagged as such and the sentence corrected to eleven of seventeen |
| **P110** | Personal, contact and nationality read with the snippet (Vahid, 2026-09-12): twenty-three groups' values in the draft (0.2.14); found and fixed, fails first: the runner read `yes` as *tick this radio* before looking at values, and on a `yes` / `no` group with *no* first ticked *no* | The value is tried first, always, in the portal's case; the boolean shortcut is for a lone radio only; `Yes` on a group offering `yes` is refused with nothing chosen. Recorded: the case differs by page and nothing normalises; three *not providing* options are two claims in one; `NotRequired` is absent on nationality; `corrContactDateType`'s *After* reads *From this date:* — pinned for Iman. Nationality's five slots carry no companion until the file inputs' handlers are read | A constant on a radio is not checked against the group at review time, only at the fill — raised; the handler read is his offer, taken up |
| **P109** | The language page's radios read live (Vahid, 2026-09-12): the certificate group's four values and companion in the draft (0.2.13), `previousEnglishEducation` as `Yes` / `No`; his two method caveats honoured — labels from the capture's per-option read, none of his parent-text strings recorded; `NotRequired`'s third meaning noted | *"Another thing we never choose, for a third distinct reason."* The token has meant the document, Sheffield's need, and the applicant's status on one form; the test asserts no companion and no mapping names it. Nothing on the page is mapped, so the companion is left as the form has it. A read-only console snippet is recorded for the exact wording and for the twenty-three groups still carrying ids | Twenty-three groups on personal, contact and nationality wait on that reading; no mapping may name one until then |
| **P108** | The Documentary Evidence radios read from the live page (Vahid, 2026-09-12): six groups, `Uploaded` / `UploadLater` / `NotSending` / `NotRequired` on each; the draft (0.2.12) names the three on every companion; the fourth value checked unreachable and the one trap closed; `certificateStatus`'s pairing flagged for Iman; the two unlabelled options recorded as read | *"Whatever the blueprint does with it, we never choose it."* Tests: a mapping of the fourth value on a companion is refused; a plan names only the attach or defer value; nothing in a plan from the draft names `NotRequired` or `NotSending`. Found and closed, fails first: a required companion of a handed slot raised `no_mapping` on the repeating page, a blocker only the refused mapping could answer. The capture's radio values were element ids — the runner sets a radio by `value`, so none would have matched. The set (0.3.10) says not to sign while the page's own fields are unmapped: a run would save a qualification that says only *later* six times. `parseBlueprint` admits an empty option label, because two of the groups show none | The language group still carries ids; the pairing question is Iman's; the education fields' mappings wait on the reviewer |
| **P107.1** | CI #204 red on the boundary check: the run driver compared a step's kind itself at the yes; narrowed through the orchestrator's `awaitsStudentAuthorisation` instead. No behaviour change | The boundary check runs after the last edit, not before it |
| **P107** | ADR-0108: what the student owes is a record on the case — blocker 24 decided A by Vahid; the record, the student's word, the reading, and the sentence nobody wanted to write | His words: *"Telling them once in a message that scrolls away is not a record of it, it is a mention of it."* Two case events: `OwnActRecorded` with the yes, one per slot per entry from the authorised preview, with what the portal was told; `OwnActDone` on the student's word, through the new `attached_myself` decision (a key, no hash), answered before the run's situation so it works after the handover too; an unknown key is refused. `GET …/runs` carries `ownActs`; the client lists them under *Still yours to do — nobody is watching this, and nobody will remind you* with a button per open item. The preview says *Nobody is watching this, and nobody will remind you* before the yes; the handover no longer calls an application with something owed *complete*, says the institution has been told these are coming later, that nobody is watching, that it will treat the application as incomplete and *I will not know*. No reminder, on his decision and his reasons. Fails first: four tests red before the fix; the journey records, reads and closes through the real routes |
| **P106** | ADR-0107: a handed slot's companion says "later" — blocker 23 decided A by Vahid; the line it must never cross enforced; the preview in the student's words; blocker 24 raised | His reason: *"'I will upload this later' is not a claim about the document, it is a statement about when … nothing is being sent in this act."* The blueprint names a companion's defer option and its not-providing option; a companion is mapped by nothing (ADR-0105's admission withdrawn), and a mapping naming the not-providing value is refused in his words; a handed slot with no defer value named is admitted only on a page the plan fills nothing on. The plan sets the companion to the defer value once per entry as a reviewed constant marked as the slot's deferral; the slot's handoff names what is told. The preview says under each entry *We are telling <institution> that your Certificate is coming later. You attach it yourself. The application is not complete until you do.*, and the hash holds the value. The handover says it beside each document. The fixture drops an unanswered save silently as Sheffield does; the journey saves two qualifications with *later*. Sheffield's set (0.3.9) no longer hands the six radios; their defer values wait on a copy of the fieldset. What happens to the deferred state after the handover — today, one sentence and nothing else — is blocker 24, with options priced |
| **P105** | ADR-0106: a page is saved when the portal shows it — blocker 22 decided by Vahid; the three read-backs built; blocker 23 raised | His words: *"Your cost answer: accepted … Build the three phases in the order you set."* Built, each failing first: after the press the runner reopens a page and reads every filled value back (compared on the redacted shape recorded at the fill — a changed value is not a kept one); a repeating page's listing is counted before and after (`repeats.recorded`, rebased onto the deployed origin at the claim); a slot the runner attached to must show the marker the blueprint names (`requiredDocuments[].recorded`). Anything not seen is `uncertain` with the new `not_recorded` code, and no transmission is recorded — the wire refuses one beside it. The fixture portal's pages re-render what they hold, its education page lists what was saved, its documents page shows a held file; the journey walks all three. The cause of his dropped save is settled — the unanswered radios — and that makes ADR-0105's handed-with-its-slot shape a page that never saves on Sheffield: raised as blocker 23 with the options priced, on his instruction, not decided |
| **P104** | Blocker 22 met on the real portal; its shape restated by Vahid; the cost answered before building | A second qualification saved with two radios unanswered: no error, and `summary.do` listing one entry — dropped silently, so an error locator or a landing URL would both have passed. His shape: saved when the portal shows the thing exists; named where a blueprint can; `uncertain` where it cannot, and uncertain is a specialist's problem. The sheet prices it against what exists: `uncertain` is already built end to end (the ledger leaves the intent open, the next claim stops, a person resumes or abandons; no transmission is written), so the rule costs a look per unverifiable page; reopen-and-read of the page's own fields costs the reviewer nothing and holds on most server-rendered forms; a listing count per repeating page and a marker per attached slot are vocabulary the reviewer names from a copy; a portal with nothing to read is a look per page — nine on Sheffield. Three phases estimated, each fails first. Why the save failed is unknown; two specific observations would settle it. Nothing built |
| **P103** | The education page after a save, observed: the P99 condition closed; a save pressed is not a save read | Vahid saved one qualification and reopened `education.do?new=true` empty: the repeat's continuation is the page's own URL, not a fifth gap, closed in his words with his caveat (*one observation, not a property shown at every count*) — and the per-item walk does depend on it, since a second opening that showed the first entry would be typed over and saved with nothing noticing; the second observation is asked for. The evidence radios accept *later* and *not providing* without a file, so a page saves with no document; whether it saves with a radio *unanswered* is unobserved and bears on ADR-0105's handed-with-its-slot shape. The save landed on *Your Details*: the runner reaches each item by URL and does not care — but after the press it reads nothing, so a refused save is reported as saved. Blocker 22 raised with a proposal; not built |
| **P102** | The institution box observed: a typeahead's entries may follow another field (found, fixed, fails first); the duplicate entries as an observed case; "Not in list" checked | Vahid observed the live institution search: a GET per keystroke carrying the typed text *and the chosen country*; *Sheffield International College* offered twice; *Not in list* closing the list. The dependency could not be written before: `checkUsable` refused `optionsAfter` on a typeahead as offering no options, and the execution would have waited on the text box as a list. Written into the fixture (the course search takes a level), watched fail, fixed: the order rules and the press apply to a typeahead, its wait is its own at the fill. Draft 0.2.11 says `institution-ts-control` follows the country box. The duplicate is P95's refusal observed rather than fixtured. *Not in list*: nothing today stops a text that reads so from choosing it — proven, OPEN; the guard (the reviewer names the escape on the blueprint) waits on the value-versus-text decision so it is built once |
| **P101** | The Tom Select entry locator confirmed from the markup Vahid copied; the runner's match is on visible text, proven | He copied the live country box's dropdown: list id `<name>-ts-dropdown`, entries `div.option[role=option][data-selectable]` with a `data-value` the form submits that is not the visible text, and state classes. The draft (0.2.10) names each box's own list by id and an entry by role and mark — the class-based default would have matched both boxes' entries at once. Proven against a fixture with his shape: the runner matches the text an entry shows, refuses the submitted value and the wrong case, ignores the classes. His expectation that a mapping names the submitted value is recorded as a proposal — buildable through the blueprint's `options`, as selects already work — and undecided; no mapping to either box is signed. The institution box's loading is what one more copy would settle |
| **P100** | ADR-0105: a slot's companion handed with its slot; a list's options loaded by a press | Both decided by Vahid in his words. *"'My transcript is in English' is not the student attaching something, it is the student answering the question the slot asks"* — a `student_handoff` on a slot's own companion is admitted when the slot is handed too, on any page, and nothing else is; it crosses with the slot, the preview says *You answer yourself* under the entry, the handover names it. `optionsAfter.press` names the control that loads a list — Sheffield's subject is a search-then-select — guarded three ways: `checkUsable` refuses a press that is any page's advance, add-another or the submission control; the runner refuses a submission name at the click; the page's URL is read before and after and a press that left the page is drift. The limit stated before building: a save that neither navigates nor reads as a save is not distinguishable from a lookup; naming the control is the reviewer's act. Fixture: a certificate status radio; start dates shown on a press; the journey holds both. Sheffield: six status radios handed with their slots; `subject` presses `subjectSearchButton` |
| **P99** | Vahid's live read of three Sheffield pages, confirmed against the capture; two shape findings raised, not built | The education page's continuation is its own `education.do?new=true` URL, which the built shape expresses with no *add another* control — one condition for the next read. Six file inputs are in the capture where four rows show; the radios' *in English* option and the asterisks are on his report alone, the capture having no labels for that page. `documents.do` has five description boxes and no radios; the 50MB page total is not expressible per slot; the institution's words on what must not go there bind the reviewer. Subject is a search-then-select, not a typeahead: the wait is marked, the button press between is not expressible. Two proposals wait on him: a slot's companion radio handed to the student with the slot; `optionsAfter` naming a control to press. Accepted formats recorded per page (draft 0.2.8) |
| **P98** | ADR-0104: blocker 21 decided — a repeating page's documents are the student's own act, said under each entry; a condition inside a repeat answered per item | Vahid's decision in his words: B, with the per-item condition; C out; A an option, not the end state. `checkUsable` admits a `student_handoff` on a repeating page's document slots and nothing else on it, and a condition that looks at the page; the plan carries the handoff once per item and evaluates the page's conditions per item against that item's own values, recording what is hidden for which entry; a document slot left to the student does not refuse transport; the preview says *You attach yourself* under each entry, inside the hash; the handover message names the same acts with their entry. The fixture's education form takes a certificate and asks a grade only of a school qualification; the journey holds the diploma's grade typed, the bachelor's box untouched, nothing attached by the runner, and the preview's two lines. Sheffield's `page7` is marked repeating with its six slots left to the student |
| **P97** | Deliberate regressions over the four gaps ADR-0103 built; the companion's value was never in the hash | Fifteen mutations applied to disk and restored byte-for-byte: the companion's check, entry and hash; the late option's order, wait and bound; the typeahead's check and its two wrong choices; the repeating page's rendering, ledger identity, hash position, driver filter and add-another; the repeated fieldRef. **Preparing M3 found a defect**: the hash held the literal text `{attachment.companion.text}`, so the mark beside a document was never in the yes — fixed, test first. Eleven of fifteen caught on the first pass; three weak tests made strong (the order rule masked by the type rule; the typeahead never meeting two identical or one near entry); one kept as labelled redundancy (the entry's index over a stable sort). `docs/p97-regression-audit.md` |
| **P96** | ADR-0103 gap 3 built — a page filled once per item of a list; each item its own page to the ledger; every entry in the preview | `BlueprintPage.repeats` names the list a page repeats over; the profile package says which fields are lists and the parser refuses any other. `checkUsable` refuses a mapping on the page drawing from anything but that list, a document, handoff or credential mapped on it, or a condition. The plan renders each item through the mapping's rule with the list's provenance and carries `item` on the instruction; an unconfirmed optional block is filled zero times and asks for nothing. The preview lists each entry in order and says none plainly, count and position inside the hash. The driver offers the page once per item, each its own ledger target; the runner returns to the page, presses add-another, saves one. The journey adds two qualifications end to end. All four ADR-0103 gaps are built; a fifth is raised — Sheffield's education page carries documents per qualification and a condition, which the rule refuses on a repeating page once mapped |
| **P95** | ADR-0103 gap 2 built — a typeahead is typed into and the one exact entry chosen; a fill, not an advance | `FieldInputType` gains `typeahead` and the field says where its entries are found; `checkUsable` refuses one that does not, and entries on a field that is not one. The runner types the text, waits a bounded five seconds for exactly one entry reading it, and clicks that entry; none or more than one fails with what was offered and chooses nothing; an entry reading as a submission is refused. The fixture portal's study page asks the course through a server-answered search; the Sheffield draft marks its two Tom Select boxes, entry locator flagged as the library's default pending the next read |
| **P94** | ADR-0103 gap 1 built — options that arrive after another field: ordered, waited for, never chosen among | `BlueprintField.optionsAfter` names the field whose setting loads this one's options. `checkUsable` refuses an order the fill could not follow, a dependent with nothing to wait for, and a mapped dependent whose earlier field nothing maps. The plan carries it to the runner, which waits a bounded five seconds for the one named option before selecting and fails the page as drift with what the list offered if it never arrives. The fixture portal's apply page now fills the passport-country list after the nationality and after a round trip; the gated fixture, the demonstration and the Sheffield draft's education chain record the dependency |
| **P93** | ADR-0103; gap 4 built — a document slot's companion is planned after the attach, entered and read back | The first real form showed four things the schema could not say; ADR-0103 decides all four and builds the fourth: `RequiredDocument.companion` names the control set beside a slot when a file is placed and the value it must hold. The plan carries it with the upload only when a document is mapped; the runner sets it after the attach and reads it back; the preview names it in the option's own words inside the hash; `checkUsable` refuses a companion that is mapped, absent or not offered. Proved on the fixture portal, whose documents page now refuses a save without the status. The Sheffield draft carries it on seven slots; ten wait on a re-read. Found on the way: two pages shared `certificate`/`certificateStatus`, so the parser now refuses a repeated fieldRef |
| **P92** | AUTH 4 and 5 in Vahid's words; the chooser picks `student_chosen`; the refusal proved to still bite | Two facts no page read could settle are settled by his direct statement of 2026-09-11, marked observed by him and not by a run: no verification step between *Start Application* and the form; no code at sign-in. Recorded with his caveat that this is Sheffield's behaviour and not a property of direct portals — per-target work. With all eight facts observed the approach chooser picks `student_chosen` for this entry; the test holds that, and holds one fact at a time that the chooser still refuses when either is set back to unobserved |
| **P85** | The dependencies read, the Article 9 sheet, and the draft blueprint with the course and intake set | Vahid's `inspect-dependencies` output recorded and read: the education chain is confirmed by handler (four selects empty until the one before them is set and the server answers — an order and a wait the schema cannot say); sixteen of seventeen file inputs tick their own *upload now* radio by `onchange`, the English-language slot does not, and the five other-documents radios were not in the capture at all — the runner's `setInputFiles` fires the page's script and verifies nothing, so the draft carries the radio as a field beside each slot and names the relation as a fourth schema gap. The equal-opportunities page asks eleven disability boxes and an ethnic-origin select: Article 9, Vahid's decision, written as blocker 20's sheet — B (pass through, unstored) does not exist here because every typed value is in the preview, the stored plan and the log by design; `student_handoff` would make the whole application untransportable; A (unmapped, passed over, named in the preview) recommended with the two things it needs. The curated draft blueprint, 0.2.0, parses and is refused as `not_reviewed` |

---

## 2 · The architecture as actually built

### Five deployable processes

```
                    ┌──────────────────────────────────────────────┐
   browser ────────▶│  1. Conversation Service    (Conversation)   │──▶ conversation DB
   (student)        │     student surface, routes, run driver      │
                    └──────────────┬───────────────────────────────┘
                                   │ internal API (mTLS)
                    ┌──────────────▼───────────────────────────────┐
   browser ────────▶│  2. Secure Interaction Service    (Secure)   │──▶ secure DB
   (iframe, own     │     the ONLY service that sees a password    │──▶ Redis (ciphertext)
    origin)         └──────────────┬───────────────────────────────┘
                                   │ reads envelope from shared cache
                    ┌──────────────▼───────────────────────────────┐
                    │  3. Fill Agent                    (Secure)   │
                    │     decrypts locally, types over CDP         │
                    └──────────────┬───────────────────────────────┘
                                   │ CDP — the runner's ONLY inbound port
                    ┌──────────────▼───────────────────────────────┐
                    │  4. Automation Runner             (Browser)  │
                    │     NO database · NO vault · NO cache        │
                    └──────────────────────────────────────────────┘
                    ┌──────────────────────────────────────────────┐
                    │  5. Background Worker        (Conversation)  │──▶ conversation DB
                    │     listens on NOTHING                       │
                    └──────────────────────────────────────────────┘
```

**Why it is exactly five.** Migrations are a *command mode* of the owning service
(`aas-conversation-service migrate`), not a sixth process — a separate migrator would need both
planes' credentials, which is the shape ADR-0037 exists to prevent. The worker does not drain the
Secure Plane's outbox; that service drains its own, because a worker that drained it would be the
one process whose compromise yields both databases.

**Two processes deliberately have no health endpoint.** The runner and the worker. A `/healthz` is
not a control API but it *is* an inbound surface, and it would be one on the two processes whose
whole design is that they have none.

### Where state lives

| State | Home | Notes |
|---|---|---|
| Conversation events | `conversations` / event log, PostgreSQL | Append-only, ADR-0031. Resumable SSE reads it (ADR-0035) |
| Case events | `case_events` (jsonb), PostgreSQL | The domain event log; state is `fold`ed, never stored |
| Runs, checkpoints | `workflow_runs` | Optimistic concurrency on `revision` |
| Consequential-action intents | `workflow_action_intents` | Written **before** the action (ADR-0054); page progress is derived from it (ADR-0047) |
| Interventions | `interventions` | What a specialist sees and adjudicates |
| Confirmed profile | `profile_entries` | Its own store; the event log stays a record of events (ADR-0044) |
| Work + worker leases | `work_leases`, `worker_leases` | Runner and worker claim by lease; PostgreSQL decides the race |
| Target offers | `target_offers` + the conversation log | Gate 2 verifies an `offerHash` against *this conversation's own* log |
| Specialist notices | `interventions.notified_at`, beside `announced_at` | Two audiences, two columns: either channel can fail while the other succeeds |
| Secret requests | secure DB, separate | No column in that schema can hold a secret — asserted from `information_schema` after migrating |
| Encrypted credential envelope | Redis, ciphertext only | ≤5-minute TTL ceiling; `verify()` refuses a server whose config would let ciphertext reach disk |
| **Document bytes** | **Nowhere.** No `bytea`, no blob column, no bucket | The vault exists and is reachable from no deployable |

### The boundaries that are enforced structurally, not by convention

- **Branded types.** `ConfirmedValue` cannot be constructed from model output; `StorableUpload` can
  only be minted by `assertStorable`; `DisclosureAuthorisation` only by `authoriseDisclosure`;
  `ReviewedConstant` only from a `UsableMappingSet`. The signature answers "was this reviewed?".
- **`scripts/check-boundaries.ts`.** Import rules per package, plus specific bans: no request logger,
  APM agent or error reporter in the Secure Service (a **measured** reason — body-parser attaches the
  raw request body to a JSON parse error as `err.body`, and `JSON.stringify(err)` emits the password
  in full); no `step.kind ==` branching in the run driver; no `requiredDocuments` anywhere in the
  planning path.
- **Contract drift guard.** The published OpenAPI is checked against the real Express router.
- **Startup refusal.** A process exits non-zero rather than starting unsafely (ADR-0055).

---

## 3 · Every ADR, and whether it is still in force

**Seventy records. Sixty-six Accepted, four Proposed.** None is superseded outright; several are
amended, and the amendment is always a later ADR that says so.

| # | Decision | Status |
|---|---|---|
| 0001 | Integration via HTTPS API + signed webhooks | Accepted · approved 2026-08-26 with Phase 0; the index missed the row until P49 |
| 0002 | AAS is the system of record for the confirmed profile | Accepted · approved 2026-08-26 with Phase 0; the index missed the row until P49 |
| 0003 | Versioned migrations, not `drizzle-kit push --force` | Accepted · approved 2026-08-26 with Phase 0; the index missed the row until P49 |
| 0004 | Branded types make model output unable to reach a form field | Accepted · approved 2026-08-26 with Phase 0; hole closed by 0.2.1 — a brand cannot defend itself |
| 0005 | Contract-first OpenAPI at the AskiMate↔AAS boundary | Accepted · generation claim corrected by 0072 |
| 0006 | Re-application requires an explicit student instruction | Accepted · §1–§5 enforced in the machine by 0072; **§3 amended in P38** — a re-application opens a NEW case, and the path is reachable |
| 0007 | Agent-led conversational intake — the student never fills in a form | Accepted |
| 0008 | Recovery-first escalation, and the learning loop | Accepted · its alerting transport was built in 0071 |
| 0009 | Requirements provenance and multi-source verification | Accepted |
| 0010 | Policy-driven document retention, with no default | Accepted |
| 0011 | Identity check, minor detection, and the minor workflow | Accepted · gate design superseded by 0013 |
| 0012 | AWS region — eu-west-2 (London) | Accepted |
| 0013 | Minor is not a blocker; minor conditions are stage-scoped | Accepted |
| 0014 | Discovery is structurally incapable of submitting | Accepted |
| 0015 | The interview is a capability of AskiMate Chat, not a new interface | Accepted · narrowed by 0051 |
| 0016 | An extracted value must quote the document, or it is discarded | Accepted |
| 0017 | Field mapping is reviewed data, and format rules are data too | Accepted · §1 amended by 0057 |
| 0018 | Amazon Bedrock is the model provider, and no model is named yet | Accepted |
| 0019 | A human specialist curates requirements, through the AskiMate knowledge workflow | Accepted |
| 0020 | The account belongs to the student, and control is handed back | Accepted |
| 0021 | University application requirements are not Student visa requirements | Accepted |
| 0022 | A document in the vault is not permission to send it | Accepted · corrected by 0067, made true by 0068, completed by 0069 |
| 0023 | Retention periods are determined from a source, or recorded as unresolved | Accepted |
| 0024 | Controlled Salesforce-rendering inspection, with four hard boundaries | Accepted |
| 0025 | A fill session is never traced, recorded, or asked to remember a value | Accepted |
| 0026 | A password the model can ask for and never see | Accepted |
| 0027 | One version for the whole repository | Accepted |
| 0028 | Versioning policy: what counts as a release | Accepted |
| 0029 | Git workflow, branches and releases | Accepted · §9 governs commit authorship |
| 0030 | The secure control runs on its own origin | Accepted |
| 0031 | One append-only conversation event log | Accepted |
| 0032 | Cancellation is its own lifecycle | Accepted |
| 0033 | Sessions are `HttpOnly` cookies | Accepted |
| 0034 | The vault is ephemeral, encrypted, shared by ciphertext | Accepted |
| 0035 | Event delivery is resumable SSE over the log | Accepted |
| 0036 | No third-party scripts on authenticated surfaces | Accepted |
| 0037 | Service topology, network boundaries, deployment | Accepted · table amended by 0052 |
| 0038 | Identity is delegated to a managed OIDC provider | Accepted · amended by 0056 |
| 0039 | Repository structure for the independent product | Accepted · client question settled by 0060 |
| 0040 | The wire contract is its own package | Accepted |
| 0041 | One implementation of each conversation decision | Accepted |
| 0042 | The credential is consumed inside the Secure Plane, not by the runner | Accepted |
| 0043 | A credential field is mapped to the Secure Plane, not to data | Accepted |
| 0044 | The confirmed profile has its own store | Accepted |
| 0045 | The Automation Runner pulls leased work; nothing calls into it | Accepted · §4 amended by 0054 |
| 0046 | A fill plan crosses as value and provenance, reassembled through the one mint | Accepted |
| 0047 | Page progress lives in the intent ledger; a lease names the page it holds | Accepted · §1 amended by 0051 |
| 0048 | A specialist resolution completes an intent | Accepted |
| 0049 | The run driver drives the case state machine | Accepted · three states removed by 0058; §5 completed by 0059 |
| 0050 | The account lifecycle completes through the student's own decision | Accepted |
| 0051 | The student answers in the conversation, and a correction can reach the portal | Accepted · completed by 0062 |
| 0052 | The system acts when nobody is watching | Accepted |
| 0053 | A student can stop | Accepted |
| 0054 | The intent is durable before the action, not after it | Accepted |
| 0055 | A process refuses to start when it is not safe | Accepted |
| 0056 | Verification is established at login, not re-read at every step | Accepted |
| 0057 | An approval binds to content, not to what the content says about itself | Accepted |
| 0058 | A case opens from an offer the student accepted | Accepted |
| 0059 | The student can read what they are authorising | Accepted |
| 0060 | The Conversation Service owns the student surface | Accepted |
| 0061 | The run says what it is waiting for | Accepted |
| 0062 | The question the run is waiting on is in the log | Accepted |
| 0063 | The published contract names the routes that exist | Accepted |
| 0064 | The interview's decision to stop reaches the system | Accepted · §2 corrected by 0065 |
| 0065 | A run only a person can carry on stops, and says so | Accepted · §6 corrected by 0066 |
| 0066 | Three declarations name a document, and one of them decides | Accepted · §6.1 answered by 0067 |
| 0067 | AAS obtains documents; what blocks it is policy, not design | Accepted |
| 0068 | The storage boundary refuses what ADR-0022 says it refuses | Accepted |
| 0069 | An authorisation is spendable only in the application it names | Accepted |
| 0070 | The portal's file field is called `fieldRef` | Accepted |
| 0071 | A stopped run reaches a person, and the notice carries nothing about the student | Accepted |
| 0072 | A decision is enforced where it is made, and a demonstration that cannot fail is not evidence | Accepted · amends 0005 and 0008 |
| 0073 | A declared capability with no production caller fails the build | Accepted |
| 0074 | A run a person is holding is returned to the student, never restarted | Accepted |
| 0075 | A refusal reaches the person it is for | Accepted |
| 0076 | The student can instruct the second attempt the system refuses them into | Accepted |
| 0077 | Two determinations, made structural rather than written down | Accepted |
| 0078 | Documents are held and reused, and the twelve periods are set | Accepted |
| 0079 | A document running out is the student's choice, once, in writing | Accepted |
| 0080 | The visa path is a compliance boundary, not a scheduling gap | Accepted · amends 0021 |
| 0081 | The browser tests run in a lane of their own | Accepted |
| 0082 | The record of what cannot be reached is checked too | Accepted · completes 0073 |
| 0083 | An ADR and the lists of it must agree | Accepted · continues 0082 |
| 0084 | The census is generated, and its arithmetic is checked | Accepted · continues 0083 |
| 0085 | A published demonstration is guarded on what it shows | Accepted · completes 0072 |
| 0086 | The research build is removed, and what it proved is kept | Accepted |
| 0087 | The four lawful-basis determinations, and the condition a national ID needs | Accepted · answers 0022 |
| 0088 | The Schedule 1 document must exist before the processing, and a decision not to determine is a decision | Accepted · completes 0087 |
| 0089 | A national ID leaves the supported document types, and its gates leave with it | Accepted · supersedes 0088 §1 |
| 0090 | The document transport: the gates run before a byte is accepted | Accepted · answers 0067's B4 |
| 0091 | robots.txt is read, obeyed and kept; and requests are paced | Accepted · conditions 0014 |
| 0092 | The document never enters a process we run | Accepted · continues 0090, on two conditions |
| 0093 | An upload URL cannot be minted unbound | Accepted · continues 0092, completes 0090 |
| 0094 | Document metadata is durable, and the transport starts in production | Accepted · continues 0093 |
| 0095 | The student's page makes the PUT, and the CORS rule is exercised by the PUT it makes | Accepted · continues 0094 and 0092 |
| 0096 | Expired document intakes are swept by the worker | Accepted · continues 0094, extends 0052 |
| 0097 | The preview names what the student holds | Accepted · continues 0095 and 0096 |
| 0098 | One yes over a preview that names each attachment, and the gates refuse in a closed set | Accepted · Vahid's decisions · continues 0097 |
| 0099 | Uploads cross as references, and the plane hands a document over only after the gates | Accepted · continues 0098, amends 0046 |
| 0100 | The page decides whether it can show the secure step before it asks for the capability | Accepted · continues 0086, amends 0086 |
| 0101 | The yes comes first, and a runner is signed in for one sitting | Accepted · decides blocker 19, amends 0045, 0049 and 0050 |
| 0102 | Use the refusal the form offers | Accepted · decides blocker 20, continues 0077, 0059 and 0043, amends 0077 |
| 0105 | A slot's companion is handed to the student with its slot; a list's options may be loaded by a press that loads options and nothing else | Accepted · both decided by Vahid, 2026-09-11; the press's three guards and the limit stated before building; built in P100 |
| 0106 | A page is saved when the portal shows it, not when a control was pressed | Accepted · decides blocker 22; decided by Vahid, 2026-09-12; built in P105 |
| 0107 | A handed slot's companion says "later": a statement about when, never a claim about the document | Accepted · decides blocker 23 (A); decided by Vahid, 2026-09-12; amends 0105's companion half; built in P106 |
| 0108 | What the student owes the portal is a record on the case, closed by their word, and nobody claims to chase them | Accepted · decides blocker 24 (A); decided by Vahid, 2026-09-12; built in P107 |
| 0109 | A typeahead mapping names the value the form submits; the reviewer records the text; both must match at the fill; the preview shows the text; the escape is named by value and never chosen | Accepted · decides the value-versus-text question; decided by Vahid, 2026-09-13; built in P118 |
| 0115 | The profile holds a student's residence over time, their UK status claims, and their previous UK study, each as facts the student states | Accepted · decides distance item 3's second half, three groups of four; decided by Vahid, 2026-09-15; the shapes confirmed by him with one change (the entry date as month and year); built in P139 |
| 0116 | A cancelled secure box is the student's stop: the run waits where it was until they carry on | Accepted · decides blocker 28; decided by Vahid, 2026-09-15; built in P138 |
| 0121 | A seeded value says it was seeded: the provenance vocabulary carries the true word | Accepted · amends the provenance vocabulary of 0007's confirmation model; decided by Vahid, 2026-09-16; built in P151 |
| 0133 | A typeahead is typed into key by key, and a box that finds nothing says what the field behind the earlier control holds | Accepted, **Context amended in P183** — Sheffield ships Tom Select 2.3.1, so the 1.x reasoning is withdrawn and blocker 49 is reopened; the decision stands and the typing is not reverted · continues 0103 gap 2 and 0109; found by Vahid's hand measurement on the live form, 2026-09-21; built in P180 |
| 0134 | A guard runs where the fill runs, and labels what it records instead of guessing at it | Accepted · restores 0014 and 0091 on the path 0046 §6 and 0101 §2 make production take; closes blockers 51 and 52, raises 53; decided by Vahid in his own words, 2026-09-21; built in P182 |
| 0135 | The widget you just used takes the focus back, so the runner waits for the page to stop moving and verifies what the box took | Accepted · **closes blocker 49** (seven attempts) and blocker 55; corrects 0133's account of the cause; found by measurement on the portal's own code in P184, 2026-09-22 |
| 0136 | An option map is checked against the list that was captured, by the build | Accepted · found by attempt 8; corrects 0112's build note, which wrote an assumption about a list as a fact; the question 0102 asks of a refusal, asked of the maps that carry the student's answers; VOIDS the Run A entry's signature; built in P185, 2026-09-22 |
| 0137 | A refusal in the runner's own words may be said; a portal's words about the student's answer still may not | Accepted · replaces the P178 boundary (drift only) with a closed list of errors whose words are ours; adds `ControlNotActionableError` and a five-second act bound; asked for by Vahid after attempt 9; built in P186, 2026-09-22 |
| 0138 | A document slot carries the condition the form shows it by, in the student's own terms | Accepted, one Consequences claim corrected 2026-09-22 (P189) — the hash it named as awaiting a signature was never signed; the decision stands · decides blocker 57, **B**, decided by Vahid, 2026-09-22 · `RequiredDocument.askedWhen` reads the repeating page's own entry — the qualification's `end.kind` — so the two pre-completion slots and their companions are not planned, previewed or set for a qualification that has ended; the known divergence (a completed qualification awarded in the future) named and accepted; built in P187, 2026-09-22; signed together with ADR-0139 on 2026-09-22 (`eb4e83e`) |
| 0139 | A document is named to a student by the page's own heading, never by the portal's field name | Accepted · found by Vahid reading Run A's own preview the day P187 produced it · `RequiredDocument.title` carries the page's heading, read off the companion row's captured label and held to it by the build (whole-word prefix, and the quotation must BE that label); the two sentences a student authorises say it, and where no capture names a slot they say so rather than printing a field name; completes ADR-0059 on the line it was broken on; built in P188, 2026-09-22 |
| 0140 | A field with several parts is asked part by part, and confirmed once | Accepted · decided by Vahid, 2026-09-23, in his own words — *"a value the student did not state is never supplied by us, however obvious the default looks from where we sit"*, given as a standing rule for every remaining parser · `FieldSpec` becomes a union: a composite is ordered `FieldPart`s plus an `assemble`, each part its own question with its own reason and parser, inapplicable parts skipped (ADR-0117's `none` arm), an optional part answerable with *there is none* and held as `OMITTED`, attempts counted per QUESTION, and ONE confirmation against the whole value · the five guardian fields built inside the phase at his instruction, with what a mandatory-review category means for the wording said plainly · the run driver STOPS rather than asking a part whose answer the conversation log cannot carry (blocker 60, measured) · built in P192 |
| 0132 | The uniqueness that protects a specialist's queue must not outlive the resolution — one OPEN intervention per stuck action, and the missing caller for the runs it already stranded | Accepted · corrects the constraint 0048 wrote in P10; the repair is the same shape as 0126; closes blocker 48; found by Vahid on Run A, 2026-09-21; built in P177 |
| 0131 | A consent notice on a portal is answered only with the student's own choice — asked in the notice's words, recorded per portal, visible and changeable, never decided for them; the fixed minimal choice refused with its reason | Accepted · decides blocker 47 with 0130; continues 0108 and 0110; decided by Vahid, 2026-09-18; built in P165 on the fixture; amended in P166 — a control with no words is never a choice; amended in P167 — the limit of option 2 and the condition for a named path; amended in P168 — the panel refuses a named path; amended in P170 — shape 3 built: a fixed path of controls with words, and the portal's record read back and checked; amended in P172 — a multi-press path must say what it means |
| 0130 | The point is read at every press, not only a failed one — as the login page opens, just before the press, and at a failure; structure only; the failure line unchanged; nothing in the runner's behaviour changed | Accepted · follows 0129; keeps 0124 and 0127 whole; first of two on blocker 47; decided by Vahid, 2026-09-18; built in P164 |
| 0129 | The runner reads the point at the moment the press fails — structure, no text — and names which check was pending; the picture waits; the reader's frame check fixed | Accepted · follows 0128; keeps 0124 and 0127 whole; decided by Vahid, 2026-09-18; built in P162 |
| 0128 | Read the page the runner sees before anything learns to push past it: `--as-runner`, `--covering`, no dismisser | Accepted · follows 0127; found by Run A's attempt 2; decided by Vahid, 2026-09-18; built in P161 |
| 0127 | The submit is two waits with two names, because the one wait did not do what it read as doing: the press at 15 s, the portal's answer at 30 s, the landing by URL, no screenshot | Accepted · completes 0124 at the submit; amends 0120's sign-in path; found by Run A's repeat; decided by Vahid, 2026-09-18; built in P160 |
| 0126 | A stop recorded before the fix needs a caller of its own: the repair, scoped to `WINDING_DOWN` and refused everywhere else | Accepted · completes 0125 for cases that stopped before it; found by Vahid pulling P158; decided by Vahid, 2026-09-18; built in P159 |
| 0125 | The stop finishes its own job: a cancellation concludes at the stop when nothing is outstanding, and the message says plainly when it is not finished | Accepted · corrects the reachability of 0053's second act; keeps 0065 whole; raises blocker 43; found by Vahid walking the failure path in Run A; decided by Vahid, 2026-09-18; built in P158 |
| 0124 | The runner says what it did, in words it is allowed to say: the outcome of every turn, the start of every sign-in attempt, a thrown error through a closed vocabulary; both attempts' codes reach the person | Accepted · closes blocker 36; completes 0120; found by Run A; decided by Vahid, 2026-09-18; built in P157; amended in P163 — the same lines for the fill, the count named as failures in an episode |
| 0123 | A star is not a requirement: the validator reads the section fact the plan reads; a step that cannot ask for what it needs stops for a person and says so | Accepted · amends 0119, extends 0065; closes blockers 34 and 35; found by Run A; decided by Vahid, 2026-09-17; built in P156 |
| 0122 | A failed page fill is tried twice, then stops for a person; the student is told which page, which attempt and what the page did; the ledger remembers what each attempt failed with | Accepted · closes blocker 32; applies 0114's rule to `execute`; decided by Vahid, 2026-09-17; built in P155 |
| 0120 | A failed sign-in is tried twice, then stops for a person; the student is told which attempt failed, and told when a wrong password cannot be told from a portal fault | Accepted · closes the sign-in half of blocker 26; applies 0114 to 0101 §3; decided by Vahid, 2026-09-16; built in P151 |
| 0119 | A box handed to the student is their own act on a page we still fill; the record distinguishes filled, handed and never mapped; a list longer than the form stops the fill by name | Accepted · extends 0104 and 0108; closes blocker 29; records the cut of 2026-09-16; decided by Vahid, 2026-09-16; built in P145 |
| 0118 | The mapping review is one signature, Vahid's, until a developer takes the system over; a single signature admits the signer's own account and nothing else | Accepted · amends 0017 §1 and 0057 property 4; changes blocker 2 without closing it; decided by Vahid, 2026-09-16; built in P144 |
| 0117 | The passport is one value, held or stated as none; a portal's words for the absence live in the reviewed mapping, never in the profile | Accepted · decides distance item 3's group 4 (A); decided by Vahid, 2026-09-15; built in P141 |
| 0114 | A failed account creation is offered to the student again once, and stops for a person on the second failure; the student is always told | Accepted · decides blocker 26 (C, two); decided by Vahid, 2026-09-15; built in P137 |
| 0113 | The interview collects a list entry by entry; a pasted CV is never the source of a profile value | Accepted · decided by Vahid, 2026-09-15; shapes the Run B interview build; not built, at his word |
| 0112 | A qualification has dates: a start and an end as month and year, and an award date held on its own | Accepted · decides blocker 27; decided by Vahid, 2026-09-14; the shape confirmed by him 2026-09-15; built in P134 |
| 0111 | Employment history is a registry group of its own; a page of jobs repeats over it; a student with none sees that said plainly | Accepted · decides distance item 2; decided by Vahid, 2026-09-14; the shape confirmed by him the same day; built in P129 |
| 0110 | A run may start on an account the student already holds; the first live run enters Vahid's own account, and never a synthetic applicant | Accepted · decides distance item 8 and blocker 4; decided by Vahid, 2026-09-14; built in P122 |
| 0104 | A repeating page's documents are the student's own act, said under each entry; a condition inside a repeat is answered per item | Accepted · blocker 21 decided by Vahid, 2026-09-11 (B, with the per-item condition; A an option, not the end state); built in P98 |
| 0103 | The blueprint says what the form does between fields | Accepted · the four schema gaps under Vahid's "Take the four schema gaps"; gap 4 built, 1–3 to follow; continues 0017, 0069 and 0102 |

**On ADRs 0001–0004, which this document called Proposed until P49:** they were **accepted on
2026-08-26**, with Phase 0, and every word written here about their being unaccepted was wrong.

The history is exact. At 08:05 `4ee6b1c` created five ADR files, all *"Proposed · awaiting Vahid's
approval"*, and an index saying the same. At 08:47 `a27cb60` carried the message *"Phase 0 approved
by Vahid on 2026-08-26. ADRs 0001-0005 moved to Accepted"* and flipped all five **files** — without
touching the index. At 09:02 `8786fff` edited the index and moved **only 0005**'s row. Four rows were
left behind, and six weeks and seventy-eight commits carried them forward into this document, into
§6's blocker list, and into a recommendation that Vahid decide something he had already decided.

0001 and 0002 remain accepted decisions describing an integration that has not been built, which is
a different thing from an unaccepted decision and is recorded as blocker 10. `scripts/adr-status-agrees.test.ts`
now holds all three records — the ADR files, the index, and §3 above — to each other.

---

## 4 · Live · stubbed · declared-but-unreachable · not built

### ✅ Live and working (against fixtures and replays, with real PostgreSQL and Redis)

- The whole student journey: conversation → target offer → explicit request → case opens → interview
  → plan → validate → preview → authorise → account creation → multi-page fill → **stop before
  submission**.
- Real account creation on a fixture portal, proved by asking the portal whether the student's
  password logs them in.
- The two-origin secure credential path in a real browser — the real student page mounting the
  real frame from the secure origin, the password typed into it — with every HTTP body on every
  wire, the browser's included, scanned for the password (P67).
- Durable runs across a genuine process restart; competing runner supervisors; lapsing leases.
- Read-only portal discovery, structurally incapable of submitting — **103 real runs executed**.
- OIDC login against a real local provider.
- The catalogue: content-hash-bound approvals, and processes that refuse to start on an unapproved
  entry.
- The transmission gate: five refusals, all tested, all reachable from the executor.

### 🟡 Built, but never run against anything real

- **Everything portal-facing.** The blueprint is a fixture; a real portal has never been filled.
- **The Bedrock adapter.** Complete, behind the LLM port. No credentials, and **no model chosen** —
  `pnpm run verify-bedrock` is written to read what an account can actually use rather than guess.
- **`packages/notify` — the specialist notice and its webhook.** Reachable, and it runs the moment
  `AAS_SPECIALIST_WEBHOOK_URL` is set on the worker. It is a configuration away, not a decision away,
  which is why P48 moved it out of the unreachable section it had been listed in while its own text
  said it was reachable. No URL has ever been set, so no notice has ever been delivered.

### ⚠️ Declared but unreachable — deliberately, and each for a stated reason

**Two lists live here, and P48 separated them because they had drifted.** The first is the register
the build enforces; the second is this document's wider notes at a granularity the register does not
work at. They used to be one table, and `checkMinorGate` — the minors gate — was in the register and
in *no* row of the table, while `packages/notify` sat under this heading with a cell that began
"Reachable". `scripts/unreachable-is-documented.test.ts` now reconciles the first table against
`scripts/check-reachability.ts` in both directions, so neither can move without the other.

#### A · The enforced register — `pnpm run reachability` fails if any of these acquires a caller

**Four** capabilities, each named by a decision, each with no caller inside any deployable's
dependency closure. It was seven until P57: `assertStorable` left this table when the document
transport gave it a production caller (ADR-0090), the first entry to move out. `authoriseDisclosure`
left it in P66 (ADR-0099), when the plane's document hand-over gave it one — and `fillApplication`
entered, because building that hand-over measured that the runner's entry point performed
`create_account` only (blocker 19). It left again in P71 (ADR-0101 §2), when the entry point began
performing `execute` in the session the account was created in. `attach_document` left in P73
(ADR-0069's third layer), when the Run Driver began opening one intent per document attached. The
reason and what would close each remaining entry are the register's own words.

| Capability | Record | Why it cannot be reached | What closes it |
|---|---|---|---|
| `blocksApplication` | ADR-0021, ADR-0080 | Nothing in production carries a `Requirement`, so there is no scope for it to read — the visa journey is absent rather than excluded | The Requirements Service phase, or anything else putting a scoped `Requirement` on a production path |
| `checkMinorGate` | ADR-0011 | Its one BLOCKING condition is at the submission stage, and submission is out of scope (ADR-0014). The trigger that stops a case for review is a different thing and *is* reachable: `suggestsMinority` | The phase that brings submission into scope |
| `purgeContents` | ADR-0010, ADR-0023 | B1 is decided; what is missing is a vault holding something to purge | The transport phase, and the job that calls this when a period elapses |
| `assessUsability` | ADR-0009 | Nothing feeds it; requirements come from the reviewed catalogue | The Requirements Service phase, if the KB workflow is ever wired |

#### B · Unreachable in ways the register does not track

The register asks about **one symbol** and answers with **one caller**. These are packages, branches
and builds — real, and not expressible as that question, so they are recorded here rather than given
a register entry that would have to lie about its granularity.

| Thing | Why it cannot be reached | Why it is kept |
|---|---|---|
| `packages/documents` — the vault, the full lifecycle, the validity and expiry engines | No transport exists by which a student can supply bytes, and no deployable holds a vault. Its two gates *are* register entries (A, above); the package around them is not | The constraint ships before the thing it constrains (ADR-0019). It refuses correctly today |
| `packages/extraction` — reading a document with grounded quotation | Same: nothing to read. It is in no deployable's closure, so ADR-0077's special-category guarantee constrains code that does not yet run | Same |
| `packages/requirements` — provenance and the evidence bar | Nothing feeds it, and it has no dependents at all. Two of its symbols are register entries; the package is not | It is the shape ADR-0009 requires when a source exists |
| `recommendWait`'s `next_intake` branch, and `WaitRecommendation.suggestedIntake` | A **branch**, not a symbol: `recommendWait` itself is *enforced* in the register. The catalogue port resolves a blueprint by id and cannot list, so it cannot know a later intake is open | The domain rule is ADR-0006's, and it becomes reachable the day a listing can answer the question. Exercised in the walkthrough |
| The interview's `request_document` capability | `nextAction` asks fields before documents, and the orchestrator only enters the interview while a field is outstanding — mutually exclusive by construction | Asserted rather than deleted |

**`packages/notify` is not on either list.** It used to be, and its own cell said "Reachable", which
is what P48 went looking at. Set `AAS_SPECIALIST_WEBHOOK_URL` on the worker and the specialist notice
runs; it is a configuration away, not a decision away, and it is listed under *Built, but never run
against anything real* where that is true.


### ❌ Not built at all

- **Submission.** Out of scope by ADR-0014, and structural — the runner's click guard admits exactly
  the locators it is given, and it is never given a submit control.
- **The document transport's tail.** The transport exists end to end (ADR-0090, ADR-0092, ADR-0093,
  ADR-0094, ADR-0095): the page hashes, declares, PUTs to the bucket and confirms; intakes and
  records are in the database; the S3 vault and the production wiring exist. What does not exist is
  what follows a held document: the retention sweep that calls `purgeContents`, the runner's fetch
  of a retrieval URL (which needs `attach_document` reachable — blocker 9), and a run that notices
  what the student has sent (`request_document` is unreachable through the driver, ADR-0064 §4).
  (This bullet said "Blocked on B5" until 2026-09-10; B5 was decided on 2026-09-07 and had stopped
  being the reason long before the transport was built.)
- **AWS infrastructure beyond the vault.** No RDS, no ElastiCache, no compute; nothing is deployed.
  What exists, created by Vahid on 2026-09-09: the bucket `askimate-aas-vault-4471` in `eu-west-2`,
  the customer-managed key, and the (revoked) verification role — ADR-0092 §4.
- **The real AskiMate integration.** Blocked on access to the production source.
- **Any real student data path.** Fixtures and synthetic data only, by standing instruction.

---

## 5 · Deviations from the original brief, and why

| # | Deviation | Why |
|---|---|---|
| 1 | **"Submit the application" became "fill and stop before submission"** | ADR-0014. A system that can submit can submit by accident. Submission is deferred until a controlled live run exists, and the incapacity is structural rather than a flag |
| 2 | **The student never types a password into AAS's own origin** | ADR-0030/0042. A separate Secure Plane on its own origin, and a Fill Agent that types the credential into the runner's browser over CDP. The runner — the component most likely to be compromised — never holds it |
| 3 | **The interview lives in the conversation, not in a form** | ADR-0007, narrowed by ADR-0051. The brief allowed a form; a form would have made the student do the data entry the product exists to remove |
| 4 | **A case opens from an accepted *offer*, not from a `blueprintId`** | ADR-0058. An identifier is not something a student can consent to, and receiving one proves nothing about what they were shown |
| 5 | **Mapping is reviewed data, signed, not model output — and a single signature admits one account** | ADR-0017 + ADR-0057 + ADR-0118. The model proposes; a signed artefact decides. The approval binds to a content hash rather than to a claim in the artefact; an approval signed by its author names the one account it admits, and the service refuses everyone else |
| 6 | **Requirements are curated by a human specialist through the AskiMate KB workflow** | ADR-0019. The brief implied AAS would derive requirements. A requirement that blocks an application needs provenance a scraper cannot supply |
| 7 | **Documents: the boundary was built before the feature** | ADR-0010/0022/0023/0068. Retention and lawful-basis gates ship and refuse *today*, while the vault they guard is reachable from nothing. Deliberate inversion of the usual order |
| 8 | **Financial evidence is out of scope for the first application** | ADR-0021. It is a *visa* requirement, not a university application requirement, and conflating them would have pulled the whole visa surface into scope |
| 9 | **Five deployables, not one service** | ADR-0037/0052. The brief did not specify topology. Trust levels forced the split: the process that sees a password and the process that loads untrusted pages must not be the same one |
| 10 | **The student client lives in the service that serves its origin** | ADR-0060. ADR-0039 had answered this for services and left it open for the client; a separately hosted client becomes a second source of workflow truth |
| 11 | **`main` is the trunk; no agent attribution in commits** | ADR-0029 §9 + `CLAUDE.md`. Your decision, 2026-08-31 |

**No deviation was made to work around a blocker.** Where something blocked, it was recorded as
blocking — which is why twelve retention questions and two lawful-basis determinations are still
open rather than quietly answered.

---

## 6 · Open blockers and decisions, ordered by what they hold up

| | Blocker | Owner | Holds up |
|---|---|---|---|
| **60** | **The conversation log carries one event per FIELD, and none for a PART, so the run driver cannot walk a composite.** Found by measurement in P192, through the real driver: an entry whose required `email` field was repointed to `contact.address` asked *"the first line of your address"*, took the answer, dropped it with the request, and stayed `running` — because `interviewFrom` rebuilds the interview from the log on every request and `InterviewState.partial` is not in it. **Not hypothetical:** `contact.address` and `identity.passport` are both named by the Sheffield draft mapping set. P192 **stops the run instead** — the part question is not put at all and a person is told, because a silent loop is worse than a clean stop (ADR-0140 §7). Closing it needs a part-scoped event on the log (`value_proposed` for `contact.address#line1`, or a new kind), `openProposal` and `attemptsFrom` taught to read it, and a migration if the event's `field_key` is constrained | Me | `identity.passport`, `contact.address` and the five guardian fields reaching a real student through the service. The interview package itself is complete and tested |
| **61** | **There is no reviewed country table, so `Address.countryCode` asks the student for an ISO code.** `countryCodeIso2` refuses *"Iran"*: turning a country name into `IR` is a lookup, and Vahid's rule is that *"a value the student did not state is never supplied by us"*. A half-table would work for some students and fail invisibly for others. So the question asks for the two-letter code and says why — which a student may well not know. The same gap sits under the three free-text country fields (`identity.nationality`, `identity.country_of_birth`, `residence.country`), which store whatever was typed and will need the same table when a portal's option list has to be matched | Me | A student completing an address without looking up their own country's code. Not blocking the phase; blocking a good conversation |
| **62** | **The guardian's consent is the minor route's lawful basis, and nothing in this system reaches the guardian.** `MINOR_ROUTE` (B2, determined by Vahid 2026-09-08) rests on *"Article 6(1)(a), consent, given by the parent or guardian"*. P192 built the five guardian questions, and they ask **the student** for a third party's name, e-mail and phone. The guardian is not in the conversation, has consented to nothing, and cannot be asked through it. The mandatory review (brief §2.5) is where a person would notice — but a review is not a consent mechanism. **Raised, not decided:** what the guardian is told, by whom, and at what point, is Vahid's call | Vahid | Any real minor's case. The questions exist and the stop exists; the consent path does not |
| **63** | **`Address.postalCode` is required in the registry, and a good many countries issue no postcode.** A student in one of them cannot complete an address: the part is asked, nothing readable can be given, and after three attempts the run escalates. Found while writing the address parts in P192 and raised rather than worked around — making the part optional in the interview while the registry says required would put an `Address` into the profile that does not satisfy its own type | Me | Students in countries without postal codes. A registry change, not a spec change |
| **58** | **Sheffield may have an AGENT ROUTE, and if it does it is a different product — probably a better one.** Raised by Vahid, 2026-09-22: *"we would be submitting as Universitio on the student's behalf rather than driving the student's own account."* Sheet: [`the-agent-portal-possibility.md`](./the-agent-portal-possibility.md). It would **delete** the held-credential question (the credential becomes ours, an operational secret), **delete** account creation for the student, and make submission honest by construction — an agent portal exists so a named agent can submit for an applicant. The machinery transfers: discovery, blueprint, mapping, preview, hash, approval and the runner are portal-agnostic; only the entry is new. What gets harder is not engineering — an agency agreement with the university, a fresh controller/processor analysis, and possibly worse student visibility. **If the route has structured intake rather than a portal, the browser runner is unnecessary on it**, and all ten of Run A's lost attempts were browser problems | Vahid — his read, in progress | Items 2, 4 and 5, all unstarted. Not item 1 | 
| **59** | **An own act discovered DURING a run, which blocks the run, has no shape.** Raised 2026-09-22 answering his question about e-mail verification at account creation. **ADR-0108's machinery runs but does not fit, in three places.** (1) `OwnActRecorded` is shaped for a document slot — `key` is *"the slot's field reference, and the entry when the page repeats"*, with `page`, `entry` and `told`; a verification link has none of those. (2) **The timing is wrong**: own acts are appended *"with the yes, from the preview the student authorised, which is the one thing the hash binds"*, and verification is discovered after the account is created, in no preview. (3) **The semantics differ in the way that matters**: an unattached document leaves the application incomplete and the case concludes anyway — ADR-0108 says nothing chases them — whereas an unverified address **blocks the run**: no sign-in, no fill. That is a stop the run waits on, and the vocabulary exists — the intervention path (ADR-0065, ADR-0050), the CAPTCHA and second-factor stops (P70), and `awaiting_email_verification` already in `packages/account/src/ownership.ts`. **So: a new kind, small, in the STOP family rather than the own-act family** — park, tell the student in chat what to open and what the portal's page said, take their word as a resume decision, and re-check by attempting the sign-in rather than by reading anything. ADR-0020 §5 stays whole: we never touch the mailbox. **Depends on blocker 37** — a sign-in stop can be raised but never resolved — or it raises a stop nobody can clear | Vahid, after his 0a read | Item 4 |
| **1** | ~~**Real portal discovery**~~ — a scoped, read-only run against the University of Sheffield's public PGT application pages (*"Sheffield target confirmed"*, Vahid, 2026-09-10; `targets/sheffield-pgt-2026-09.json`, course and intake supplied 2026-09-11, not run as a crawl — read through attached inspection, P81) | Closed | *Everything portal-facing.* Nothing downstream is real until this exists. The earlier target (Ulster Birmingham / QA HE): 103 discovery runs saw zero file inputs because the application is behind a login. **Closed by Run A, 2026-09-22.** Not merely discovered: attempt 10 signed in through the consent path and filled, saved and read back six pages of Part 1 on Vahid's own account, under the fifth signature, stopping at `ready_to_submit` with nothing submitted. Everything downstream that this row said was not real until it existed, now is — see [`what-run-a-proved.md`](./what-run-a-proved.md), including what it did NOT prove |
| **2** | **The review of the blueprint and the mapping set — changed by Vahid, 2026-09-16 (ADR-0118), and not closed.** *"Drop it to one: I approve, and I am the only signature."* One signature, his, in `approvals.json` with his own `studentId` under `ownAccountOnly`; the service then serves the entry to his account and to nobody else, enforced at the listing, the offer, the start and every later lookup. **What that leaves uncaught:** a field mapped to a plausible wrong source, which every test passes and no gate refuses. **A second reviewer is a precondition of serving a real student, not an improvement** — his words; the developer's list: [`what-was-skipped-to-get-it-standing.md`](./what-was-skipped-to-get-it-standing.md) | You sign, for your own account; a second person before any real student | Any real fill for anyone but his own account. `checkExecutable` refuses a draft; `checkUsable` refuses an unreviewed mapping set; the loader refuses an unbounded self-signed approval and carries a bounded one's admission onto the entry |
| **3** | **Bedrock credentials**, then four model IDs | You, then me | The interview, interpretation, extraction and navigation workloads. The adapter is built and idle — and **not wired**: `wiring.ts` constructs `DeterministicModelClient` in every path, so the service has no code that builds a Bedrock client with or without credentials (found P79). Credentials are necessary and not sufficient |
| **4** | ~~**An account**~~ — **decided by Vahid, 2026-09-14 (ADR-0110), built in P122:** the first live run enters his own existing account; a synthetic applicant on the real form is refused; the start path onto an existing account exists and is proved through the five processes **Exercised end to end on 2026-09-22:** Run A entered his own existing account through the consent path, filled Part 1 and handed it back. The mixture ADR-0110 accepted is now real on that account — Niloofar Hosseini's name, date of birth, nationality, education and employment beside his own title, gender and permanent address — and is recorded, not reversed ([`what-run-a-proved.md`](./what-run-a-proved.md) §4) | Closed | — |
| **5** | ~~**B5 — hold or pass through**~~ | — | **Decided A — hold and reuse, 2026-09-07 (ADR-0078).** Documents are stored and reused; a student is never asked for the same document twice |
| **6** | ~~**B1 — twelve retention determinations**~~ | — | **All twelve answered, 2026-09-07 (ADR-0078).** Eleven periods set in schedule `1.2026-09-07`; row 12 (`bank_statement`) stays unresolved and blocking by ADR-0021 |
| **7** | ~~**B2 — the ADR-0022 lawful basis**~~ | — | **Answered 2026-09-08 by Vahid Mohammadi (ADR-0087).** Four determinations, review 2027-09-08. Ten of seventy (type, purpose) pairs now pass both storage gates. The one pair ADR-0087 left open — `other / audit_evidence` — was **decided-refused** on the same day (ADR-0088). The vault still does not open — what remains is transport, an implementation and a deployable, none of which is a decision |
| **8** | **DPA 2018 Sch. 1 appropriate policy document** | The DPIA owner | **Nothing today — and it is a live constraint, not a closed one.** `national_id` was the only special-category document type in scope and it was removed in ADR-0089, so no processing currently needs this. It binds again the moment one is added: **the document must exist BEFORE that processing**, which is why ADR-0089 puts the requirement at the `DocumentType` union itself and not only in an ADR |
| **16** | ~~**Billing alerts, one S3 bucket, the vault's CMK and a prefix-scoped credential, to verify the checksum binding AND SSE-KMS through a pre-signed PUT**~~ | — | **Created by you and run on 2026-09-09; VERIFIED on both halves (ADR-0092 §4, *Run 2026-09-09*).** The first run said REFUTED because the SDK hoisted the checksum into the query string, where S3 never reads it — your reading: *"the run refuted the property under the SDK's default presign, not the property itself."* The second run, with the checksum a signed header, established all three things you asked for: a mismatched body is refused (400 `BadDigest`), an uploader who omits or alters the header is refused (403 `SignatureDoesNotMatch`), and SSE-KMS holds with the binding in place. **The port is still untouched** — the reshaping starts on your word, and carries the constraint that the checksum is a signed header, never a query parameter |
| **17** | **The vault's service role, the bucket's CORS rule and its lifecycle** | You — AWS spend is your act | **The transport in production (ADR-0094).** The service starts with the transport once four variables are set; what its role must be allowed, what CORS the page's origin needs, and what lifecycle the bucket should and should not have: `docs/provisioning-request-document-vault.md`. Nothing has been created by the agent. The first request this service makes to AWS is on your deployment |
| **18** | ~~**The gate's reason on the wire — two of your rules disagree**~~ | — | **Closed by Vahid, 2026-09-10 (ADR-0098):** *"The contract's Problem stays without detail. Close the gap the way P41 closed its own: a closed set of refusal codes, each with wording written for the student and covered by the wording-coverage guard."* Done in P65: three codes, words on the page, every `detail` off the wire, and a guard. He asked to be told if a gate's refusal could not be expressed as a code; all five could |
| **19** | ~~**The runner performs `create_account` only — execute work has no production performer**~~ | — | **Decided 2026-09-10 (ADR-0101).** A1 built in P69: the yes comes before the password and the account. The detection of a CAPTCHA or second factor built in P70; A2 (one sitting, in memory, five minutes) built in P71; B (`portal_sign_in` as the resume path only) built in P72. Open portals refused explicitly; C recorded as the plan for a second-factor portal. The row that follows is the record as it stood when it was found. **Was:** | **Found in P66 (ADR-0099).** `apps/browser-runner/src/main.ts` answers `needs_the_student` to every work kind but `create_account`. `fillApplication` — the thing that fills and saves a page — is called by `scripts/journey.test.ts` and by no deployable, and it is now in the register as declared-but-unreachable. The reason is a design nobody has made: the browser session an account was created in does not survive to the next work item, and the password that would sign in again was single-use and is gone (ADR-0042). A second gap sits behind it: for a portal with no login, execute work is never handed out at all, because `ClaimedWork` carries an account's email and approach (ADR-0045) and `accountDetail` answers null with neither. The attachment path's slices d and e wait on this; the transport, the gates and the hand-over do not. Two things need your word: whether a runner may hold a signed-in session across work items (and where its credential comes from), and whether a portal with no login is a route this product serves. **Framed as a decision sheet in P68 — [`decision-sheet-blocker-19-how-a-runner-is-signed-in.md`](./decision-sheet-blocker-19-how-a-runner-is-signed-in.md)**: five options costed, a recommendation (one signed-in sitting with authorisation moved first; a second ask through the secure box as the resume path; co-browsing deferred; a persisted cookie rejected), and four questions in §7 |
| **20** | **A field the portal asks for that our rules forbid us to hold** — Sheffield's equal-opportunities page: eleven disability checkboxes, a support-needs box, an ethnic-origin select (Article 9). Sheet: [`decision-sheet-article-9-fields-a-portal-asks-for.md`](./decision-sheet-article-9-fields-a-portal-asks-for.md) | **Decided by Vahid, 2026-09-11, and built in P86 (ADR-0102)**: the empty save is refused, so *"use the refusal the form offers"* — never presented as the student's answer — with the three conditions enforced at the mapping boundary, in the plan and in the preview. Both values confirmed *Prefer not to say* from the live dropdown, his correction recorded. The equal-opportunities mapping set and the classified draft are written for Iman Behravan's review (`review-pack.md`) | The review: 23 judgement rows, 193 mechanical, the page's two refusals |
| **22** | ~~**A page is saved when the portal shows it, not when a control is pressed**~~ — Sheet: [`decision-sheet-blocker-22-a-page-is-saved-when-the-portal-shows-it.md`](./decision-sheet-blocker-22-a-page-is-saved-when-the-portal-shows-it.md) | — | **Decided by Vahid, 2026-09-12 (ADR-0106):** *"Nine looks per application on a portal with nothing to read is the honest price and I would rather pay it than report a success we cannot see."* Built in P105: reopen-and-read, the listing count, the slot marker; `uncertain` and no transmission where none holds. Left for a later phase: the intervention's text does not yet say which field was not seen |
| **23** | ~~**A companion the page will not save without**~~ — Sheet: [`decision-sheet-blocker-23-a-companion-the-page-will-not-save-without.md`](./decision-sheet-blocker-23-a-companion-the-page-will-not-save-without.md) | — | **Decided A by Vahid, 2026-09-12 (ADR-0107):** *"'I will upload this later' is not a claim about the document, it is a statement about when."* Built in P106. On Sheffield the six defer values wait on a copy of the education fieldset, since the capture holds one radio value three times over |
| **30** | **The blueprint's condition language cannot say what Sheffield's script says.** **Vahid, 2026-09-16: *"not now. Half a day, and the case it protects is a student we are not serving yet. Keep the row and the two-line note."*** Cheapest honest version when it is taken up: the runner reads the page's own visibility at the fill and stops loudly when a required box it planned is hidden, or a visible required box was never planned. A `visibleWhen` names one controlling field with one operator; `nationality.js` decides sections by the *better of two* nationalities and by an OR across nationality and residence (P142). The draft reads the first nationality only and writes the study block's rule on the nationality alone — recorded, not approximated, in ADR-0115. **Vahid, 2026-09-15:** *"'neither case is on Run A's path' is true of Run A and not of the product. A UK national living abroad is an ordinary student, and the day one arrives the condition is silently wrong rather than loudly missing."* Options, none chosen: conditions that take several clauses (`all`/`any`); a second nationality field mapped and a *better-of* rule; or the runner reading the page's own visibility at the fill and refusing a hidden required box loudly | You — in your words; then me | Any student with a second nationality of a different class, or a UK national resident abroad, on the nationality page |
| **31** | **The runner signs in to an existing account with the profile's `contact.email`, not the account's own e-mail.** Found writing Run A's sequence (P154): `existingAccountStep` resolves the account's e-mail from `contact.email` (`packages/orchestrator/src/run.ts`), and the `existing_account` decision carries no e-mail of its own (ADR-0110). For the synthetic profile that is `niloofar.hosseini@example.com` — not the address Vahid's Sheffield account holds — so the sign-in would fail as `portal_refused` on both attempts (ADR-0120) before a page was touched. For a real student the two addresses can differ for the same reason. Options, none built: (A) for Run A only, the profile's `contact.email` is the account's address — one row re-seeded, the personal page's two e-mail boxes then carry it too; (B) the declaration carries the account's e-mail (contract, driver, orchestrator, client), the honest product shape. **Vahid, 2026-09-17: A for Run A** — *"re-seed with my account's e-mail, from the copy outside the repository"* — and B is *"the product's honest shape (the declaration carrying the account's e-mail)"*, recorded *"as the thing to build, not as a note."* The re-seed is his act on his machine (commands in the P155 report); the repository's fixture keeps the synthetic address. | Run A: Vahid (the re-seed). The product: me, B, after Run A | Run A cannot sign in until the re-seed; a real student's account e-mail is a fact about the account, not the contact address | **Run A: decided A.** Product shape B: **Open — to build** | — |
| **32** | **A page fill that fails cleanly is re-offered without limit** — the same shape as blocker 26 was for the creation and its sign-in half (ADR-0114, ADR-0120), on the third path: `execute`. A `portal_refused`, `portal_drift` or `robots_disallows` report completes the page's intent `failed_cleanly`, `#beginIntent` re-opens it (ADR-0047: *already_done + failed_cleanly is offered again*), the Worker re-offers on its next tick and the runner tries the page again, paced only by the one-second floor, until someone stops the stack or the student presses *Stop this application*. Against a live portal that is a failing save retried every few seconds. Found writing Run A's sequence (P154). The fix is ADR-0114's shape a third time: two attempts per page, then stop for a person with the page and the code on the record, the student told. **Vahid, 2026-09-17:** *"build the cap first… Two attempts then stop, same shape as 26 and 120, and the student told which attempt failed and what the page did. If a page's save failure cannot be told from a portal fault, say so rather than guessing, as 120 does."* Built in P155 as ADR-0122. | — | Run A: what happens if any page does not save; every portal | **Closed — ADR-0122, P155** | — |
| **33** | **A robots refusal on a sign-in is counted and worded as a password attempt.** Found writing the P155 report. The runner checks robots.txt before any work opens a page; a `robots_disallows` on `sign_in` reaches `#afterFailedSignIn` (ADR-0120), which counts it as an attempt made (only `secret_unavailable` is not one), reopens the password box, and says *"check it carefully, since I cannot"* — words aimed at a wrong password, not a robots rule. The reason it states is true (`failureInWords`: the portal's rules for automated visitors do not allow that page); the count and the advice are wrong for it: a page robots.txt disallows is not a chance failure and a second box changes nothing. Not on Run A's path if Sheffield's robots.txt allows the login page as it did in P122. **Vahid, 2026-09-17: *"leave it… I would rather it stayed on the list as a known small wrong thing than be fixed on the morning of the run."*** | me, after Run A | Any portal whose robots.txt disallows the login page: the student is told to retype a password that was never the problem | Open — deferred past Run A at his word | — |
| **34** | **The Run A entry does not pass validation for the profile it was signed over.** Found by Vahid at Run A step 3 (2026-09-17): the run stood at `fix content (running)` with no question and no message. Reproduced here on the signed entry with the repository's synthetic profile: `planFill` has no blockers (the P150 test checked that and nothing more) and `validatePlan` has **18 violations**, every one `required … and the plan has nothing for it`, every one on page 6, English language: `firstLanguage`, `previousEnglishEducation`, `previousEducationLanguage`, and the qualification block (`title`, the three `dateOfAward` selects, `certificateNumber`, `certificateNumber2`, `awardingBody`, the six scores, `languageCertificateStatus`, `languageCertificate`). The e-mail changes nothing (checked with a gmail-shaped address). The read he signed over says *"Left empty … the form does not require them"* and quotes the form's own sentence; the blueprint carries the page's asterisks as `required` (`observed_marker`) on all 18; the two disagree, and only the validator runs in the run. Nothing was typed into Sheffield: the run never reached authorise. **Vahid, 2026-09-17, from his own save of 16 September:** *"The eighteen are not required, and we have observed it rather than inferred it… The star is not the evidence. The save is… the section-level optional fact you built in P147 is right and should govern. If the validator is not reading it, that is the defect — not the marks and not the mapping."* The validator read the marks alone; the plan read the section fact. | — | Run A: stopped at step 3; resumes from the same conversation on the fixed validator | **Closed — ADR-0123, P156.** The validator reads the section fact; the marks untouched; no draft changed, no re-sign owed; the Run A test now runs the validator on the signed entry | — |
| **35** | **A `fix_content` step whose violations name unmapped boxes is a silent dead end.** Same finding, the mechanism: `nextStep` answers `fix_content` when the validator objects; `phaseFor` sends it to the interview surface (*"a personal statement over the limit is theirs to shorten"*); but `interviewActionOf` is `null` for `fix_content`, so nothing is asked, nothing is said, no intervention is raised, no log line is written, and the Worker re-derives the same step every tick. The student sees `fix content (running)` and a Stop button, for ever. The violations live in the step only — the checkpoint keeps position, not text — so no route and no command can show them; they can only be recomputed. Blocker 32's shape on a fourth path: a position with no way out and nobody told. **Vahid, 2026-09-17:** *"Blocker 35 is the more serious finding and I want it treated that way… a step that cannot ask for what it needs must stop for a person and say so. Build that too."* | — | Every portal: any required box the mapping set does not cover stops the run for a person, named | **Closed — ADR-0123, P156.** `contentHandoverOf` + the ADR-0065 stop: `specialist:content_rejected`, escalated, the student told once. The recompute command is not built; the Run A test compares the two readings before any run | — |
| **36** | **The sign-in record holds one failure code, not every attempt's.** Found by Vahid reading the intervention after Run A stopped at the sign-in (2026-09-18). `run_sign_in_failures` carried `last_failure`, one code per run, and the intervention named the second attempt's code and said only that the first *"also failed"*. Both of Run A's attempts were `runner_fault`, so nothing was lost this time and the conversation named the first; a sign-in that lost its connection and was then refused would have reached a person as a refusal alone. The gap ADR-0122 had already closed for a page fill (`attempt_failures` on the intent row) and this path never got. **Vahid, 2026-09-18:** *"Raise the missing first code as a blocker, and carry the first attempt's code onto the sign-in record in the same phase. Same shape as ADR-0122, and the reason is the one you gave: when the two codes differ, a person reading one of them is reading half the story."* | — | Every portal: a person adjudicating a stopped sign-in reads one code where two happened | **Closed — ADR-0124, P157.** Migration 0022 adds `attempt_failures`, appended per attempt made; the intervention names every code in order | — |
| **37** | **A sign-in stop can be raised but never resolved: the parked run cannot be released.** Found trying to resume Run A after P157 (2026-09-18) and reproduced against real Postgres. `#afterFailedSignIn` stops through `#stopForPerson` keyed on `sign_in_to_portal`, but a sign-in deliberately writes NO intent row (ADR-0101 §3: its durable trace is `run_sessions`, and a second row for one fact would be two models of one thing, ADR-0041). `resolveIntervention` completes an intent unconditionally, so it throws `No intent for …:sign_in_to_portal:…` and the run stays `escalated`. Both outcomes hit the same line, so neither *resume* nor *abandon* works. Measured: `PROBE-RESULT status=escalated threw=No intent for run_case_…:sign_in_to_portal:run_case_…`. No test covers resolving a sign-in stop; every existing resolution test resolves a page or an account, which DO have intent rows. The consequence for a person: the only way out of a stopped sign-in is the student cancelling and re-applying, which is a new case. Options, none built: (A) `resolveIntervention` completes an intent only for an action that has one, reading the same `LEDGERED_WORK` the claim and the report read; (B) a sign-in gets an intent row, which ADR-0101 §3 argued against. | Vahid, in his words; then me. A is the smaller change and keeps ADR-0101 §3 | Every portal: a sign-in that stops for a person strands its run; Run A is stranded now | **Open — found 2026-09-18, blocks resuming a stopped sign-in** | — |
| **38** | **The stop message tells a student we created an account when they declared their own.** Found by Vahid stopping Run A after the sign-in failed (2026-09-18). `cancellationMessage(entry, hasAccount)` takes ONE boolean — *is there an account on this case* — and its wording asserts *how* it came to exist: *"The account at University of Sheffield was created in your name and still exists — it is yours, and I will help you take control of it."* Since ADR-0110 an account can be on a case because the STUDENT declared one they already held, and on that path every clause is false: we created nothing, and there is nothing of ours to hand over. The condition and the sentence disagree, and the ADR-0053 comment above the function says exactly why the sentence is conditional — *"a student who stops before any account exists must not be told one does"* — which is the same rule, one case short. A student reading it would believe we made them an account. Worse than being stuck: it is a falsehood to the student about their own portal account. | Vahid, in his words; then me. The shape is a third branch, not a second boolean: created by us, declared by them, none | Every run started on an existing account (ADR-0110) that the student then stops — Run A's own path | **Open — raised 2026-09-18** | — |
| **39** | **The route back after a stop is not the one the runbook describes, and I gave it wrong twice.** Found by Vahid, 2026-09-18, following the resume steps I wrote. The re-application affordance is NOT the ordinary composer and NOT available directly after a cancel: it appears only when the student ASKS TO APPLY AGAIN from the offer and the server refuses with `already_applying` carrying `concluded: true` — the refusal that is not a dead end (P42, ADR-0006). Typing a sentence into the chat box after a stop appends a message and does nothing else, which is what a person naturally does and what the page invites. The `not_found` shown at the stop is a follow-up read of a run that has just concluded, not the cancel failing; the cancel had already recorded. Neither the runbook nor the Run A README describes this sequence, so the only place it exists is P42's test. | me — document the sequence in the runbook, and consider whether the page should offer the second attempt where the student is rather than behind a refusal they must trigger | Any student who stops and wants to try again; every operator following the runbook | **Open — raised 2026-09-18** | — |
| **40** | **A stop moves the case to `WINDING_DOWN` only; the second act that concludes it never runs for an escalated run, so the case can never conclude.** Raised 2026-09-18 and CORRECTED the same day after reproduction — the first wording of this row said the cancel "never touches `workflow_runs.status`" and called that the defect, which was wrong. The two-act design is deliberate: `decide` on `cancel_case` emits `CaseCancelled`, optionally `AuthorisationVoided`, and `CaseStateChanged → WINDING_DOWN` (machine.ts ~744-796). Reaching `CANCELLED` is a SECOND guarded transition performed by `#windDown` (run-driver.ts ~5039-5116), which runs only on an ADVANCE. The strand: the Worker advances `running` and `suspended` only — *"`uncertain` and `escalated` wait for a PERSON by design"* — so a run that stopped for a person and is then stopped by the student is never advanced, `#windDown` never runs, and the case stays at `WINDING_DOWN` for ever. **Measured here on the fixture catalogue:** a case wound down with NOTHING outstanding still sat at `WINDING_DOWN`, `concluded=false`, and the transition to `CANCELLED` was `accepted=true` — nothing was blocking it; nothing performed it. The user-facing consequence is blocker 42. | Vahid, 2026-09-18, in his words: *"Option one. The stop finishes its own job… the escalation rule was deliberate and it should stay whole."* | Every student who stops a run that had stopped for a person | **Closed — ADR-0125, P158.** `#concludeCancellation` is called by the stop as well as the advance; ADR-0065 unchanged. The second defect it uncovered — the obligations guard being unreachable through `decide` — is closed in the same phase | — |
| **41** | **The re-application panel does not open, correctly, and nothing says why.** The page shows the offer again with no message after a 409. Reproduced: the 409 carries `concluded: false` because the prior case is at `WINDING_DOWN` (blocker 40), so the client's condition is right to keep the panel shut. The defect is not the gate and not the parser: it is that a student is told nothing at all. The refusal that is *not* a dead end (P42, ADR-0006) becomes a dead end whenever the prior case cannot conclude. | me — the page should say what is outstanding, or say a person must look | Every student re-applying after a stop | **Open — raised 2026-09-18; its CAUSE is closed (blocker 40, P158), the silence is not.** A stop with nothing outstanding now concludes, so the 409 carries `concluded: true` and the panel opens; a stop with an account still outstanding correctly keeps it shut, and the student is still told nothing on that path | — |
| **42** | **A student admitted to apply once cannot apply again while the prior case is unconcluded, and the refusal is a bare 403.** Vahid, 2026-09-18, on the signed catalogue: `POST …/reapplication/prior-outcome` → 200 with advice, `POST …/reapplication` → 403 `forbidden` seconds apart on the same cookie. His hypothesis was the ADR-0118 one-signature gate. **Reproduced here on the FIXTURE catalogue with no `ownAccountOnly` approval anywhere**, changing only the prior case's state: 409 `concluded=false`, advice 200 with the identical rationale, instruct 403 byte for byte. **So it is NOT the gate.** `reapplication_refused` maps to 403 and one of its causes is *"an unconcluded prior case"*; the prior case cannot conclude because of blocker 40. The 403 names nothing, so the cause is invisible from the wire — the detail *"names a case and quotes a rule"* and is deliberately dropped because there is nowhere on the wire for a sentence. | me | Every student re-applying after a stop, on any catalogue | **Open — reproduced 2026-09-18; the gate is exonerated; its CAUSE is closed (blocker 40, P158).** A prior case that owes nothing now concludes, so this 403 no longer fires for that reason. What stays open is that the 403 names nothing: a student refused because something IS outstanding still reads a bare `forbidden`. **Walked by Vahid, 2026-09-18:** the two raw calls open a new case end to end once the prior case is concluded | — |
| **43** | **A stop that concludes leaves a specialist's intervention open on a closed case.** Found in P158 while building the stop's second act (2026-09-18), and pre-existing rather than caused by it — what changed is that the conclusion is now REACHABLE for an escalated run, so the pairing is live. Nothing in the run driver calls `interventions.resolve` on a cancellation. A person's queue therefore keeps an intervention describing work that will never happen, on an application the student has stopped and whose case is terminal; acting on it does nothing the case will accept. The obvious fix is the one this repository will not take on its own: `resolve` records a `specialistId` and a `resolvedBy`, so closing it here would write an adjudication under a person's name for an act no person performed — the class of record ADR-0082 to ADR-0084 spent their phases removing. Options, none built: (A) a lifecycle state for *withdrawn by the student*, distinct from a resolution; (B) the queue filters interventions whose case is terminal, leaving the row untouched and honest. | Vahid — A writes a new fact, B writes none | Every stopped run that a person was holding | **Open — raised 2026-09-18 in P158** | — **Seen on a real queue, 2026-09-21:** Vahid's `interventions` listing shows **two** open interventions for the two concluded cases — the defect is no longer only reasoned about, it is in front of the operator every time they look at the queue, and a real one raised beside them has to be told apart by eye. |


| **49** | ~~**The institution box on `education.do` answers with an empty list.**~~ Attempt 5 said why the list was empty — the page made no request — and **Vahid's hand measurement, 2026-09-21, found the fork**: typing `sheff` key by key into `#institution-ts-control`, with the country chosen by hand, opened the list with all eleven entries; the runner's one-act fill asked the portal nothing. (He withdrew a console test of his own that appeared to agree: his Network panel's type filter hid Fetch/XHR, so it showed nothing either way. The evidence is the runner's line.) **The cause, from Tom Select's own published source:** 1.x binds `keyup` and has NO `input` listener (`tom-select.ts:317` in 1.7.8); 2.x binds `input` instead (2.0.0 onwards). Playwright's `fill` dispatches one `input` event, which on a 1.x page fires nothing — no `load`, no `refreshOptions`. **Which version Sheffield ships is NOT established**: no capture holds that page's HTML or scripts. **And the difference he asked about:** the country box is a LOCAL list, so `onFocus` → `refreshOptions` renders its entries with no search at all and the click chooses one; the institution box is REMOTE, so nothing is in it until `load` runs. Country passed for a real reason — but nothing verified it, which was his deeper point and is now fixed. | — | — | **CLOSED — ADR-0135, P184, by measurement.** The widget the runner has just finished with takes the focus back about ninety milliseconds after its entry is clicked (`open()` → `focus()` → `setTimeout(onFocus,0)` → `openOnFocus` → `open()`), and the next box's keystrokes land in it — traced by patching `HTMLElement.prototype.focus` on the portal's own code in a fixture at its own version, driven through `attach()`. **Ours, not the portal's.** The runner now drains the focus at the end of the act that caused it, reads the box back, retypes bounded at three, and says plainly when a box did not take what was typed. Five runs in five each way. Seven attempts and four wrong explanations are recorded in ADR-0135's table |
| **50** | **The institution list's values disagree between two reads of the same box, and it is unresolved.** `0159` for *University of Sheffield* on 2026-09-11; `SHEFFIELD` on 2026-09-13, cross-checked against the `<select>` the form posts. ADR-0109 recorded the disagreement as settled — the earlier copy taken from a different box and mislabelled, *"my error … not Sheffield's instability"*. **Corrected by Vahid, 2026-09-21, against his own copy:** the markup in it is `id="institution-ts-dropdown"` with entries `institution-opt-1` to `-11`, which is this box. Same country, same typed text, identical labels in identical order, every value different, two days apart. So the risk ADR-0109 weighed — a value that moves on the portal's side breaks a mapping pinned to it — is **observed on this portal**, not merely conceivable. The date confusion is closed the other way: the copy is of the 11th (`2026-09-11_at_21_35_55`), and he corrected his own passing reference to the 14th. | Vahid — his read; the `<select>` the form posts is the drift test ADR-0109 named | Every mapping pinned to a value on this portal. Not reached by attempt 5, which never got a list | **Open — raised 2026-09-21. ADR-0109's decision stands; its Context and one consequence are amended.** The fill's behaviour is the mitigation: a value the list no longer carries is a loud refusal, never a wrong choice | — |
| **51** | ~~**The preparation network guard is not installed on the path a deployed run takes.**~~ `decidePreparationRequest`, the host allow-list, the robots check on subresources and the `WriteLog` that records *“everything preparation sent that could have changed something”* all live in the `context.route` handler inside `PlaywrightPreparationSession.open`. Production does not call `open` and cannot: ADR-0046 puts the form behind a login, so `performer.ts` attaches the fill to the context the sign-in holds, and `attach` installs no handler; `SessionHold.open` installs none either. Found in P181 by reading the code in answer to Vahid's question of 2026-09-21. **Unaffected:** the TYPE, CLICK and NAME layers, which live on the session and which `attach` carries whole — the submission guarantee is where the file always said it was. **Affected:** during a real fill nothing constrains which host the page's own scripts reach, nothing applies robots.txt to a subresource, and the run's record of what it sent is empty — `summarise()` would answer *“No state-changing requests were sent. The portal saved nothing”* after a run that saved three pages, which is why nothing production runs calls it. The build did not catch it: `check-reachability` finds a production call site for the function, and states in its own header that a function called only by another function nobody calls passes. | — | — | **Closed — ADR-0134, P182.** Installed on the context by `guardContext`, called by `attach` and `open` alike; once per context, logs on the context. `WriteLog` is armed by the guard, so an unarmed log reports *NOTHING WATCHED* and cannot say *the portal saved nothing*. Proved through `attach()` — the door production uses — by three tests that fail without it. The classification is decision 4 of ADR-0134: navigation or not, a label and never a refusal. **Carried forward as blocker 53:** `forbiddenEndpoints` is installed and has no source |
| **52** | ~~**The runner cannot see a non-GET request the page makes, nor a script error — and one failure line said more than it knew.**~~ The P179 lookup watcher records GET only (its first line returns on any other method), and there is no `pageerror`, `console`, `requestfailed` or `crash` listener anywhere in the repository. So on attempt 6 the sentence *“While this box was being filled the page made NO request of its own to the portal”* asserted the absence of exactly the class of request it cannot observe — and the one lookup on this page whose shape is on record is a POST carrying its parameters in the query string (`getGradingSystemsForCountry.do?institutionCode=&noCache=…`, the 10 September capture). If the institution search has that shape, the box may well have searched and the line was false. | — | — | **Closed — ADR-0134 §5, P182.** *“Build both… they are not a guess about the box; they are the instrument that stops the guessing”* — Vahid. The watcher records every method on the portal's own host, each labelled navigation or background; a `pageerror` listener says a script FAILED and never its text. Red first on a fixture in Sheffield's shape: a search by POST with its parameters in the query string, and a handler that throws before it fires |
| **53** | **`forbiddenEndpoints` is installed and always empty — the third layer of the preparation guard has no input.** The network guard refuses a URL under a submission endpoint the blueprint records; the blueprint's `SubmissionModel` records a page reference and a control and **no URL**, and `performer.ts` passes no list, so the find is over an empty array on every request. Found in P182 while installing the guard, and said rather than left to imply otherwise. **This is not the submission guarantee** — that rests on the type (`FillableSession` has no `submit`) and the click guard (only the plane's own controls, and never a submission-looking name), both of which `attach` carries whole and both of which are tested. | Vahid — whether a submission URL belongs on a reviewed blueprint at all | Nothing today. It is defence in depth that is absent, not a defence that has failed | **Open — raised 2026-09-21 (P182).** Two ways out: record the submission endpoint on `SubmissionModel` at discovery and carry it to the runner, which puts a URL under the reviewer's signature; or delete the layer and say the guarantee is the type and the click guard alone. Not decided |
| **56** | ~~**The Run A entry is unsigned: the month correction moved its content hash.**~~ Attempt 8 met the portal's refusal on `Sep`; correcting `startDateMonth`, `endDateMonth` and `awardDateMonth` to the select's own names changed what will be typed, which moves the hash and voids the signature — ADR-0057 working exactly as Vahid asked it to: *“I would rather that happened loudly than be worked around.”* The void approval is **removed**, not re-pointed: a hash written into `approvals.json` by anything but him is a signature he did not give. | — | — | **Closed by Vahid, 2026-09-22 (commit db5fe3d):** *“Run A: re-signed after the month-spelling correction.”* A fourth signature, on `sha256:56388e65…8f21ef`, his own account only. (The test asserting the refusal was left behind by that commit and made `main` red for eight hours; corrected in P186.) |
| **57** | ~~**Sheffield hides the two pre-completion document rows by a DATE COMPARISON, and `FieldCondition` cannot express it**~~ | — | **Decided B by Vahid, 2026-09-22 (ADR-0138):** *"the condition in the student's own terms, on the slot, signed. When the qualification's end is 'completed', the two pre-completion slots — `certificateStatus` and `transcriptStatus` with their files — are not planned, not previewed and not set."* A is out because *"a signed plan must not depend on the day it runs"*; C is out because it would make the preview false — the student authorised a statement the runner would then not make, while the page recorded a different one in its place. Built in P187 as `RequiredDocument.askedWhen`, answered per entry. `expected` sets both; `completed` and `discontinued` set neither — the page has no notion of `discontinued` and goes by the date, which for a qualification the student left is past. The divergence he accepted: a completed qualification awarded in the future would be shown the block, the save would leave two radios unanswered, the entry would be dropped silently, and the listing count reports *uncertain* |
| **54** | ~~**The institution Tom Select's construction is not captured, and it is the one thing that can say why `load()` never ran.**~~ From the vendored 2.3.1: `canLoad(q)` is `!!settings.load && !loadedSearches.hasOwnProperty(q)`, so a box whose instance has no `load` option asks nothing, silently, with no error — which is exactly what attempts 4 to 7 saw. `education.js` defines `loadInstitutionSearch` and never constructs anything; the `new TomSelect(…)` call, with `load`, `shouldLoad`, `preload`, `valueField`, `labelField` and `searchField`, is inline in `education.do`, and the 2026-09-21 capture holds only the two external scripts. | — | — | **Answered by Vahid, 2026-09-22**, from the live widget on his own account, nothing saved: no inline script builds it, but Tom Select keeps its instance on the element, so he read `#institution.tomselect.settings` directly — `load` IS set, with `shouldLoad: e => e.length > 0`, `loadThrottle: 300`, `valueField: "code"`, `labelField: "displayName"`, `searchField: []`, `openOnFocus: true`; `window.erasmusStudyAbroad` is `false`. Those settings ARE the fixture's construction in P184. (A first read returned *no tomselect* because his session had lapsed onto `overview.do`; he discarded it.) |
| **55** | ~~**A lookup recorded inside a box's fill window may belong to an earlier act, and the words say *asked* as though it did not.**~~ `watchPage` records on `response`, and `#lookups.mark()` is a position in that log, so an answer to a request made before the mark lands after it. Attempt 7's line read *“While this box was being filled the page asked the portal once: POST getGradingSystemsForCountry.do?institutionCode=(empty)”* — and the code's reading is that this was the COUNTRY choice's own consequence (`updateOriginalInput` → `change` → `institutionChanged()` → `loadGradingSystems()` with `#institution` still empty), arriving late. The line is not false about what was answered; it is wrong about what asked. Same family as the overclaim P181 corrected. | — | — | **Closed — ADR-0135 §4, P184.** The log records a request when it is ASKED, so positions are ask times and an earlier act's answer cannot fall into a later box's window. It also makes a state nobody could report visible: a request that went out and has not come back reads `no answer yet`, where before it read exactly like a request that was never made |

| **48** | **A stop that follows a resolution raises nothing, and the run disappears from every list.** Found by Vahid on Run A, 2026-09-21, the morning after he resolved the 18 September intervention `--did-not-happen` and attempt 3 stuck on the same page: three runs in the listing, one `uncertain` from that morning, and **no intervention for it** — while the two concluded cases still showed theirs (blocker 43, from the other side). Read from the code, then reproduced on the database: `idempotencyKeyFor` is `runId:action:target` with no attempt and no time (`packages/domain/src/workflow.ts:87`); `raise` inserts `ON CONFLICT … DO NOTHING` and, when nothing was inserted, answers with the row already there (`postgres-intervention.ts`); and migration 0003's `UNIQUE (run_id, idempotency_key)` carried **no condition on `resolved_at`**. So the uniqueness the comment described — one OPEN intervention per stuck action — was written as one intervention per stuck action **for ever**. The second stop collided with the resolved row, `#pause` read its `announcedAt` (18 September) and told the student nothing, and the status write moved the run to `uncertain` — outside `AUTOMATABLE_STATUSES`, so no later poll could reach it and raise what the first one swallowed. A paused application with an empty queue behind it. The resolution route cannot release it either: the collided row is resolved, so a second resolution is a 409 by design (ADR-0048). | me | Every run that stops twice on the same action — which is every second attempt at a page that failed once | **Closed — P177.** Migration 0007 makes the uniqueness partial (`WHERE resolved_at IS NULL`); `raise` infers that index and `findForAction` answers the OPEN one, as the port always said it did. Red first at both levels: the store contract, and the driver on the real database (the second episode raised nothing and `open()` was empty). For the runs it already happened to, `scripts/local-stack.sh raise-missing <conversationId>` — the missing caller, the same shape as ADR-0126's `finish-stopped`: it raises through `#pause` itself, refuses a run not held by a person, refuses one that already has an intervention, refuses one with nothing unfinished, and resolves nothing | — |


| **44** | **A case cancelled before P158 cannot be finished by P158, and stays at `WINDING_DOWN` with no caller.** Found by Vahid, 2026-09-18, pulling 412d001 and re-running the two calls: prior-outcome 200, re-application 403, unchanged. His own diagnosis, and correct: *"the fix changes what a new cancel does and nothing re-examines a case already half-cancelled … every case that stopped before 412d001 stays stuck for ever, and one of them is mine."* **Reproduced here** on the fixture, stopping a case the pre-412d001 way (the first act alone) and measuring all three doors shut: the Worker advances `AUTOMATABLE_STATUSES` only, so an `escalated` run is never picked up; `claimWork` offers a stopped case nothing; a second cancellation is refused by the transition table. The class is not closed by P158 either — a stop that lands with no blueprint on the binding, and a case legitimately waiting on a handover whose run is escalated, both end in the same place. | Vahid, 2026-09-18: a repair rather than abandoning the case — *"I am not looking for a fifth workaround"* | Every case stopped before 412d001, his own included | **Closed — ADR-0126, P159; walked by Vahid the same day, concluded on the first run.** `scripts/local-stack.sh finish-stopped <conversationId>`: refuses anything not at `WINDING_DOWN`, performs no transition of its own so the obligations guard is unchanged, idempotent | — |
| **45** | **The attached reader called two tracking pixels' iframe loads "the page tried to send the tab elsewhere".** Found by Vahid reading the as-runner capture (2026-09-18): `Navigations refused 2`, `tr.snapchat.com/cm/i` and `facebook.com/tr/`, which read as a lead — a page sending the tab off while a click is pending would match the symptom. Both are pixels in iframes (the second a POST); Playwright's `isNavigationRequest()` is true for a subframe's first load and the guard never checked the frame. The rule was right and the check was one frame short. It cost a round of attention, which is the kind of cost that adds up. | me | Every read that lists refused navigations | **Closed — ADR-0129, P162.** Only a main frame's navigation is a navigation refusal; a subframe's load falls to the host rule and is recorded as an off-host read; proved on a fixture pixel frame | — |
| **47** | **A cookie-consent overlay stands over the sign-in button, and dismissing it is a choice made on the student's account.** Measured by attempt 1 of the third conversation (2026-09-18): `div#ccc-overlay`, fixed, the full viewport, at the press; attempt 2 pressed through unread, so why it was absent is not known. Six options put with costs: 0 measure at every press and as the page opens; 1 stop and name it (what exists); 2 the student decides once per portal; 3 a fixed minimal choice; 4 do not load the consent script; 5 pre-set the consent cookie; 6 push past it. | Vahid — decided 2026-09-18 | Every run on a portal whose consent overlay wins the race with the press | **Decided; options 0 and 2 built (P164, P165).** His words: *"Option 0 always, option 2 on top of it. Not 3."* Option 0 first and unconditionally, because *"anything built on top of an unmeasured absence is built on a guess"*. Option 2 because *"a cookie choice is a choice made on the student's account, in their name, against an institution that may one day be asked what they consented to"*, with two conditions: per portal, durable, not permanent, visible and changeable by the student; and the question in the banner's own words, answerable by someone who does not know what a cookie banner is. Option 3 REFUSED, reason kept for the next time it is proposed: *"'We declined non-essential cookies for you' is still a choice we made. Telling the student afterwards is not the same as asking."* 5 refused — a fabricated consent record; 6 refused — an assertion that a person clicked where a person could not; 4 not built. Build order: P163 (the fill log), P164 (option 0), P165 (option 2), each estimated, stopped at twice. **Option 0 built — ADR-0130, P164. Option 2 built — ADR-0131, P165**, on the fixture: the mechanism is whole; what stays open is the Sheffield notice itself, never read (his capture had no notice; the runner carries no text), so the entry's `authentication.consent` waits on a read of the notice when it is present and on his signature. Until then a press it intercepts stops for a person, as attempt 1 did. **The notice read by Vahid, 2026-09-19:** two buttons with words (*Accept all cookies*, *Settings*) and a close control with none; what the close control does is not known and is never named (ADR-0131 amended, P166); option 2 as built CANNOT express *accept all, or a settings panel nobody has read* — recorded as option 2's limit (P167): its choices are each one press. His order: shape 1, a named path through the Settings panel (every button named and quoted, the path fixed), on his read of the panel; shape 2, *accept all, or do not apply through us on this portal*, kept available. **Shape 1 is refused by the panel itself (P168):** no save button, and three category toggles with no `id`, `name` or label text. Opening the panel writes `optionalCookies: {}` with `interactedWith: true` and a 90-day expiry; whether that is a durable refusal is settled by two reads (toggle state; the page's `initialState` per category) and, only if those leave it open, one press of *Close*. Shape 3 — a fixed path of controls that have words plus a read-back that verifies the recorded state — was **decided live by Vahid on 2026-09-19** after three reads and one press (opening Settings records the notice met with nothing accepted; closing leaves it standing; the notice does not return for 90 days) and **built in P170**. What stays open: the Sheffield entry itself, drafted in ADR-0131 and unsigned, and the fact that what *Close* records rests on one press, one account, one day. | — |
| **46** | **A fresh conversation cannot re-apply until a start has been refused in it, and nothing says so; the 409 and the advice name different prior cases.** Found by Vahid, 2026-09-18, third conversation, two concluded cases: `prior-outcome` 404, then `runs` 409 `already_applying` naming the FIRST case. Reproduced on the signed catalogue as the admitted student. `adviseReapplication` and `reapply` open with `bindings.caseFor(conversationId)`, so a conversation with no binding answers `unknown_conversation`; `withBinding` writes the binding and an empty `cases` row and COMMITS them even when the start is refused — the refusal is the side effect that makes the pair work. Then the prior case is found by the student (`#latestAttempt`): the advice names the latest attempt while the 409 names the submission key's holder, the first. Two routes, two prior cases, both true, and the page's bridge between them is blocker 41. The rule that a student's prior case is the student's is what the second step does; the first step still needs a target bound to THIS conversation, and today only a refused start binds one. | Vahid — whether a fresh conversation should be able to open the pair from the offer alone (bind on the offer, or let the pair take a target) | Every student starting a later attempt in a new conversation | **Open — raised 2026-09-18; the way through recorded in the Run A README (offer → refused start → the pair)** | — |
| **29** | ~~**A fifth period of residence in three years is dropped silently.**~~ **Decided and built by Vahid's word, 2026-09-16 (ADR-0119, P145):** *"do it now … A history that silently drops a period is the exact class of error this system exists to refuse, and leaving it because Run A will not hit it is how it stays."* The plan counts the blocks a page maps and refuses by name (`list_exceeds_form`) when the confirmed list is longer; the fill stops for a person with the count in the text; nothing is typed short. Proved on the Sheffield draft's four blocks. The row as it stood: **A fifth period of residence in three years is dropped silently.** Sheffield's nationality page takes four previous-country blocks for *"the countries you have lived in over the last 3 years"*; the interview asks for the last three years most recent first and the mapping types the first four (ADR-0115, decided 2026-09-15). A student with five periods in that window has one dropped, and nothing tells them. Vahid: *"That is not acceptable and I am not solving it now — raise it as its own item rather than letting the decision above bury it."* Options, none chosen: the preview says which periods reach the form and which do not; the plan refuses the page when the list exceeds the blocks; the interview caps at four and says so | You — in your words; then me | Any live fill for a student with more periods in three years than the page has blocks. Not on Run A's path unless the synthetic profile has five |
| **28** | ~~**A secure box the student cancels loops the account creation silently**~~ — found building ADR-0114. **Decided by Vahid, 2026-09-15 (ADR-0116):** *"a cancel is the student's stop. Not a reopen and not a person's problem."* — the run stops where it is, the student is told plainly and can carry on from there, and the record tells this stop from a portal's refusal and from a stop nobody was told about. **Built in P138** | Closed | — |
| **27** | ~~**The registry's `Qualification` has no start or end date, and the education page's four date selects are mandatory**~~ — **Decided by Vahid, 2026-09-14 (ADR-0112):** *"add the dates to Qualification. Same reasoning as employment."* A start and an end as month and year, matching the employment entry's; the award date held on its own and optional; confirmed by him 2026-09-15 and **built in P134** | Closed | — |
| **26** | ~~**A failed account creation is re-claimed without limit, and cannot succeed once the secret is spent**~~ — observed on the P121 journey. **Decided by Vahid, 2026-09-15 (ADR-0114):** *"Blocker 26: C, and the number is two."* — *"once is chance, twice is the portal"*; the intervention names which attempt and why, and the student is told at both points. **Built in P137:** the ledger counts attempts made and names the spent secret; the box reopens after the first failure; the second stops for a person. **Sheet:** [`decision-sheet-blocker-26-…md`](./decision-sheet-blocker-26-a-failed-account-creation-is-re-claimed-without-limit.md) (P136). **The sign-in half (P150, found; ADR-0120, decided and built P151):** the same loop on the resume path — no count, no message, a dead handle handed to the next runner until the outbox landed — closed the same way, two then stop, and the undistinguishable case (a wrong password from a portal fault) said to the student and to the person rather than guessed | Closed | — |
| **25** | **The institution box cannot be mapped by rule from a free-text profile field** — ADR-0109 makes a typeahead mapping name a recorded entry's value through an option rule; a student's institution is whatever they typed, and Sheffield's list is Sheffield's. Raised in P119 (`docs/distance-to-a-reviewed-sheffield-run.md`, item 5) | You — in your words; then me | The education page's mapping, and every portal with an institution lookup. Options named, none built: (A) the box as the student's own act; (B) an interview step offering the portal's entries; (C) a reviewed option map per institution **P142:** the same shape twice more on the nationality page — the five per-level UK-study qualification selects (the portal's list against the student's free-text qualification) and the nationality selects, whose partial maps are keyed on ISO codes while `identity.nationality` has no fixed vocabulary **P149:** for the synthetic profile only, at his word, option (C) as far as one reviewer typed: the institution box maps *University of Sheffield* → `SHEFFIELD` and nothing else (set 0.3.30). Run A on his account no longer hits this box; a real student still does, and the row stays open **P150:** the same for the nationality page's university-level qualification select, which the synthetic profile's UK study opens — one value named (set 0.3.31), the other four per-level selects untouched **Sheet:** [`decision-sheet-blocker-25-…md`](./decision-sheet-blocker-25-a-portals-own-list-against-the-students-words.md) (P143) |
| **24** | ~~**The deferred state after the handover**~~ — Sheet: [`decision-sheet-blocker-24-the-deferred-state-after-the-handover.md`](./decision-sheet-blocker-24-the-deferred-state-after-the-handover.md) | — | **Decided A by Vahid, 2026-09-12 (ADR-0108):** the durable record on the case, closed by the student's word, as the floor; the reminder out on its own terms — *"a reminder that fails silently leaves the student worse off than no reminder, because we have now implied someone is watching. Better that the record says plainly nobody is."* Built in P107 |
| **21** | ~~**A document per item of a repeating page, and a condition inside one**~~ — Sheet: [`decision-sheet-blocker-21-a-document-per-item-of-a-repeating-page.md`](./decision-sheet-blocker-21-a-document-per-item-of-a-repeating-page.md) | — | **Decided by Vahid, 2026-09-11, and built in P98 (ADR-0104): B, with the per-item condition.** *"C is out. The education page is the heart of a university application."* A repeating page's document slots are the student's own act, said under each entry in the preview and at the handover; a condition inside a repeat is answered per item. **A is recorded as an option, not the end state** — his correction of the sheet's *"B now, A when the registry holds education"*: *"that is a product decision I have not made and am not making by answering this sheet."* The Sheffield education page is marked repeating with its six slots left to the student |
| **9** | ~~**`attach_document` intent identity**~~ | — | **Done in P73 (ADR-0069's built note).** One intent per upload at the claim, keyed `page/field=documentId@hash`, settled from the runner's report for this run's case only, with `document_transmissions` as the record of what left. No new `WorkKind` was needed: an attachment is part of the page item that carries it. Proved against a served page in P74 |
| **10** | **The AskiMate production integration** | Access, then me | The real conversational entry point. ADRs 0001–0002 are **Accepted** and describe an integration that has not been built — P49 corrected the claim that they were Proposed |
| **11** | **Authenticated specialist identity** | You, then me | Nothing today — one operator. ADR-0048 §3's condition for making it a release blocker is a *second* specialist existing at all |
| **12** | ~~**Accept or revise ADRs 0001–0004**~~ | — | **Already answered on 2026-08-26, and this blocker should never have existed.** All four were accepted with Phase 0; a partial index edit fifteen minutes later missed the rows, and this list then asked Vahid to decide it again. Corrected in P49 (ADR-0083), and now checked |
| **13** | ~~Arm the submission key, and make re-application reachable~~ | — | **Done in P38.** Both, together: the key is claimed at case-open and a refused second application has a route to a second case |
| **14** | ~~A declared capability with no production caller fails the build~~ | — | **Done in P39 (ADR-0073).** Six entries on the reviewed unreachable list, each with a reason and what would close it |
| **15** | ~~`start` on an ESCALATED run throws rather than resuming~~ | — | **Decided by Vahid and done in P40 (ADR-0074):** the student stays in the same conversation, and lands back in it |

**Not blocked and available to work on now:** nothing on this list — 12 turned out to be already
answered and 15 was done in P40. The ADR re-audit that used to sit here was done in P37; see
[`p37-adr-audit.md`](./p37-adr-audit.md); its two open findings were closed in P38.

---

## 7 · Test and verification state

**2,829 tests · 145 files · zero skipped · zero pending**, run against real PostgreSQL 16 and real
Redis (`--save "" --appendonly no --maxmemory-policy noeviction`). `pnpm run verify` chains
typecheck → lint → dependency boundaries → version check → tests; CI runs it plus a separate
integration job.

**Two lanes since P47** (ADR-0081). `vitest.workspace.ts` runs the browser files (sixteen then, twenty-one now) one at a
time and everything else in parallel, because three or four browsers landing together on a four-CPU
container starved pages past a twenty-second poll and failed two full runs in five — each on a
different test, each of which passed 4/4 alone. Peak Chromium processes 21 → 7, peak load 5.13 →
3.13, wall time 119s → 176s. No assertion or timeout was changed to buy it.

<!-- census:begin — generated by `pnpm run census`, do not edit by hand -->

**2,829 tests**, by the workspace they live in. Generated — run
`pnpm run census` after changing the suite. The rows and *everything else* sum to the total
exactly; the figure this replaced was approximate and had drifted 136 tests without anyone
being able to see it (ADR-0084).

| Area | Tests | Area | Tests |
|---|---|---|---|
| `apps/conversation-service` | 450 | `packages/profile` | 62 |
| `apps/browser-runner` | 391 | `packages/catalogue` | 58 |
| `packages/domain` | 383 | `packages/preparation` | 58 |
| `scripts` | 356 | `packages/conversation` | 52 |
| `packages/case-store` | 156 | `packages/disclosure` | 47 |
| `packages/orchestrator` | 133 | `packages/interview` | 43 |
| `packages/documents` | 100 | `packages/extraction` | 28 |
| `packages/contracts` | 92 | `apps/secure-filler` | 22 |
| `packages/mapping` | 83 | `packages/requirements` | 22 |
| `apps/secure-service` | 72 | `apps/worker` | 21 |
| `packages/secrets` | 69 | everything else | 64 |
| `packages/account` | 67 |  |  |

<!-- census:end -->

### What is genuinely covered

Not "a test exists" but "removing the control fails a test". Every phase since P10 has ended by
deliberately breaking the thing it built and confirming the suite notices. The properties that hold
under mutation include: a password reaching no database column, log or model prompt (asserted by
scanning every column of every row, and every HTTP body on every wire); a crash not producing a
second account; the intent ledger refusing a duplicate consequential action; approvals bound to
content; the five transmission refusals; both storage gates; the three attachment-identity
components; and that a specialist notice carries no student identifier and no free text from the
failure.

**A guard on the demo (P37):** `scripts/walkthrough.test.ts` runs `pnpm run walkthrough` in a real
process and fails on a non-zero exit. The walkthrough declares an expectation per step, so a change
to the state machine that quietly stops it demonstrating what it claims is now a failing test rather
than nine lines of "REFUSED" nobody reads.

**A guard on the guard:** `scripts/ci-guard.test.ts` starts each database-backed suite *without* a
database and asserts it **fails** rather than skipping. A silent skip is how a security proof
evaporates.

### Known coverage gaps — stated, not hidden

1. **The surviving mutation.** Replacing `work.caseId` with a constant in
   `apps/browser-runner/src/fill-application.ts` passes all 204 browser-runner tests. `toStoredPlan`
   refuses any plan with uploads, so the runner's `plan.uploads` is always empty and the upload gate
   is never reached there. **This is the transport gap appearing as a coverage gap, not a defect** —
   the wiring becomes reachable, and testable, the day something can supply a document.
2. **Everything portal-facing is fixture-only.** The fixture portal is well-behaved by construction.
   A real portal will drift, rate-limit, use CAPTCHA and change its DOM.
3. **No model is exercised.** Every LLM path runs against a fake. The prompts have never met Bedrock.
   **2b. A repeating page's entries are counted, never read back** (ADR-0106 §2, stated as a limit
   by Vahid on 2026-09-14): nothing confirms a saved qualification's or job's values, only that
   one more entry exists. Sheffield offers an edit view of a saved entry, so a read-back is
   possible there in principle; not built, not decided.
4. **Type-level properties need source assertions.** `StorableUpload`'s brand cannot be broken by a
   behavioural test — widening `store`'s signature back passes every behavioural test and is caught
   by a source assertion. Recorded as the honest form for a property that lives in a type.
5. **`packages/documents`, `packages/extraction` and `packages/requirements` are tested in isolation
   only** — no integration test crosses from a deployable into any of them, because none can.

---

## 8 · Infrastructure and cost

| | |
|---|---|
| **AWS credit** | ~$1,000, per ADR-0018 |
| **Spent** | No longer $0.00. A customer-managed key carries a flat monthly charge and two verification runs (2026-09-09) made a handful of S3 and KMS requests. The amount is read from billing, not written here |
| **Provisioned** | **The vault's bucket and key** (created by Vahid, 2026-09-09, ADR-0092 §4): `askimate-aas-vault-4471` in `eu-west-2` with block-public-access, TLS-only and SSE-KMS; the CMK; a verification role, since revoked. No RDS, no ElastiCache, no compute, no VPC. Nothing is deployed |
| **Region, when it happens** | `eu-west-2` (London), ADR-0012 |
| **Model provider** | Amazon Bedrock, ADR-0018 — adapter built, **no model selected**, no credentials |
| **Running cost today** | £0. Everything runs locally and in GitHub Actions |
| **CI** | GitHub Actions, two jobs, ~3 minutes, within the free allowance |

**The first spend will be Bedrock inference**, and it will be small — the interview is the only
high-volume workload, and ADR-0018 already flags it as the row where a cheaper model earns the most.
**The first material spend will be storage**, and B5 *is* answered "hold" (ADR-0078): S3 with a
customer-managed KMS key, plus RDS if the databases move off anything self-hosted. Both are tens of
dollars a month at this scale, not hundreds. **The credit is not the constraint. The policy
determinations are.**

---

## 9 · The three things I would fix first, given free choice

### 1 · ~~An alerting transport for interventions~~ — **done in P36 (ADR-0071)**

The Background Worker now sends a `SpecialistNotice` to a configured webhook, once per open
intervention, with a `notified_at` marker so a stopped run is paged once rather than every fifteen
seconds. The notice carries identifiers and categories and no student — its destination is outside
every boundary this repository controls.

**What it did not do**, and is the honest remainder: the specialist is still asserted rather than
authenticated (ADR-0048 §3, unchanged, and its ending condition is a second specialist existing);
the request is not signed; and there is no backoff or dead-letter, deliberately.

*The replacement for this slot, and now the first unblocked item:* **re-auditing ADRs 0005–0021** —
see item 3, promoted in practice by five consecutive phases finding an older ADR asserting a
guarantee the code did not provide.

### 2 · The attachment path — measured on 2026-09-10, and the order it has to be built in

The transport now ends at a held document (ADR-0095, ADR-0096). What is missing is the document
reaching a portal, and P62's scoping measured exactly where the path is cut. Five cuts, each a fact
about the code today:

1. **The preview never sees a document.** `RunDriver` hands the orchestrator `documents: new Map()`
   (`run-driver.ts:1712`), so a mapping with a `document` source stops the run at
   `document_missing` before the student is ever asked. Nothing looks a held document up by the
   reviewed mapping's `documentRef`.
2. **A plan with uploads does not cross to the runner.** `toStoredPlan` refuses `has_uploads`
   (`plan-transport.ts`), because the runner is forbidden the documents package and may hold none.
3. **No `DisclosureRequestRecord` is ever constructed.** `authoriseDisclosure` has no production
   caller (the register). Determination 3 (ADR-0087) says what the authorisation instrument IS —
   *"the preview a student reads, the authorisation text, and the content hash … registers them as
   required"* — and nothing yet builds the record from those three.
4. **The runner has no `DocumentSource`.** `executePlan` takes one; `fill-application.ts` supplies
   none, and the runner holds no vault credential (ADR-0042) — it must be handed a retrieval URL
   and the authorisation, by the service, under its lease.
5. **`attach_document` has no intent.** Uploads ride `advance_portal_page`, whose target is computed
   from `plan.instructions` only; replacing a passport does not change the key, while the action's
   own comment says *"Duplicates are visible to admissions."* Identity frozen in ADR-0069:
   `(fieldRef, documentRef, contentHash)`.

The order is forced by the dependencies, and it goes through the transmission gate, never round it:

| Slice | What | Gate it keeps |
|---|---|---|
| **a** ✅ | **Built in P64 (ADR-0097).** The driver supplies the student's held documents to the preview, keyed by the reviewed mapping's `documentRef` (the domain document type). The preview then names each attachment, and the `authorise` decision covers `(fieldRef, documentRef, contentHash)` as ADR-0069 froze it | ADR-0057/0059 — the authorisation binds to content |
| **b** ✅ | **Built in P65 (ADR-0098), on Vahid's decision.** The preview's presented text carries, per attachment, which document, going where and for what, and the destination host is inside the content hash, so the recorded `AuthorisationCaptured` IS the specific student authorisation determination 3 requires. `StudentDisclosureAuthorisation` is built from that event in slice c: `presentedText` = the preview, `method` = `chat_affirmation` | ADR-0022 — no `consented: boolean`; the text names what, where and for what |
| **c** ✅ | **Built in P66 (ADR-0099).** Plan transport carries uploads as **references** (`fieldRef`, `documentRef`, locators; no bytes, no ids). The runner asks the service, under its lease, for each `documentRef`; the service builds the `DisclosureRequestRecord` from the case's authorisation, runs `authoriseDisclosure` and `mayTransmit` **with the case**, and answers a sixty-second retrieval URL plus the authorisation record. The runner's `documentSourceFor` re-runs the gate before `executePlan` runs `mayTransmit` again | ADR-0069 — the case binding, checked server-side before any URL exists |
| **d** | ~~The runner's `DocumentSource` fetches the bytes and hands `executePlan` the `AuthorisedDocument`~~ — built in P66 as `documentSourceFor`. What remains of d is the runner's ENTRY POINT performing execute work at all: it performs `create_account` only (blocker 19), so the source has no production caller yet | ADR-0022 — the gate at the moment of sending |
| **e** | `attach_document` intent per upload, target `(fieldRef, documentRef, contentHash)`, written at claim beside the page intents; `assessIntent` consults it; `TransmissionRecord` written from the runner's report | ADR-0054 — verify first, never repeat |

Slices a and b are the Conversation Service alone. c is the first to change what crosses to the
runner, and the first that must not be built without b: a retrieval URL minted for a document no
specific authorisation names is the two-line failure ADR-0022 was written against. Each slice is a
phase with its own ADR; none weakens `mayTransmit`'s case check, the content hash, or the
mandatory-review categories, which are Vahid's hard limits on this work.

**Decided by Vahid, 2026-09-10 (ADR-0098):** *"One authorisation, over a preview that names each
attachment separately."* With the condition that *"the preview must name each attachment plainly —
which document, going where, for what."* Slice b is built to that condition.

### 3 · ~~Re-audit the oldest Accepted ADRs~~ — **done in P37 (ADR-0072)** · ~~close the four Proposed ones~~ — **there were none (P49)**

This recommendation rested on a stale index. ADRs 0001–0004 were accepted on 2026-08-26 with Phase 0;
a partial index edit fifteen minutes later missed four rows, and the omission propagated here. The
observation underneath it was still right — branded types and versioned migrations are among the most
load-bearing decisions in the system — but the conclusion, that they needed accepting, was not.

More importantly, the last five phases each found that an **older ADR asserted a guarantee the code
did not provide** — ADR-0022 on storage (P31), the transmission gate's case check (P34), the
`documentRef` ambiguity (P35), and before those ADR-0038's verified-email guard and ADR-0045 §4's
crash detection. That is now a pattern rather than a coincidence, and the remaining unaudited
records are the oldest ones. I would read ADRs 0005–0021 against the code, in order, and write down
every sentence that is not true today. On the evidence, I expect to find two or three more.

---

## Appendix · Where to look

| Question | Document |
|---|---|
| What happened in one phase | `CHANGELOG.md`, and `docs/where-we-are.md` for the narrative |
| Why a decision was made | `docs/decisions/` — the index in `README.md` carries the amendment chain |
| What each deployable does at startup | `docs/deployables.md` |
| The document transport decision | `docs/document-transport-options.md` + the B5 decision sheet |
| What is unresolved about retention | `docs/retention-analysis.md` + the B1 decision sheet |
| What a controlled live run still needs | `docs/what-a-controlled-live-run-needs.md` |
| How the regressions for a phase were run | `docs/p<N>-regression-audit.md` |
